# EPIC-4 · 遗迹可探索 + 存档落地（W7-8，19h）

> 里程碑门 M4：遗迹三层 + 宝箱/调查 + 存读档（单槽）；存档在此必须存在（A8 行 4）
> 正本：探索 GDD（`design/gdd/map-exploration-gdd.md`）、ADR-3。

## Story 列表

### E4-S1 SaveManager 完整实现 · 3.5h
- 需求依据：ADR-3（JSON 手写 schema、version 字段、原子写）；探索 GDD §3.4（原子写 = 临时文件 + rename，失败保留旧档）。
- 做什么：schema 全字段（map/position/party/story_phase/flags/chests_opened/discovered_weakness_set/cleared_enemy_set + version）；读档回灌 GameData；原子写。
- 验收标准：
  - [ ] 存→读→存往返数据无损
  - [ ] 写入中途强杀进程，旧档可正常读出（任务管理器杀进程实测）
  - [ ] version 字段 + 迁移函数骨架就位

### E4-S2 道路地图 · 3h
- 需求依据：探索 GDD §3.1 道路行（48×64、宝箱 2/调查 3/敌人 3 巡逻、密度 ≈每 8s 一发现）；§3.1 密度校验口径。
- 做什么：蜿蜒路线 + 地标（断桥/石像）；B1/B2 敌编组摆位（战斗 GDD §7）。
- 验收标准：
  - [ ] 实测走图 ~60s 主路径，8 个发现点间距达标
  - [ ] spawn 点周围 8 格无敌人初始位、无碰撞（§3.4 制作校验项）

### E4-S3 遗迹三层地图 · 5h（实际 ≈1h，2026-08-30；工具链与蛇形链结构配方复用 E4-S2 抵扣）
- 需求依据：探索 GDD §3.1（f1 56×44 / f2 48×48 / f3 40×40；三层共用遗迹 tileset 与结构模板）；§3.2（f2 有定守精英）。
- 做什么：三层各一场景，开阔厅 + 窄走廊结构（降低卡墙概率，服务"甩掉敌人"乐趣）；f3 为 Boss 前厅（不放普通敌人）。
- 验收标准：
  - [x] 三层纵向推进的"往深处去"暗示成立（结构自查）
  - [x] 各层宝箱/调查点数量对表（3+2+1 / 2 / 1；调查 4/3/2）
  - [x] f2 精英定守位即交战位

  > 施工证据：`evidence/e4s3-ruins-maps.md`（verify 156 项全绿 + GUT 229/229 基线 221 零回归 + 8 新用例）。

### E4-S4 敌人三态完整 AI · 1.5h（实际 ≈0.5h，2026-08-30；态机骨架与三态参数锚点在 E2-S2 基座已预埋）
- 需求依据：探索 GDD §3.2（追击：4 tile 半径 ±90° 伪视野、速 3 tile/s、卡墙 1.5s 或拉开 6 tile 放弃；定守）；边缘情况 1。
- 做什么：在 E2-S2 巡逻基础上补追击/放弃/回位与定守态。
- 验收标准：
  - [x] 追击/放弃阈值生效，被墙卡住能回位
  - [x] 敌人间互穿无碰撞（边缘 1）

  > 施工证据：`evidence/e4s4-enemy-ai.md`（GUT 240/240 基线 229 零回归 + 11 新用例；visible_enemy.gd 单文件 195→383 行，场景件零改动）。

### E4-S5 宝箱/调查事件模板 · 2.5h
- 需求依据：探索 GDD §3.3（宝箱 = 事件模板：not_flag → play_sfx + give_item + dialogue + set_flag；开启状态走 chests_opened）；对话 GDD §3.2（动作白名单）。
- 做什么：宝箱/调查两个模板事件 + 全部 27 个内容点位 JSON（9 宝箱 + 18 调查，文案可占位）；依赖 dialogue/give_item/set_flag 三动作的最小集——JSON 加载器若未提前就绪，此条允许先走硬编码触发器、E5 完成后回迁。
- 验收标准：
  - [ ] 27 点位全部可交互，开启状态入存档（存→读→已开不重开）
  - [ ] 调查文案至少 50% 为最终文案（一半氛围一半趣味）

### E4-S6 传送网络 + 进图自动存档 · 2.5h
- 需求依据：探索 GDD §3.4（12 处传送、"过传送点存，不进图即存"精确时序：teleport → 载入 → 落位 → map_ready → save()）；架构 A5。
- 做什么：trigger_teleport 薄壳 + 全部传送事件；map_ready 信号接线 SaveManager。
- 验收标准：
  - [ ] 12 处传送全通，每处 spawn 落位正确
  - [ ] 自动存档时机实测 = 站在新图入口（非转场中间态）
  - [ ] 存档时右下角图标闪现 0.5s（探索 GDD §4）

### E4-S7 失败读档打通 · 1h
- 需求依据：战斗 GDD §3.5 + 对齐裁决 1（失败 → 自动读档至进图存档点）；探索 GDD I3。
- 做什么：替换 E3-S3 的占位失败流程：读 save.json → GameData 回灌 → Router 回图。
- 验收标准：
  - [ ] 任意地图战败后回到该图入口存档点，状态=存档时状态
  - [ ] 读档后 DialogueRunner 强制 IDLE（对话 GDD 边缘 5，此时系统未建则预留）

## M4 收口
- A8 行 4 全绿：三层遗迹探索 + 单槽存读档可演示；"存档/读档"Must 条目（5h）关闭。
- 试玩视频 #4 + git tag `m4`。
- 工时口径：本 Epic 19h = 探索余量（22h − E1/E2 已耗 ~8h）+ SaveManager 3.5h；结算计入 README 总账。
