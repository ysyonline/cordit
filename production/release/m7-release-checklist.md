# M7 发布清单 · 《轨迹残响》垂直切片

> 编制：路远行（release-ops-lead）｜日期：2026-09-05｜状态：**待用户拍板后执行**
> 依据：`production/epics/EPIC-7.md`、`production/README.md` 第 3 节里程碑门、`export_presets.cfg`（commit 9cc0de5）、`git log m6..HEAD`、`git status`（实查）
> 定位前提：本作是**个人作品集性质的 30 分钟垂直切片，不是商业发行**。本清单按"相称"原则裁剪——不设多级渠道、不做多语言、不做热更与社区矩阵；但**署名合规与可复现导出这两项法律/工程义务不放松**。

---

## 1. 版本号定义（建议：`v0.1.0-slice`）

### 1.1 结论

**对外版本号：`v0.1.0-slice`**
**内部里程碑标签：`m7`（沿用 m1–m6 序列，照 `production/README.md` §3/§5 纪律打）**

两者并存、不冲突：`m7` 回答"第几个里程碑"，`v0.1.0-slice` 回答"对外这是第几个可玩版本"。

### 1.2 为什么不是 v1.0

1.0 在 SemVer 与玩家心智里都等于"功能完整的正式版"。这一版是**4 张地图 + 5 场战斗 + 一条主线**的 30 分钟切片：无音效（`assets/sfx/` 为空，`chest_open.ogg` 路径悬空）、无设置菜单、无多语言、无窗口/音量选项、敌人与角色均为占位色块、单存档槽。叫 1.0 会让作品集评审按"完整游戏"标准衡量，反而扣分。

### 1.3 为什么不是 v0.0.x

0.0.x 通常指"跑不起来的内部原型"。这一版有明确的开头与结尾、可完整通关、有存读档、有可双击运行的 Windows 包——它是**第一个可被外人从头玩到尾的版本**，够得上 0.1.0。

### 1.4 为什么带 `-slice` 后缀

不带后缀的 `v0.1.0` 容易被读成"抢先体验版/试玩 Demo，后续会有完整版"。`-slice` 是给读者的一句话免责声明：**这就是交付物本身，不是成品的前哨**。作品集场景下这个区分很重要——评审看到的是"一个有头有尾的垂直切片"，不是"一个做了一半的游戏"。

### 1.5 备选方案与不推荐的理由

| 备选 | 不推荐理由 |
|---|---|
| `v1.0-slice` | 主版本号仍然宣称"完整"，与 `-slice` 自相矛盾 |
| 直接用 `m7` | 里程碑编号是内部排期语言，读者看不出"这是第几个可玩版本"，且下一个切片会变成 `m8`，版本号无法表达版本间关系 |
| `2026-09-05` 日期版 | 无版本间可比性，无法回答"这一版比上一版新在哪" |
| 不打版本号 | 试玩者反馈将无法归因到具体版本（"你玩的是哪一版？"），M7 之后的任何修复都失去参照系 |

### 1.6 版本号落地动作（3 项，均非阻塞）

- [ ] `export_presets.cfg` 回填 `application/product_version="0.1.0"` 与 `application/file_version="0.1.0"`（当前为空，exe 文件属性里看不到版本）
- [ ] `application/product_name` 已为"轨迹残响" ✓；建议补 `application/company_name` 与 `application/copyright`（署名 / 年份，作品集场景下能强化归属）
- [ ] 分发包文件名统一为 `轨迹残响-v0.1.0-slice-win64.zip`

---

## 2. 产物清单（`export/win/`，实查）

| 文件 | 字节 | 体积 | 作用 | 是否可单独分发 |
|---|---:|---|---|---|
| `轨迹残响.exe` | 109,132,288 | 104.1 MiB | Windows x64 启动器（含 Godot 4.7.2 运行时） | **否** |
| `轨迹残响.pck` | 3,132,568 | 2.99 MiB | 游戏资源包（场景/脚本/数据/素材） | **否** |
| **合计** | **112,264,856** | **107.1 MiB** | | |

### 2.1 关于 exe/pck 成对约束（分发时的硬约束）

`export_presets.cfg` 中 `binary_format/embed_pck=false` —— pck **未嵌入** exe。因此：

- 两个文件**必须同目录、主文件名一致**（`轨迹残响.exe` + `轨迹残响.pck`），缺其一则启动失败。
- 打包 zip 时必须两个一起打，且**保留中文文件名**（改名会破坏匹配关系，除非同时改两个文件的名字）。
- 若希望"单文件 exe 更好发"，可后续改 `embed_pck=true` 重导（体积相近，约 107 MiB）——但 100 MB 单文件对网盘/邮件并不比 zip 更友好，**建议保持现状**。

### 2.2 pck 内容核验（合规关键，实查 pck 二进制目录段）

**确认已入包的授权素材**（均为 CREDITS A/B 区已登记项）：

- `assets/tiles/town_tiles.png`、`forest_tiles.png`、`classical_temple_tiles.png`、`16oga.png`
- `assets/characters/charsets_12_m-f_complete_by_antifarea.png`、`charsets_12_m-f_antifarea_bright.png`
- `assets/faces/48x48_Faces_1st_Sheet_Update_CharlesGabriel_OGA.png`
- `assets/fonts/fusion-pixel-12px-proportional-zh_hans.ttf`（+ 其 `.fontdata`）

**确认未入包（排除规则生效）**：

- `castle` 关键字在 pck 内命中 **0 次** → bart 城堡件（GPL/SA 红线）**未进分发包** ✓
- `DawnLike` / `Kenney` 命中 **0 次** → 未取用素材未进包 ✓
- `design/**`、`tests/**`、`evidence/**`、`tools/**`、`docs/**`、`production/**`、`addons/gut/**` 全部按 `exclude_filter` 排除 ✓

**pck 内缺失项（需要注意）**：

- 无任何 `licenses/` 文件 → **许可全文不随包分发**，CC-BY 署名与 OFL"保留许可全文"义务在游戏包内没有落点（详见 `m7-credits-audit.md` 第 4 节）
- 无任何音频文件 → `data/json/events/chests.json` 引用的 `res://assets/audio/sfx/chest_open.ogg` 为悬空路径（运行时静默失败，不影响流程）

---

## 3. 源码与产物的分发方式建议

前提：`export/` 已被 `.gitignore` 排除，仓库内只有 `export_presets.cfg`（commit 9cc0de5 入库）。这是**正确且应保持**的——不要让 100 MiB 二进制进 git 历史。

### 3.1 三条分发路径

| # | 分发对象 | 载体 | 内容 | 建议 |
|---|---|---|---|---|
| A | **试玩者 / 外部测试者** | 网盘直链或私发 zip | `轨迹残响.exe` + `轨迹残响.pck` + 随包 `怎么玩.txt` | 走"试玩者交付包"，由 quality-lead 主责（本清单只留接口，不重复） |
| B | **作品集展示** | 公开仓库 + Release 附件 | 仓库（源码 + `export_presets.cfg` + 文档）+ Release 挂 `轨迹残响-v0.1.0-slice-win64.zip` + 试玩视频 #1–#7 | **强烈建议**这一条：评审既能看代码又能一键下载试玩 |
| C | **归档** | git tag | `m7`（里程碑）+ `v0.1.0-slice`（对外版本） | 双标签，互不替代 |

### 3.2 关键建议

1. **Release 附件挂 zip，不要把 `export/` 提交进仓库** —— 保持 `.gitignore` 现状。
2. **克隆可复现性是资产，要在 README 里写出来** —— `export_presets.cfg` 入库意味着任何人 clone 后装 Godot 4.7.2 即可一键重导。这是作品集的加分项，值得在仓库 README 明写一句。
3. **导出脚本 `tools/dev/` 目前未入库**（`git status` 显示 `?? tools/dev/`）。若要让"clone 后可复现"名副其实，建议随 M7 提交一并纳入版本管理（含 `e7s3_install_export.ps1`/`e7s3_pipeline.ps1`）。注意：`evidence/e7s3-export.log` 记录了一次失败（"给定的导出路径不存在"），正因脚本先建目录再导出才修好——**把脚本入库，这段踩坑才有价值**。
4. **随包必须附带一份 `CREDITS.txt`** —— 内容取 `assets/CREDITS.md` 第 D 区致谢条目（游戏内无致谢画面，见审计文档）。

---

## 4. 发布前置检查项

> 图例：**[阻塞]** = 未清不放行（go/no-go 门禁）；**[可延后]** = 不阻塞 M7 门，可入下阶段或明确记录为已知未达项。

### 4.1 A. 构建与产物

- [ ] **[阻塞]** A1 工作区 18 项变更提交入库 —— 现状：**未提交**。
  - **好消息（已实查验证）**：现有 `export/win/` 产出时点 11:14:56，**晚于**全部工作区改动（B4/B5 数值 08:54、剧情三文件 09:56）。且 pck 内可直接检索到实写剧情原文（"石门上的封条"、"魔力浓度不对劲"、"第一个，还有五个" 各命中 1 次，而占位标记"P2 遗迹入口·占位"命中 **0** 次）——**证明当前磁盘上的包已包含 B4/B5 调校与 E7-S2 剧情实写，可以直接送测，不必先重导。**
  - **但提交仍然阻塞**：① tag 只能打在 commit 上；② 未提交的工作区无法被他人复现、也无法被审计；③ 当前产物不对应任何 commit hash，**一旦后续发现问题无法回溯代码状态**。
- [ ] **[阻塞]** A2 提交后重导一次并复跑 headless 冒烟 EXIT=0（确保"已提交的代码"与"已分发的包"严格对应）
- [ ] **[阻塞]** A3 GUT 全量 PASS（当前 514/514，提交后复跑确认）
- [ ] [可延后] A4 exe 文件属性写入版本号与版权信息（§1.6）
- [ ] [可延后] A5 应用图标替换 —— 现状：pck 内 `icon.svg` 注释自述为"**临时占位项目图标**（TASK-13）"，而 `export_presets.cfg` 的 `application/icon` 为空（回落到 `project.godot` 的 `config/icon`）。**exe 的图标目前是一个临时占位图**，作品集场景下这是第一眼印象，值得花 15 分钟换掉。
- [ ] [可延后] A6 包体积优化：剔除未被引用的 `16oga.png`、原样 `charsets_12_m-f_complete_by_antifarea.png`、全量 6.7 MB 字体（合计可省约 7 MB 源，占现包体 2.99 MiB 的大头）——**切片阶段不值得做，除非网盘限速**

### 4.2 B. 运行时验证（EPIC-7 E7-S3 验收标准原文）

- [ ] **[阻塞]** B1 **导出包在非开发机可启动、可完整玩到结尾** —— 现状：**未做**。这是 E7-S3 验收第 1 条，也是"Windows 导出包"这条 Story 的定义本身。
- [ ] **[阻塞]** B2 **存档读写正常（`user://` 路径在打包环境实测）** —— 现状：**未做**。E7-S3 验收第 2 条。打包环境的 `user://` 指向 `%APPDATA%/Godot/app_userdata/轨迹残响/`，与编辑器环境不同路径，**必须实测**。
- [ ] **[阻塞]** B3 中文文本在目标机正常显示 —— 现状：项目**未配置自定义字体**（`project.godot` 无 theme/自定义 font 节点，`assets/fonts/` 的 Fusion Pixel 字体未被任何场景或主题引用），中文依赖 Windows 系统字体回退。开发机"能看"不等于目标机"能看"，**非开发机实测时必须逐屏确认无豆腐块**。
- [ ] [可延后] B4 窗口缩放/全屏表现核验（640×360 canvas_items + integer 拉伸，多分辨率显示器下可能留黑边）

### 4.3 C. 内容与验收（EPIC-7 其余 Story）

- [ ] **[阻塞]** C1 **E7-S1 三问测试过线** —— 其中"至少 1 名外部测试者在 B4 后主动说'这场有点意思'"为验收明文项。现状：外部测试者反馈**未拿到**。依赖 quality-lead 的试玩者交付包，本清单记为**外部依赖阻塞**。
- [ ] **[阻塞]** C2 E7-S2 三份对话 JSON 实装后 GUT 通过（现状 514/514）+ 结尾钩子经外部确认
- [ ] **[阻塞]** C3 E7-S4：`CREDITS 与实际入库资产一致（0×SA、0×GPL）` —— 现状：**不一致**（B 区 2 条悬空登记）+ **bart 城堡件在仓库内**（P0，详见审计文档第 3 节）。
- [ ] [可延后] C4 7 段试玩视频归档（#1–#7）—— 属 E7-S4 范围内，但归档动作本身不阻塞打 tag

### 4.4 D. 合规与署名（法律义务，不因"只是切片"放宽）

- [ ] **[阻塞]** D1 bart 城堡件（`design/art-bible/mockup/_src/castle_tiles.png`、`castle_tiles2.png`）**在仓库内**——阻塞"仓库公开发布"（分发路径 B）。不阻塞导出包分发（已被 `design/**` 排除，实测 pck 内 0 命中）。（P0，已点名，见审计文档）
- [ ] **[阻塞]** D2 随分发包附带许可全文与署名文本 —— 现状：pck 内无 `licenses/`、无 `CREDITS.txt`、游戏内无致谢画面。OFL 的"随分发保留许可全文"与 CC-BY 的"署名"义务目前**只有一个仓库内的 md 文件作为落点**，随包分发即失位。
- [ ] [可延后] D3 游戏内致谢画面（CREDITS D 区四行式条目的最终归宿）—— 切片阶段用随包 `CREDITS.txt` + 仓库 README 已可满足"合理位置署名"

### 4.5 E. 版本与仓库

- [ ] **[阻塞]** E1 工作区 18 项变更提交入库（同 A1，此处不重复勾选理由）
- [ ] **[阻塞]** E2 打 tag `m7` + `v0.1.0-slice`（**须用户明确指令，本清单不代执行**）
- [ ] [可延后] E3 仓库 README 补"版本 / 如何复现导出 / 素材致谢链接"三节

---

## 5. go/no-go 判定表

| 门禁项 | 现状 | 判定 |
|---|---|---|
| 构建可复现（`export_presets.cfg` 入库） | ✓ 已入库（commit 9cc0de5） | **GO** |
| 现有包内容为最新 | ✓ 已验证：包内检索到实写剧情原文，占位标记命中 0 | **GO（可直接送测）** |
| headless 冒烟 | ✓ EXIT=0 | **GO（待重导后复验）** |
| GUT 全量 | ✓ 514/514 | **GO（待提交后复跑）** |
| 产物可回溯到具体 commit | ✗ 工作区 18 项未提交，包不对应任何 hash | **NO-GO** |
| 非开发机实测（启动+通关+存档） | ✗ **未做** | **NO-GO** |
| 外部测试者三问反馈 | ✗ **未拿到** | **NO-GO（外部依赖）** |
| CREDITS 与实入库一致 | ✗ 2 条悬空 + 1 条 P0 红线 | **NO-GO** |
| 随包许可/署名文本 | ✗ 缺失 | **NO-GO（对外分发场景）** |
| 工作区已提交 | ✗ 18 项未提交 | **NO-GO** |

**总判定：当前 NO-GO。** 阻塞项共 9 条（A1/A2/A3、B1/B2/B3、C1/C2/C3、D1/D2、E1）。全部为"执行即可消除"型，无架构级阻塞。

### 5.1 建议执行顺序

```
1) 提交工作区 18 项            （程基岩 / 用户）
2) 重导一次 + headless 冒烟 + GUT 514 复跑   ← 让"包"与"commit"严格对齐
3) CREDITS 修订（按审计文档建议文本，用户拍板后）
4) bart 城堡件处置（用户拍板：清理历史 / 移出仓库 / 仓库保持私有）
5) 随包 CREDITS.txt + licenses/ 打包
6) 非开发机实测（B1/B2/B3 一次跑完，全程录屏 = 试玩视频 #7）
7) 外部测试者试玩（quality-lead 交付包）→ 三问反馈回收
8) 打 tag m7 + v0.1.0-slice
9) Release 附件 + 仓库 README 补齐
```

**两个省时合并点**（对每周 5–10h 的业余开发者，建议直接采纳）：

- **第 6 步与第 7 步合并**：非开发机实测与试玩者试玩本质是同一件事——找一台非开发机、一个人、一次跑完并录屏。分开做要准备两轮环境、写两份说明。合并后一次拿到 B1/B2/B3 的实测结论 + 三问反馈 + 视频 #7。
- **第 1 步到第 2 步之间的等待为零**：既然现有包已含全部改动（见 A1 的实查结论），第 6 步**不必等第 2 步的重导完成**——可以边提交边送测，只要最终分发的包是重导后的那一版即可。

---

## 6. 回滚预案（切片版，刻意从简）

不做商业发行的多级回滚。切片只有一个"上一个已知可玩状态"，回滚就是回到它：

| 场景 | 回滚动作 | 代价 |
|---|---|---|
| 重导后冒烟失败 | `git stash` 未提交改动 → 用 `export_presets.cfg` 在 tag `m6` 处重导 | 约 10 分钟（一次导出约 1–2 分钟 + 冒烟） |
| tag 后发现 P0 | 立即删除 `export/win/` 产物与对外 zip 链接；`git` 层面回退到 `m6` 代码态重导 | 约 15 分钟 |
| 已发 Release 后发现合规问题 | 先把 Release 设为草稿/删除附件，再修 | 依平台，通常即时 |

**不需要**热更、差分补丁、版本兼容层——切片没有在线服务，也没有必要维持旧档兼容（存档 schema 变更在切片阶段可直接弃档）。

**唯一需要保留的审计**：每次对外分发的 zip 名、产出 commit hash、GUT 结果。建议在执行第 8/9 步时把这三行记进 `evidence/`。

---

## 7. 待用户拍板项

1. **版本号是否采纳 `v0.1.0-slice`**（或改用 `m7` / `v1.0-slice`）
2. **bart 城堡件处置方式**：① 从仓库移出（推荐，需清理 git 历史）② 仓库保持私有、仅发 Release 附件 ③ 维持现状不公开发布
3. **`tools/dev/` 是否随 M7 入库**（影响"clone 后可复现"这一说法能否成立）
4. **是否把非开发机实测与试玩者试玩合并为一次**（推荐，省一轮环境准备）
5. **CREDITS 修订文本是否照审计文档第 6 节采纳**（我只写建议，不改 `assets/CREDITS.md`）
