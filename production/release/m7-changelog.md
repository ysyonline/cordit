# 《轨迹残响》更新说明 · v0.1.0-slice（里程碑 M7）

> 面向：试玩者 / 玩家｜编制：路远行（release-ops-lead）｜2026-09-05
> 变更来源：`git log m6..HEAD`（m6 = `e5ad6a0`，2026-09-05 05:44 → 2026-09-08 `c05d164`，**共 17 个提交**）＋ 2026-09-12 `539ba19`（导出预设署名落定），全部实查，无推测。
> 2026-09-12 补注：原文"3 个提交 + 工作区未提交改动（18 项）"是 2026-09-05 的时点值；那 18 项已由 9-08 的 8 批提交全部入库，故此处改按**已提交的变更**计数。
> 版本说明见 `m7-release-checklist.md` 第 1 节。

---

## 一句话

**这一版把"能打通"变成了"打起来有感觉"：五场战斗里有三场重新调过数值（遗迹一层群战、遗迹二层守卫、遗迹三层 Boss），最后两场终于能把它们的拿手招式放完；遗迹入口、Boss 战前、通关结尾三段剧情从占位文字换成了实写，结尾留了一个钩子。另外修掉了两个会卡住流程的 bug。**

---

## 玩起来有什么不同

### 战斗：以前是"走过去"，现在是"要打一打"

这一版动了五场战斗里的三场——**遗迹一层群战、遗迹二层守卫、遗迹三层 Boss**。改的不是伤害公式本身，动的是敌人的**血量、攻防数值和出场编组**——所以操作手感你不会陌生，变的是**战斗的长度和你必须做的决定**。

**遗迹一层的第一场群战（火蜥 + 冰晶 + 飞蛾）**变化最明显。以前两个回合就结束，全队几乎没掉血，你甚至不会注意到这是一场"消耗战"。现在它要打五个回合左右，主角的血量会被真正压下来，你得开始考虑"这回合是抢输出还是先奶一口"。

更重要的是属性陷阱现在真的有牙齿了：**火蜥抗火、冰晶抗冰**。如果你一路火球推过来，会发现打到火蜥身上伤害被砍半——正解是给莉娜换冰锥。这场战斗的设计意图就是逼你在冰和火之间来回切。

**遗迹二层的守卫（精英）**以前有个尴尬问题：它还没来得及放出群击和毒击就被打死了，等于它的两套教学招式白设计。血量上调后，你会稳定看到它至少各放一次——群击逼你开群愈，毒击逼你净化或者掏道具。

**三层的 Boss（遗迹核心）**是这版改动最大的一场。它有一招"蓄力 → 下回合重击"，设计上是要你看懂预告条、然后按下防御硬吃这一下。以前它四回合就倒下，蓄力刚起手人就没了，这招等于不存在。现在能打六个回合，蓄力会触发两次——**你真的有时间看到"它在蓄力"，也真的需要决定要不要防**。

**时长口径（设计目标，非实测结论）**：普通战目标 **2–4 分钟**、Boss 战目标 **5–8 分钟**；其中**教学场（第一场道路飞蛾、第二场雷壳甲虫）按设计意图低于普通战下限**——它们只负责教会攻击/道具/打弱点，不该拖长。本次调校是朝上述目标改的，**实测时长尚未回收**（外部试玩数据回来后以实测为准）。

**第一场（道路飞蛾）和第二场（雷壳甲虫）没有改。** 这两场是教学场，本来就该简单：一场教你攻击和用道具，一场教你打弱点。模拟器验证下来数值合理，就不动了。

### 剧情：三段占位文字换成了成稿

三处地方以前能看到 `【P2 遗迹入口·占位】`、`【P3 战前·占位】`、`【P3 钩子·占位】` 这样的方括号标记——那是我们留给自己的待办。这一版全部替换成正式文本：

- **遗迹入口**：进遗迹那段氛围实写了，莉娜会告诉你"魔力不是变浓了，是在往一个方向流"——这是给你的探索引导。
- **Boss 战前**：石棺开启、暗红核心登场的那段前置演出。
- **通关结尾**：核心熄灭之后发生了什么，以及最后那句钩子。这里不剧透，只说一句：它留的是一个**未解之谜，不是没写完的剧情**。

另外这三段现在**主角队有头像了**。之前的版本里这些对话没有指定头像窗，画面左侧是空的。

### 画面与角色：小镇有了光，人也换了新形象

这一版在"看得见的地方"也动了刀。

- **小镇有光了。** 整张小镇地图加了一层**暖色调**，另外布了 **7 处光源**——其中 **3 处会缓缓明灭**（有呼吸感），玩家身上还带一圈**随身光**。以前俯视画面是一片平的亮，现在有了明暗层次，影调更像"有光的镇子"而不是一张贴图。
- **小镇变热闹了。** 装饰密度提了一档：灌木、草簇、红花、黄花交错摆开，并且**避开了所有能互动的点位**（不会挡住宝箱和调查点）；**壁炉与存档点也换成了真贴图**（之前是占位件）。
- **角色换装了。** 玩家现在有**四向 idle/walk 帧动画**（8 FPS——朝哪边走就是哪一套），不再是一个永远正面站着的人；**NPC 也各自换上了对应形象**（12 席，含长者、搬运工、铁匠、牧羊少年等 4 个配色变体）。
- **接委托通了。** 在镇内调查点**接委托**的流程接通了（原先该入口走不通）。

> ⚠️ 需要说明：这一版换的是**玩家与 NPC**，**敌人美术还没有替换**——地图上的敌人与战斗里的敌人都仍是占位色块（详见下方"已知问题"）。

### 流程：两个会卡住你的 bug 修掉了

- **在道路上打完敌人，回不去道路了。** 道路地图上三个敌人记着"打完回哪张图"，但写的地图名是简称，系统的地图存在性校验不认简称，直接拒绝跳转。现在改成完整路径，打赢后能正常回到道路继续走。
- **走正常路线进道路 / 遗迹二层时，队员聊天对话不显示。** 只有从某条特定路线进图才正常。根因是对话播放器没有正确交给事件执行器，对话动作被整段跳过。现在从正常入口进图，队员聊天会正常弹出来。

---

## 这一版的完整变更

### 战斗数值

| 位置 | 改动 | 为什么 |
|---|---|---|
| 遗迹一层 · 火蜥 | HP 55 → 100，ATK 12 → 13，DEF 4 → 8 | 原配置两回合结束、全队近满血，无消耗感 |
| 遗迹一层 · 冰晶 | HP 50 → 95，ATK 11 → 13，DEF 7 → 9 | 同上 |
| 遗迹一层 · 编组 | 飞蛾 2 只 → 3 只 | 敌方行动轮次 +1，把血线压力逼出来 |
| 遗迹二层 · 遗像守卫 | HP 240 → 360 | 原来 3 回合击杀，群击/毒击来不及放；现约 5 回合，两套招式各至少触发一次 |
| 遗迹三层 · Boss 遗迹核心 | HP 480 → 600 | 原来 4 回合击杀，蓄力→重击循环来不及展开；现约 6 回合，蓄力触发 2 次 |
| 道路飞蛾 / 雷壳甲虫 | **不改** | 教学场，模拟器验证合理 |
| **B4/B5**：角色成长 / 敌人 ATK 与 DEF / 技能倍率 | **零改动** | 只动 HP 一项，避免"动一处、全场重调"（**B3 另含 ATK/DEF 微调**：火蜥 ATK 12→13、DEF 4→8；冰晶 ATK 11→13、DEF 7→9） |

受击数值**朝设计分档收敛**（剑士 4–10%、辅助 8–14%、术士 10–16%），避免出现"一下秒人"或"挠痒痒"。模拟器抽样反算的受击比例均落在该区间内；**完整的逐场核对表尚未汇总**。

### 剧情与文本

- 三份剧情对话实写（`story_p2_ruin_enter`、`story_p3_boss_front`、`story_p3_finale`），移除全部占位标记
- 三段对话补上头像差分（`rina_normal`），主角队不再空着头像窗
- 全量对话文本 **202 条**，在设计预算上限 350 条以内

### 缺陷修复

- 道路地图敌人战后回图被拒（地图名简称 → 完整路径）
- 道路 / 遗迹二层聊天点走正常入口时对话被跳过（对话播放器未注入事件执行器，已加幂等守卫防重复注入）

### 测试

- GUT 全量 **530/530 PASS**（M6 收口时 513/513，本 slice 累计 +17 条；其中"覆盖正常入口进图路径"的回归用例 +1，见 `b827a63`）
- 战斗数值断言随调校同步更新

### 开发者侧（玩家无感）

- Windows 导出预设入库，任何人 clone 仓库后装 Godot 4.7.2 即可一键重导出同样的包
- 战斗 GDD §7 数据表与调校记录同步
- 数值平衡模拟器与证据日志留档（`evidence/`）

---

## 已知问题（这一版就有，不是 bug 报告对象）

1. **没有声音。** 宝箱开启等处挂了音效路径，但音频文件本身还没做。不会报错、不影响流程，只是安静。
2. **敌人仍是占位色块。** 这一版的**玩家与 NPC 已换成正式像素形象**（玩家有**四向 idle/walk 帧动画**、NPC 各有换装），但**敌人仍是占位方块**——地图上是暗红块，战斗里是按编组色相派生的方块阵。这是垂直切片的既定范围——**先把玩法跑通，敌人美术是下一阶段的事**。
3. **中文使用内置像素字体**（已随包，不依赖系统字体）。理论上各机器显示一致；若你仍看到方框（豆腐块），请回报具体屏号。
4. **单存档槽。** 覆盖式保存，没有多档位。
5. **窗口固定 640×360 整数倍拉伸。** 在高分辨率显示器上是小窗，全屏会有黑边。

---

## 怎么运行

1. 解压后保持 `轨迹残响.exe` 和 `轨迹残响.pck` **在同一个文件夹里、文件名都不要改**（这两个是配套的，改任一都会启动失败）。
2. 双击 `轨迹残响.exe`。首次启动会在系统用户目录创建存档文件夹。

**操作**：`方向键（↑↓←→）` 或 `WASD` 移动 · `Z` / `E` / `空格` / `回车` 交互 · `X` 或 `Esc` 取消 · `C` 打开菜单（另 `M` 为调试面板，正常游玩用不到）

**通关时长**：游戏本体约 **30 分钟**；**含问卷与存档自检的试玩总时长约 45–60 分钟**。

---

## 反馈回收

试玩后最想听到的三件事（这就是我们内部说的"三问测试"，决定这一版算不算通过）：

1. **你在哪一场战斗里第一次主动按下了防御？** 如果没有——哪一场你觉得"其实该防但我没防"？
2. **你有没有主动切换过攻击目标或技能属性？** 遗迹一层那场（火蜥 + 冰晶 + 飞蛾），你是一路火球推过去的，还是中途换了冰系？
3. **打完遗迹二层那个守卫之后，你脑子里冒出的第一句话是什么？** 这句最真实就好，不用替我们客气。

另外欢迎随手记：哪里卡住了、哪段对话看不懂、哪个数值让你觉得"这不讲道理"。
