# M7 关闸汇编 · 主理人整合（Phase 8）

> 编制：游承峰（主理人）｜2026-09-12｜M7-CLOSE-01 / M7-CLOSE-02 汇编
> 成员产出正本：`production/release/m7-package-closeout.md`（路远行）、`production/qa/m7-playtest-kit.md`（严守真）
> 本文件**只做跨成员整合与决策编排**，专业结论以两位成员的正本为准。

---

## 一、关闸进度总览

| 放行条件 | 状态 | 依据 |
|---|---|---|
| R-1 外部试玩者 B4 正向反馈 | ❌ 未回收 | 缺"人" |
| R-2 非开发机实测（启动/通关/存档） | ❌ 未做 | 缺"机器" |
| R-3 视频 #7 归档 | ❌ 未录 | 缺"录屏" |
| **包侧收尾（非门控项，但发放前置）** | ✅ **完成** | M7-CLOSE-01 |
| **试玩包定稿（发放工具）** | ✅ **完成** | M7-CLOSE-02 |

**判定：门仍为 🟡 CONCERNS。三个条件全部只依赖"外部的人 + 另一台机器"，代码与包均已就绪。**

---

## 二、事实更正（本轮最重要的产出）

### ⚠️ 更正 1：旧包**不是** WASD-only（推翻主理人 05:23 的诊断）

- `project.godot` 工作区 mtime = **2026-09-05 19:52:47**；旧包产出 **19:56:58**。
- 二进制实证：旧包 `project.binary` 内 LEFT/UP/RIGHT/DOWN/ENTER/ESC 六个 keycode **全部命中**。
- `7f8627a`（9/8 21:40）是把 9/5 已生效的工作区改动**提交入库**，不是 9/8 才加键位。
- **⇒ 试玩者拿旧包不会踩"方向键没反应"的坑。**
- **主理人失误根因**：用 *commit 时间* 推断包内容，而包是按 *导出时刻的工作区状态* 打的。两者不同步。

### 更正 2：重导仍必要，但理由要写对

旧包**功能不坏**，真正落后的是 **9/5 20:00 之后的美术**。穷举 mtime 晚于旧包的已跟踪文件，影响表现的共 11 个：

| 类别 | 文件 | 日期 |
|---|---|---|
| R7 角色换装 | `_bright_alpha*` ×5、`char_anim.gd`、`player.gd/.tscn`、`npc.gd/.tscn` | 9/6 |
| A1 光照 | `map_lighting.gd`、`town_map.gd` | 9/7 |
| A2 装饰 | `town_map_tileset.tres`、`town.tscn` | 9/8 02:44 |
| 署名 | `CREDITS.md` | 9/6 |

**已在旧包内（无需重导即有）**：B4/B5 数值、头像修复 + 71 处对话 JSON、E7-S2 剧情、键位、中文字体。
**新包补齐的实证**：启动日志新增 `行走图 M1` / `[MapLighting] Map_Town 光照装配完成：Tint=有 + 静态光 7 处 + 随身光=有`；`char_anim`、`_alpha_v4_shepherd`、`alpha_v1_elder` 在旧包命中 0 次 → 新包 4/6/6 次。
**⇒ 送测必须用新包**——理由从"功能修复"改为"美术落后一档，会污染第一印象类反馈"。

### 更正 3：清单两处技术结论已过期（已更正）

| 原结论 | 实际 |
|---|---|
| §2.2「pck 内无 `licenses/`」 | **错**。`include_filter` 9/5 19:54 写入、19:56 生效，旧包内实测 **12 条** |
| §4.2 B3「未配置自定义字体」 | **过期**。现已有 `gui/theme/custom_font` + `.fontdata` 随包，**"豆腐块"的项目侧根因已消除** |

---

## 三、包侧收尾结果（M7-CLOSE-01）

| 项 | 值 |
|---|---|
| exe | **109,132,800 B**｜2026-09-12 06:05:30｜SHA256 `01F7DCF0…CAA70AF8`（署名重导后**体积一字节未变**）|
| pck | **3,404,804 B**｜06:05:30｜SHA256 `BAA778C5…9B2CB94F`（CREDITS 修订链后 +1,328 B）|
| 导出退出码 | **EXIT=0**，零 error/warning |
| 冒烟 | **EXIT=0 ×2**：① 默认 APPDATA；② 干净 APPDATA 首启 → `P0 开局锚点装配完成` → `story_p0_intro（51 条目）` → `save_point：已发存档请求` |
| 读档旁证 | 默认 APPDATA 那次 `[TownMap] 已有存档（has_save=true）` → 打包环境**读档生效** |
| 版本号 | `0.1.0` **已实测进 exe 属性**（OS VersionInfo + PE VS_VERSIONINFO 双证）；署名真值 `轨迹残响工作室`，旧占位 0 命中 |
| 许可 | **验到**：pck 内 `assets/CREDITS.md` + **12 条** `assets/licenses/**`；zip 根另置**松动件** `CREDITS.txt` + `licenses/`（受包者不解包即可读）；反向排除 tests/evidence/tools/docs/production/addons/export **0 命中** |
| 分发包 | `export/轨迹残响-v0.1.0-slice-win64.zip` — **42,020,129 B / 40.07 MiB**，14 条目，CRC OK，SHA256 `25BF07E9…E8CD94` |
| 指纹沿革 | zip：`C1C2187B…`(42,019,046 / 01B) → **`25BF07E9…`(42,020,129 / 01D 终值)**；pck：`0B206AE1…` → **`BAA778C5…`**；exe 全程 `01F7DCF0…` 未变 |
| 回滚保险 | 旧包备份 `export/_prev-20260905-win/`（**仅回滚，禁止发放**） |

**许可缺口（不粉饰）**：
- **不存在 `CREDITS.txt`** —— 实际落点是 `assets/CREDITS.md`，与旧清单口径不一致。
- 许可与致谢**只在 .pck 内部**，分发包旁无直接可读文本（需解包工具）。→ D2 拆 **D2-a 已验到（包内）/ D2-b 未达成（松动件）**。

---

## 四、试玩包定稿结果（M7-CLOSE-02）

- §7.3「方向键没反应」→ **已修复**（标注 `7f8627a` 与键位）；§7.4 整节改写为"已修复记录"。
- **§3 操作表逐条核对**与新 input map 一致。
- §7.1 扩为**可勾选发放前检查单**（A 包本体 / B 试玩者内容 / C 回收方自检）。
- 新增 §7.6 分发物清单与红线、§7.7 时长处置预案。
- 回收模板就位：`production/playtests/m7-external-playtest-01.md`（逐题**原话留档、禁止转述** + §6 判据逐条判定 + 三个禁止合规核查位）。
- §6 判据阈值**一字未动**（`git diff -U0` 核验）。旧数字 514/514 → **530/530** 一并更正。

> ⚠️ 注：更正 1 之后，kit §7.1 里"包导出时间晚于 2026-09-08"这条硬检查的**措辞理由**需微调（原写"否则是 WASD-only"已不成立），保留为"确认包含 9/5 后美术"的检查。

---

## 五、待决项（供用户拍板）

> **⚡ 状态更新（2026-09-12 用户已全部裁定并执行完毕）**
> - **U-1** 提交闭环 → `539ba19`（仅 `export_presets.cfg`，未 push / 未 tag）
> - **U-2 / U-3** 署名真值 = **轨迹残响工作室** → 已进 exe 属性（双证一致，旧占位 0 命中）
> - **U-4** 松动许可件 → `CREDITS.txt` + `licenses/`（12 条）已置于 zip 根
> - **U-5** `CREDITS.md` → 已按 credits-audit 修订（E 区建立、2 条悬空登记移入）→ 重导包 → 重打 zip
> - **U-6** 图标 → **门内保持现状**，换正式图标移交 **M7 后 polish 首项**
> - **U-7** tag `m7` → **待门控转 PASS 后执行**
>
> 下表保留原始提议，供追溯。

### 5.1 包侧（路远行列 U-1…U-7）

| # | 事项 | 影响 | 主理人建议 |
|---|---|---|---|
| **U-2** | `company_name` 真实署名主体 | exe 属性署名 | **待你给定**（涉及真实署名，禁编造） |
| **U-3** | `copyright` 真实著作权人/年份 | exe 属性版权 | **待你给定** |
| **U-1** | `export_presets.cfg` 改动**未提交** | 产物可回溯 | **建议提交**（否则 clone 重导丢版本号与署名） |
| **U-4** | zip 内是否补松动 `CREDITS.txt` + `licenses/` | 合规可达性 | 建议**做**（1 分钟；受包者不必解包即可读许可） |
| **U-5** | `assets/CREDITS.md` 修订文本是否照 credits-audit 采纳 | 合规内容 | 待你定（B 区 2 条悬空登记处理方式） |
| **U-6** | 是否换正式图标 | 美术资产 | 建议**门内保持现状**（选项 A），M7 后排 polish 首项 |
| **U-7** | 是否打 tag `m7` + `v0.1.0-slice` | 版本发布 | **门控转 PASS 后再打** |

### 5.2 时长口径（严守真列 B1/B2 预案）

模拟器回合数 B1=2 / B2=2，GDD §8-11 口径普通战 **2–4 分钟** → 教学场有低于下限的可能。
**建议 A（判为可接受）**：教学场低于下限属设计意图，收口档注明；**须同步改写 `m7-changelog.md:29`** 的"时长落在设计目标区间内"措辞。

### 5.3 仓库路径（本轮新暴露，风险等级高）

**D1 判为"部分解除"**：`ad2efa2` 只把 castle 件从 HEAD 树移出，**但 git 历史 blob 仍可达**（`git log --all -- '*castle*'` 可列出，8af601f 加入 / ad2efa2 删除）。
**⇒ 历史未清 + 公开 clone = 仍构成 GPL/CC-BY-SA 再分发 → 公开仓库路径仍 NO-GO。不要对外说 D1 已解决。**
选项：① 保持私有（作品集只发 zip）② 清 git 历史 ③ 暂不定。

---

## 六、关闸路径（还差什么）

```
[包侧]  ✅ 完成（新包 + zip + 版本号 + 许可验证）
[工具]  ✅ 完成（kit 定稿 + 回收模板）
   │
   ├─ 待用户拍板：U-1…U-7 + B1/B2 + 仓库路径
   │
   ├─ 用户侧：锁定外部试玩者 ──┐
   ├─ 用户侧：录视频 #7 ───────┤
   └─ 用户侧：游戏内目检 ──────┘
                              │
                       一次外部试玩
                              │
                    回收 R-1 + R-2 + R-3
                              │
              最终 QA 门复判（❌→✅）→ 转 PASS
                              │
                    tag m7 + v0.1.0-slice + push
                              │
                       切片宣告完成
```

**当前唯一真正的外部依赖仍是"人和机器"。包与工具已全部就绪。**

---

## 七、未做清单（勿误记）

- ~~C3（CREDITS 一致性）本轮**未复核**~~ → **2026-09-12 08:50 复核 PASS**：
  ① 工作区 `assets/CREDITS.md` SHA256 `760E0665…07BDCE`，与 verify.log `[9]` 节记录**一致**（源件未再动）；
  ② zip 松动件 `CREDITS.txt`（12,596 B）实读确认含修订后 E 区（"规划中"3 处命中）；
  ③ zip 内 pck 与 export/win pck 逐字节一致（verify.log 实证，SHA256 `BAA778C5…` 双录）。
  ⇒ 三方（源件 / pck 内 / zip 松动件）同为修订后文本，**无"包内旧、松动件新"不一致**。C3 = ✅ 闭合。
- 非开发机实测 B1/B2/B3 **未做**；外部三问反馈 **未收**；tag `m7` **未打**（仍止于 m6）。
- 图标 `RT_GROUP_ICON` 未目视确认（已如实标注为"未验证"）。
- 追加包级 GUT：**✅ 已补**——干净 APPDATA 全量 **530/530 PASS**（`evidence/m7-closeout-gut-clean.log`，82.97s；exit 尾部 RID 警告为 headless 常见伪影，与测试判据无关）。
