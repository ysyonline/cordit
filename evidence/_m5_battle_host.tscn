[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://evidence/_m5_battle_host.gd" id="1_m5host"]

; ── M5 收口 VICTORY 演示舞台（临时场景，仅供录制，录后可删）────────
; 组装真实 BattleCommand + BattleUI，确定性打一场 Boss 教学战；
; 结束发 battle_finished(VICTORY) → BattleEventBridge 消费 → 全局
; executor.resolve_victory() → 回 f3 续行战后续行段（I5 兑现）。
; 载荷经 SceneRouter.get_staged_payload() 取（enemy_group_id）。

[node name="M5BattleHost" type="Node2D"]
script = ExtResource("1_m5host")
