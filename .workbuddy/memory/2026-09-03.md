# 2026-09-03 工作日志

## M5 收口 · demo 链路全绿（dryrun10 PASS），余录制/验收档/tag 三件
本会话（09-02 下午接续至 09-03 上午）完成了 M5 收口第 1 件（demo 全绿），全部修复与验证已落地。**新会话从「录制视频」开始接手。**

### dryrun9 FAIL 根因与修复（已实施+验证 ✅）
- **根因**：`SceneRouter.change_scene` 第 172 行（受理段）对每次切换**无条件** `_staged_payload = payload.duplicate(true)`。demo 第 7 幕切战斗替身传空载荷（`change_scene(BATTLE_HOST_PATH, {}, false)`），把桥（battle_event_bridge）转发受理暂存的完整 Boss 四字段载荷**清成空字典** → VICTORY 后 BattleResultHandler "无回图目标"（result/staged 均缺 return_map）→ 停留替身场景 → 无 map_ready → save_point 意图永不消费 → 存档停在 f3 入口 autosave（phase=2）。旧注释"暂存位保持原样"的假设不成立。
- **修法（evidence/_m5_auto_demo.gd，4 处）**：
  1. 第 7 幕改**捕获重传**：`var staged := SceneRouter.get_staged_payload()` + `change_scene(BATTLE_HOST_PATH, staged, true)`——完整四字段过 A5 校验并原样重暂存，替身与真实战斗场景读到的载荷逐字段一致；
  2. 第 8 幕 finale 收束改**补按循环** `_advance_until_idle()`（新工具函数，0.6s 一按直到 runner 回 IDLE，上限 6 次）——长条目逐字未完时一按只"补完"不"翻页"，原两次固定节拍按会差按（dryrun9 收束超时的真因）；
  3. 第 9 幕验证打印文案改"预期入图落位 (320,40)"——**存档坐标=入图落位是生产行为非缺陷**（回图 map_ready 时 announce_ready 先落盘，此时玩家还没被 deferred 回置到棺前位）；
  4. 头注同步更新。
- **_m5_battle_host.gd 头注**同步改（"不带 payload"→"捕获重传"）。
- **原计划的"第 8 幕 flush 存档意图"确认不需要**：修好回图后，resolve_victory（战后 2 帧）早于 0.4s 转场完成，phase=3 先于 f3 装载落盘，announce_ready 自然消费意图。二次 flush 属无害空转，别加。
- **二次 announce_ready 副作用已核实安全**：生产侧 map_ready 唯一监听者 BattleResultHandler 在 `_pending_return` 空时早退。

### dryrun10 全绿证据（evidence/m5-demo-headless-dryrun10.log）
- EXIT=0；全链路：战前拍 → 桥转发受理（暂存 ruins_f3 @ (320,576)）→ 捕获重传切替身 → R1-R5 确定性教学战 → VICTORY → **受理切换 ruins_f3 ✅**（dryrun9 卡死环节已通）→ 战后续行 phase 2→3 → save_point → 回置棺前位+免疫 0.5s → finale 1 次补按收束 → **终态验证 PASS：存档 ruins_f3 / phase=3 / (320,40)**。
- 全日志仅 1 条无害警告（story_p3_boss_front start 条目 66 字>60 渲染折行提示，既有文案）。

### 全量 GUT 复跑 ✅（evidence/m5-final-gut-full.log）
- **415/415 全绿** "All tests passed!"（Sprint 5 后基线 415，非旧 410/286）。本会话 3 处生产变更（ruins_f3_map.gd executor 注入恒假修复 / scene_router.gd 哨兵直通补丁 / battle_event_bridge.gd 头注）确认不破测试。

### 本会话生产代码变更（2 处逻辑+1 头注，均已在 dryrun10+GUT 验证）
1. `scripts/maps/ruins_f3_map.gd`：`_assemble_interaction` 中 runner 注入改直取 `SceneRouter.global_event_executor`（成员 event_executor 在其后 `_assemble_boss_anchor` 才赋值，`if event_executor != null` 恒假——战前/战后台词从不演的生产级缺口；E5-QA 备忘"恒真 if"实为恒假）；
2. `autoload/scene_router.gd`：`_on_enemy_touched` 对带 `_from_event_battle` 哨兵的载荷直通跳过——旧序 Router 先连接先受理哨兵载荷占住 `_switching` 闸 0.4s，桥的完整载荷转发反被防重入闸拒绝；
3. `scripts/events/battle_event_bridge.gd`：头注改"本桥是事件战斗唯一受理转发方"。

## 下一步（新会话从这里接手）
1. **录视频**：写 `tools/record_m5_gameplay.bat`（镜像 record_m4_gameplay.bat：GODOT_EXE=D:\software\Godot\Godot_v4.7.2-stable_win64.exe，`--write-movie ... --fixed-fps 30 --quit-after N res://evidence/_m5_auto_demo.tscn`）。**帧数计算**：demo 实际时长以 dryrun10 时间轴推算（M4 是 1920 帧/64s；M5 全 9 幕含 5 轮战斗拍+3 次对话+4 次跨图转场，估 ~3300 帧/110s，可 grep dryrun10 各幕时间戳精算）；产物 `evidence/m5-gameplay.avi`。**.gitattributes 已配 `*.avi filter=lfs`**——注意：旧 4 条 m1-m4 avi 在 git status 显示 M（Binary 变 133 bytes）是 LFS filter 生效的显示效应，录制后 git add m5-gameplay.avi 即走 LFS；若 git lfs 未安装需先装。
2. **写验收档** `evidence/m5-acceptance.md`（镜像 m4-acceptance.md：A8 行 5 判据+对话 GDD §8+demo 全绿证据+dryrun10 关键日志摘录）。
3. **commit + tag m5**：⚠️ 无指令不 commit，须用户确认；建议先清 probe*.txt（evidence/_probe_exit.txt）与过期 dryrun 日志的入库范围（只挑关键证据 add）。
4. 收尾可删演示临时件（_m5_auto_demo/_m5_battle_host 两套三件，与 M2/M3/M4 纪律一致——M4 实际未删，沿例保留亦可）。

## 环境坑（本会话新踩/复确认）
- **Edit 工具对未读文件拒绝写入**（"File has not been read yet"）——恢复会话后要先 Read 再 Edit，即使上会话已编辑过。
- **后台任务跨会话/网络断连后 TaskOutput 报 not found**——长命令输出重定向到文件（`> log 2>&1; echo EXIT=$?`）比依赖 task-notification 可靠。
- dryrun 启动命令正本：`MSYS2_ARG_CONV_EXCL="*" "D:/software/Godot/Godot_v4.7.2-stable_win64_console.exe" --headless --path "D:\\code\\cordit" res://evidence/_m5_auto_demo.tscn > evidence/m5-demo-headless-dryrunN.log 2>&1`（场景直启，非 -s gut_cmdln；EXIT=0）。
- GUT 全量日志尾部 RID 泄漏 ERROR 属引擎退出噪音（TextServer/CanvasItem），**不是测试失败**——判据只看 "All tests passed!" 与 Totals 表。
- 仓库工作区当前有大量 `.import` 文件 M 标记（LF/CRLF 警告噪音）+ 旧 avi 133 bytes 显示效应，均非真实内容变更，commit 时注意甄别（`.import` 可整体 add 无害）。

## M5 收口②录视频（11:15-11:25）
- 写 `tools/record_m5_gameplay.bat`（镜像 M4，安全阀 1920→5400 帧：M5 demo 8 幕远超 M4 64s）。
- **坑**：WorkBuddy Git Bash `cmd //c "...bat"` 引号传递失败（bat 被吞，930ms 空退）；改直调 Godot exe，参数与 bat 一致，成功。
- 产物 `evidence/m5-gameplay.avi`：4849 帧 @30fps = 161.6s，306MB，demo 终态 PASS（I5 全序列），无 ERROR。LFS filter 确认生效。
- 片长超估算原因：Movie Maker 固定帧步进下对话逐字/补按节拍比 headless 实时慢，正常。
- 留证 `evidence/m5-recording-log.md`。**按用户指令停在验收前**：③验收档 ④commit+tag 均未动，等用户发话。

## M5 收口③验收档（11:27-11:45）
- 写 `evidence/m5-acceptance.md`（镜像 M4 格式）：四件套进度表 + ①②③详情 + Sprint5 五 Story 基线演进表（286→313→367→384→393→410→415）+ QA 独立复核 PASS + 冒烟五通道满额表 + commit 预案。
- 关键口径：qa-e5=410（QA 时点）与 m5-final=415（收口修复后）差异已在档内注明，避免误读。
- commit 预案：剔除 58 个 .import 行尾噪声；m1-m4 avi 的 M 状态是 LFS filter 效应非内容变更，拟一并剔除（待 commit 时验证）。
- ④commit+tag m5 仍待用户发话。用户另要求估算 M6/M7 时间——已答复。

## M5 收口④commit+tag（11:53-12:15）——本地完成，push 被阻
- 预检：58 个 .import 全量哈希校验=0 内容差异（纯 stat 噪声，未入库）；m1-m4 avi 的 M 状态=LFS filter 生效比对差异，已随本 commit LFS 指针化（根治幽灵 M）；_probe_exit.txt 为无引用探针残留（内容"1"），未入库（文件消失，可能在早前操作中已删）。
- commit `b550126`：145 files, +22488/-178（E5 测试5件套+fixtures、事件/对话脚本、69 JSON、60+ 证据、验收档、录制 bat、视频#5 LFS 306MB、Sprint5 记忆 3 件）。
- tag `m5`（轻量，沿 m4 惯例）已打。
- **push 失败未决**：代理 127.0.0.1:7890/7892 均拒连（netstat 无 LISTEN=代理客户端未跑）；SSH 22 直连也被 abort（kex_exchange_identification）；直连 github.com 000。间隔重试 20s/60s/120s/180s 全失败。**commit+tag 安全在本地，代理恢复后 `git push origin main m5` 一条即可**。

## M5 push 完成（12:42）
- 代理恢复后推送成功：内置 Git HTTPS 无 tty 凭据提示失败 → 沿 2026-08-31 惯例改用 `/d/software/Git/cmd/git.exe`（v2.41.0）执行。
- LFS 上传 5/5 对象 474MB（含 m5 视频 306MB + m1-m4 重整化的 4 个 avi）；`229017c..b550126 main->main` + `new tag m5`。
- 远程核验：ls-remote main 与 refs/tags/m5 均指向 b550126。**M5 至此全链路关门**。
- 工作区仅剩当日 memory 日志的未提交增量（属常态，随下次收口一并入库）。

## Sprint 6 计划与任务拆分（13:26-15:18，本会话）
- **用户要求列出 M6 任务计划** → 读 `production/epics/EPIC-6.md` 正本输出五 Story 清单（S1 菜单 6h / S2 胜利结算 3h / S3 逃败结算 1.5h / S4 聊天+手动存档 1.5h / S5 剧情实写 5.5h），给出排期：S2→S3→S1→S4（S4 依赖 S1 存档入口），S5 纯 JSON 穿插。
- **用户问"AI 开发也要 6h？"** → 已澄清：6h 是人时复杂度标尺（S1≈2 个 AI 会话、S2/S5≈1 会话、S3/S4 半小时级），真正耗时在人眼手感（布局来回改）、415 条回归、验收遍历，非纯编码。用户认可双口径解读，未要求改 EPIC 文档。
- **用户批准按建议排期开发并要求细化拆分** → 建任务工具 7 项（编号映射：T7=S2、T2=S3、T3=S1、T4=S4、T6=S5、T5=M6 收口、T1=M5 收口），依赖链 T7←T1、T2←T7、T4←T3、T5←{T2,T3,T4,T6}。各 Story 拆 3-5 步（详见 MEMORY.md 快照②）。
- **用户纠正：M5 收口 commit+tag 早已做完** → `git log` 核实 b550126 (tag: m5, origin/main)，T1 关闭 completed；MEMORY.md 快照②已同步（"从②开始"作废）。教训：会话启动先 `git log --decorate` 核实进度，不轻信记忆快照的"下一步"。
- **实现前探索（未写码）**：已读 `battle_result_handler.gd`（战后写回：party_state 覆写/cleared_enemy_set/save_requested_pending 置位/DEFEAT 读档回置）、`battle_scene.gd`（占位场景：E2-S3 时代产物，`_build_result` 已带 exp_gained 空数组字段）。**关键发现：battle_scene 还是占位方块阵，真实战斗结算数据链（exp_gained/drops）尚未产出**——E6-S2 的入口在此；胜利自动存档链路已在 E2-S4 落地（save_requested_pending），S3-T3.3"补线"范围比 EPIC 预估小，实际只需验证+可能补画面时序。
- **下一步**：T7（E6-S2）开工，从结算画面骨架+结算数据协议（exp_gained 数组结构/drops 表）定义开始。

## T7（E6-S2）T2.1 结算器与结算画面协议（13:26-15:30 全绿收步）

- **产出**：① `battle_command.gd` 新增 `build_settlement()` 纯函数结算器（exp_events 逐条事件流 / drops 按只累计 / 升级判定 / 技能习得逐级逐角色），`_build_result()` VICTORY 时嵌入协议（DEFEAT/ESCAPE 空协议向后兼容）；② `battle_ui.gd` `show_result()` 扩展四类行渲染（exp/level_up/skill/drops），全部可缺省键零回归；③ 新建 `test_e6s2.gd` 14 用例（A~G 七组）+ .uid。
- **切片口径（勿翻案）**：结算器总经验**从零计**（非跨战累计池）——GameData 无经验字段、存档 v2 无；跨战累计池 + 存档 v3 扩展待产品裁定，注释已写明。
- **修 4 条失败**：
  - 3 条断言数字算错：B1=15 EXP 恰达 Lv2 阈值（**含等号**）→ 全队升 2 级 → 三人各出习得行（凯尔 wide_sweep / 莉娜 ice_shard+thunder_burst / 莫娜 group_heal）→ 事件流 **5 条**（exp1+lv1+skill3），非 4 条。
  - 1 条 party 越界（**跨套件泄漏，正本已修**）：`test_e5s3.gd` test_a5 手写 `"party":[]` v1 旧档 load → `_deserialize_party([])` 把 `GameData.party` 整体换空数组；e5s3 的 after_each 不还原 party（Sprint 5 潜伏缺陷，当时无人索引 party 长度）→ 传到 e6s2 首个索引 `party[1]` 的用例爆雷。**修法**：e5s3 补 party 快照/重建式还原（治本）+ e6s2 `_ensure_party_3()` 兜底防御。
- **GUT 日志阅读技巧**：Run Summary 末尾会复述失败套件标题（形如第二次出现"res://tests/gut/test_e6s2.gd"），**不是重跑**——排查套件顺序时别被骗。
- **验证**：全量 GUT **429/429 全绿**（7719 asserts），证据 `evidence/e6s2-gut-t21-fix.log`。无越界报错。
- **状态**：T2.1 完成，**已停下汇报等用户确认**，确认后才进 T2.2（EXP 逐条事件流弹出节奏）。

## T7（E6-S2）T2.2 EXP 逐条事件流弹出节奏（16:16-16:40 全绿收步）

- **产出**（纯 `battle_ui.gd`，结算器零改动）：`show_result` 改两段式——表头（结局+party_state）立即出；结算三区行入 `_reveal_queue`，`_process` 按行停顿逐条弹出。dwell 两档：普通行（exp/掉落）0.35s、里程碑行（升级/习得）0.60s。
- **口径**：**逐行停顿**——弹完一行计时器归零，按下一行的 dwell 等待（非从 show_result 起累计）。B1 全程 ≈ 0.35+0.60+0.60×3+0.35 = 3.1s。
- **手动接口**（T2.3 接确认键用）：`step_reveal()` 弹下一条 / `finish_reveal()` 全部立即出；测试查询 `is_revealing()` / `get_reveal_remaining()`。headless 确定性：不入树直接调 `_process(dt)` 驱动。
- **测试**：新增 F2 组 5 用例（表头即出 / 手动步进顺序 / dwell 两档时序 / finish 跳过 / 旧协议不启动计时）；T2.1 的渲染断言用例改为 finish_reveal 后断言全文。GUT **434/434 全绿**，证据 `evidence/e6s2-gut-t22b.log`。
- **踩坑**：首跑 2 条失败均为用例时序口径错（①渲染断言在表头阶段查文本；②我的 dwell 数学按累计算，实现是逐行归零）。实现本身无改。
- **状态**：T2.2 完成，**停下汇报等用户确认**，确认后进 T2.3（多级连升+习得集成验证：demo host 端到端 + 确认键接 finish_reveal）。

## T7（E6-S2）T2.3 确认键跳过 + 端到端集成验证（16:30-17:10 全绿收步）

- **产出**：① `battle_ui.gd` 加 `_unhandled_input` 路由——揭示期间按 `interact` 键 → `finish_reveal()`（跳过剩余行），沿 dialogue_runner 先例（InputEventAction 注入语义等价）；`inject_interact_press()` 测试注入口同款；headless 不入树时 `get_viewport()` 为 null，加空守卫。② 新增 F3 集成组 4 用例：VICTORY 真载荷全文对表 / `skills_up_to` 差集≡结算器习得行（验收"技能菜单出现"的表驱动验证）/ 确认键跳过+非揭示期不拦截 / B5 多级连升端到端（Lv4 一次列出、习得行按等级分行升序）。GUT **438/438 全绿**，证据 `evidence/e6s2-gut-t23d.log`。
- **两处认知修正（防复发）**：①技能中文名——wide_sweep=**横扫**（非重斩，重斩=heavy_slash 是 Lv1 既有技能）；②结算器习得行是**按等级分行**（for lv→for u：凯尔 B5 出【横扫】【掩护】两行非合一），且 party_state 渲染行"名 HP x/x MP x/x"**不含 Lv 字样**（"Lv4"全文计数恰 1=升级行）。
- **状态**：T2.3 完成，**停下汇报等用户确认**。E6-S2 剩 T2.4（掉落入包接线到 handler 真链路 + demo host 升级流实拍验证）。

## T7（E6-S2）T2.4 掉落写回真链路 + demo host 实拍适配（16:41-17:40 全绿收步）

- **产出**：① `battle_result_handler.gd` VICTORY 链路新增 `_apply_drops()`（步骤 1.5，紧随 party_state 覆写）——result.drops 逐条并入 GameData.inventory，**I2 口径与宝箱 give_item 同入口**（inventory[id] += count），空 id/非正数防御跳过；DEFEAT 不写（读档整体回滚，写了白写）、ESCAPE 空数组天然幂等。② GUT 新增 G2 组 3 用例（handler 写回累计 / 脏数据防御 / 三结局写回分支全链路）。③ `_m5_battle_host.gd` 适配：RESULT_HOLD 2.6→4.0s（B5 载荷揭示 5 行 ≈2.6s 弹完 + 缓冲，保证收口视频事件流完整），_on_battle_over 打印揭示状态供录制日志核对。
- **B5 实拍推演**（demo host Lv4 开局打 B5）：after_lv = maxi(4, level_for_exp(260)=5)？——否，260<300 → **Lv4，无升级行**；揭示 = exp1(核心260)+drops1(大药瓶×1) 共 2 行 ≈0.7s 弹完。
- **验证**：GUT **441/441 全绿**（438+3），证据 `evidence/e6s2-gut-t24b.log`。
- **状态**：T2.4 完成 = **E6-S2 四步全收**，停下汇报等用户确认（确认后关 T7、进 T2=E6-S3 逃败结算）。

## T2（E6-S3）逃跑/失败结算完善（17:00-17:07 全绿收步，单次完成）

- **产出**：① `battle_ui.gd` 三结局文案按 GDD §3.5 落位——DEFEAT 改"**残响中断……**"+ 第二行"队伍被召回进入此地时的存档点"（去向提示，替代旧占位"全灭……"）；ESCAPE 改"成功撤退——**敌人仍在原地徘徊**"（保留语义明示，与数据行为对表）；VICTORY 不动。② 新建 `tests/gut/test_e6s3.gd`（+uid）6 用例三组：A 文案渲染（DEFEAT 含"残响中断/存档点"且无"全灭"、ESCAPE 含"敌人仍在原地"且不启动揭示）/ B 敌人保留（**ESCAPE 后 cleared_enemy_set 不登记** + 集合外敌人实例化不自删——"不登记⇔装载保留⇔可绕行可再战"数据链锚定，visible_enemy 语义天然成立只缺测试）/ C 意图位（**VICTORY 置位 save_requested_pending = I3 时机②生产端锚定**（消费端 e4s6 已覆盖）、ESCAPE/DEFEAT 不置位）。GUT **447/447 全绿**（441+6 首跑即绿），证据 `evidence/e6s3-gut-run1.log`。
- **实现前探索结论（重要）**：S3 的两条 EPIC 验收语义**实现层早已成立**（handler 只在 VICTORY 写集合+置位；visible_enemy 数据驱动自查自删），缺的全是测试锚定——文案是唯一真代码变更。e2s4 缺口已在此补齐。
- **踩坑防御**：DEFEAT 用例真调 `load_save()`——若测试机有残留存档会真读档真换场景，**必须覆写 save_path 指空**（e2s4 C 组同款隔离手法，首版漏了后自查补上）；WARNING"无回图目标/兜底回暂存图"是设计路径预期输出，非缺陷。
- **状态**：T2（E6-S3）完成，**停下汇报等用户确认**。下一 Story = T3（E6-S1 主菜单三页，最大块 6h，含用户过目手感环节）。

## E6-S1 · T3.1 菜单壳+状态页 完成（本会话，全量 GUT 460/460 全绿）
- **用户三项裁定（会话前段）**：C 键呼出 / 重建最小装备 schema / 初始背包直塞 2 件。
- **产出**：`scripts/ui/menu_panel.gd`（+uid）菜单 Control；`tests/gut/test_e6s1.gd`（+uid）13 用例；`scenes/player.tscn` Player 入 "player" 组；`scripts/maps/town_map.gd` `_assemble_menu()`（UILayer 常驻+幂等守卫按脚本判定）；证据 `evidence/e6s1-gut-t31.log`。**基线 415→460**（+45 断言数另计）。
- **实现要点**：门闸三条=SceneRouter._switching / current_scene_path==BATTLE_SCENE_PATH / 兄弟 DialogueRunner.is_idle()（runner 缺席视为空闲）；模态吞键=开启态 _consume_event 一律 true（防漏给交互控制器）；玩家锁经 "player" 组动态解析（菜单常驻 UILayer 不持玩家引用）；置灰唯一授权项=读档（has_save()=false），打开时重算；头像映射 lina→rina_normal（portrait_catalog 占位注册名）；九宫格内容子节点必须在 build() 之后加（_rebuild 清空子节点）。
- **本会话踩坑**：①GUT "Nonexistent function 'new' in base GDScript" = 脚本 Parse Error 的连锁面具（真因 `_refresh_disabled` 漏写，parse 失败→.new() 失效），先看日志头部 SCRIPT ERROR；②autofree 不入树则 _ready 不跑——Control 默认 visible=true，菜单 is_open() 恒真假象，UI 测试必须 add_child_autofree；③KEY_W 已绑 move_up，"无关按键"用例要用 KEY_N；④GUT wait_frames 已废弃→wait_physics_frames；⑤重挂宿主用 reparent（二次 add_child 报 already has a parent）。
- **下一步（T3.2 道具页，待用户确认后开工）**：confirm_current match "item" 分支接道具列表窗（8 行滚动、usable_in_map 过滤、使用写回 GameData.party）；T3.3 装备页（EquipmentData schema+2 件 .tres+build_party_unit 并项+存档 v2→v3 迁移）；T4 存读档接线（save/load 分支接 SaveManager）。

## E6-S1 · T3.2 道具页 完成（本会话，全量 GUT 470/470 全绿，基线 460→470）
- **状态机**：menu_panel 加 `enum Mode { MAIN, ITEM_LIST, ITEM_TARGET }`，`_consume_event` 按 `_mode` 三路分派（`_consume_main/_consume_item_list/_consume_item_target`）；**C 键语义改单向**（关闭态开菜单；开启态只吞不动作——防列表深处按 C 弹穿回地图），关闭统一 X/Esc。close() 复位子窗与模式；try_open() 复位 _mode。
- **道具页实现**：子窗 (144,32,232,208) 覆盖状态面板区（_build 尾部创建保层级），8 行滚动列表（`_item_scroll` 跟窗）+ 描述两行 8px autowrap；行文本"名 ×N HP+"。`_open_item_list()` 过滤口径=`usable_in_map()`（验收第 2 条）+ count>0（0 库存隐藏），行序=背包插入序；**detox 地图态裁决：列表显示、可用、扣库存，数值无变化**（CharacterRecord 无中毒字段，真实清毒在战斗侧，不做隐藏）；**死亡不作用药门禁**（战斗侧 _do_item 同口径，复活块置灰即时解除）。写回=HP/MP mini 钳上限、扣库存"减一不 erase"（与宝箱/掉落同字典）。用药后留目标态（连续喂药）。目标高亮=非目标块压暗 Color(0.72,0.68,0.74)、目标块正常（叠加死亡置灰之上）。
- **测试**：F 组 10 用例（过滤行序/阶段字段口径/光标回绕/层级不弹穿/空背包 Z 静默/HP 钳制扣减/MP+detox 语义/跨目标写回/死亡拉起置灰解除/子窗冻结坐标九宫格）；读取口 `get_item_window/get_mode/get_current_item_id/get_item_page_entries/is_item_page_open`。证据 `evidence/e6s1-gut-t32.log`。
- **踩坑**：①首跑 2 挂全是测试笔误——目标态 X→列表后还需再 X 才回主模态（少按一次）；`_item_confirm` 默认目标=0，打莉娜需 `_target_move(1)`（默认落凯尔满蓝被钳制）。生产逻辑首跑即对。②git 大面积 .import "改动"=CRLF 噪音，`git -c core.autocrlf=false diff` 零差异，勿入库勿处理。
- **下一步**：T3.3 装备页（EquipmentData 最小 schema+2 件 .tres 初始背包直塞+build_party_unit 并项+存档 v2→v3）→ T3.4 集成验证 → T4 聊天+存读档 → T5 收口。**停下汇报等用户确认**。
