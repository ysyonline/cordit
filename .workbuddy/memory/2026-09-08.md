# 2026-09-08 · A1/A2 游戏内目检反馈 → 立体感根因诊断（用户拍板前置）

## 用户反馈
- 两次拉起游戏目检（第二次 town→对话→road→ruins_f1 全链路 0 ERROR，2m51s）后明确反馈：**整体仍是"贴图 2D 平面感"，目标是空之轨迹式 2.5D 的地图与人物**。要求分析原因，不写代码。

## 诊断结论（主理人，程基岩知识域交叉确认）
空轨式立体感的三个真实来源：
1. **相机俯角 ~30°（¾ 视角）**：能看到建筑立面（墙是"竖起来的"），不是 90° 正上方看顶面
2. **地图有真实几何高度**：坡道/台阶/桥/高地落差（空轨地图是真 3D 多边形）
3. **角色是大比例 ¾ 视角直立精灵 + 脚下投影**：屏占 40-60px、可见脸，读作"站着的人"

现状逐条对照：相机 90° 正俯视（墙=地面色块）/ 单平面 tilemap 全图 z=0（零高度差）/ 角色 16×18 俯视比例 2 头身（像棋子）。**透视错位是根因**：角色精灵是 ¾ 画法、地图是正俯视画法 → "贴纸贴地上"感。

## 重要修正（记录在案）
此前"Y-sort = 2.5D 纵深 80%"的架构判断**仅对遮挡关系成立**（人被树冠遮挡），对用户期望的视角/几何立体感**不成立**。A1 光照/A2 装饰改善氛围但触碰不到立体感天花板——"给平面画打光加密度"。

## 三路线（已呈用户待拍板）
- **甲 · ¾ 视角 JRPG 重制**（DQ/宝可梦式）：换 ¾ 透视 tileset（墙 1-2 格立面）+ 角色放大/换大号 ¾ 精灵 + 脚下影子。架构（Y-sort/生成器/传送）存活，素材语言大换血。成本：中。观感：经典 JRPG 立体感，到不了空轨的坡道起伏。
- **乙 · 真 3D 地图 + 2D 精灵告板**（空轨原版技术）：Godot 3D 场景建图 + Sprite3D 角色。管线重开、碰撞/相机/Y-sort 全换、全链回归。成本：最高（超此前 B 路线）。观感：最接近空轨。
- **丙 · 现架构继续叠假体积**（A3 高墙堆叠+影子）：成本最低，但按用户体感天花板明显。

## 状态
- 用户尚未拍板甲/乙/丙。五批次（R6/R7/头像/A1/A2）仍未 commit。

## M7 五批次入库收口（21:36 用户拍板 B + 全批执行，主理人直操）
- **拍板**：①design/ 选 B（确认删除入库）②headless --import 补 .uid 进批次 5。
- **commit 链**（ad2efa2..HEAD 共 11 个）：
  - `4f3b717` chore(design) 52 件确认删除入库（历史可恢复）
  - `70fb1cb` feat(M7-R6) 接委托接线 + test_m7r6
  - `c64eaa2` fix(M7-portrait) 头像修复 + 71 处回填 + 剧情统稿
  - `bae5f11` feat(M7-R7) 角色换装套件 + NPC 分配初排
  - `cb0cc73` balance(E7-S1) B4/B5 数值 + 模拟器
  - `ab6677a` feat(M7-A1) 光照层（含新补 map_lighting.gd/test_m7a1.gd .uid）
  - `760da1a` feat(M7-A2) 装饰密度 + 生成器正本收编（含 MovieWriter 帧序列 360+ 件）
  - `7f8627a` build(M7) 导出管线 + 字体 antialiasing=1→0（真改动非 CRLF）
  - `a56cbb3` docs(M7) QA/release 三件套 + 记忆档案
  - `7071a4c` chore(evidence) 补 5 个 .uid/.import 伴生件
  - `a1e3582` chore(evidence) 提交后 GUT 复核日志
- **验证**：干净 APPDATA 全量 GUT **530/530 全绿**（53.9s）；工作区净（仅 .workbuddy/artifacts 不入库）。
- **前置发现**：字体 .import diff = antialiasing 1→0 真改动（像素字体关 AA），非 CRLF 噪音，保留批次 7。
- **未 push**（等用户发话）。M7 剩余：外部试玩者锁定 / 非开发机实测 / R2 形象分配目检定稿。

## 归档 + push 收口（21:47 拍板"先归档，再push"）
- 归档：m7-commit-plan.md → `production/release/m7-commit-plan.md`（commit `df563ce`）。
- **push 遇阻与解法**：代理 127.0.0.1:7892 未运行（10061），SSH config ProxyCommand connect.exe 走代理全挂；**直连 ssh.github.com:443 通**（密钥认证正常），`GIT_SSH_COMMAND="ssh -o ProxyCommand=none" git push origin main` 成功。
- 三方一致：本地 HEAD = 远程 refs/heads/main = `df563ce`（9cc0de5..df563ce 共 13 个 commit 上云）。
- **⚠️ 代理隐坑备忘**：GitHub 访问不再强依赖代理；代理软件没开时用 `GIT_SSH_COMMAND="ssh -o ProxyCommand=none"` 前缀即可。
