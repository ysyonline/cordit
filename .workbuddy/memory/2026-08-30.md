# 2026-08-30 工作日志

## E3-S5 转场与打击反馈（收口）
- 新建 `scripts/battle/battle_background.gd`（截图+暗角占位，set_screenshot 接口）、`battle_hit_feedback.gd`（受击闪白 0.1s + 克制橙字 1.3×弹"弱点！" + 写 GameData.discovered_weakness_set）、`battle_transition.gd`（进战黑屏 0.2s）。
- `battle_ui.gd` 接线：_build() 加三层（背景/打击反馈/转场，转场置顶）；_on_battle_event 处理 damage（闪白+浮动数字）/weakness（弹字+写 GameData）；新增 8 个查询接口供测试。
- `tests/gut/test_e3s5.gd` 5 用例，覆盖 §4.8 背景 / §3.3 弱点三步 / 转场；真实端到端用例（莉娜火球打甲虫）PASS。
- 全量 GUT 179/179 PASS（退出码 0），零回归。证据 `evidence/s3-gut-e3s5.log`。
- 踩坑：TextureRect 用 `STRETCH_KEEP_ASPECT_COVERED`（非 STRETCH_COVER）；GUT 跑测 `MSYS2_ARG_CONV_EXCL="*"` 禁路径转换，--path 必须 Windows 反斜杠路径。
- 纪律：未 commit/tag，待用户批准 M3 收口。

## E3-S6 边缘 8 条实测 + M3 收口
- `tests/gut/test_e3s6.gd` 8 用例覆盖 GDD §3.6 边缘1~8，全 PASS。修复 2 个 GUT 失败：
  - D1（代码 bug）`BattleCommand.set_inventory`：`inv.duplicate(true)` 对空数组返回未类型化 `Array`，赋 `Array[Dictionary]` 成员报类型错；`as Array[Dictionary]` 不生效，改逐元素重建。
  - D2（测试参数位错）`test_逃跑成功` 把 `0.0` 传入 `submit_command` 第 3 位 variance，roll 随机导致约 20% 失败；改为第 4 位显式传 `roll=0.0`。
- 全量 GUT **187/187 PASS（5994 断言，退出码 0，零回归）**。证据 `evidence/s3-gut-e3s6.log`。
- M3 收口：EPIC-3.md 六 Story 验收全 [x] + M3 收口段；overview.md M3→PASS（收口待门3）；`evidence/m3-closeout.md` 写就。门1/2/4 PASS；门5 tag m3 **已打**（commit 72e1354 / tag m3，用户批准）；门3 试玩视频#3 待用户本机录制。Sprint 3 全程改动随 M3 统一提交（19 文件 +5360 行）。
- 勘误：EPIC-3.md "S6 边缘 7 条"→GDD 实为 8 条；`.gitignore` `!evidence/*.log` 已放行，日志无需 `git add -f`。

## 下一步
- 门3：用户本机用 auto-demo + Godot `--write-movie` 录 `evidence/m3-gameplay.avi`（沙箱 RendererDummy 无法渲染）。
- 门5：用户批准 M3 收口 commit + tag `m3`（Sprint 3 全程未提交）。
- EPIC-4（19h）：先修 `tools/` 4 脚本 7 处硬编码路径，再克隆 town 工具做三层遗迹 + E4-S7 真读档。

- 2026-08-30 16:02（主理人）：用户要求"当工作室推进+告知当前阶段"。工程记忆 MEMORY.md 超限（约 2.6x），已收拢重写：合并 M1-M3 过程细节为快照，保留全部裁决/数值裁定/已踩坑，现约 2.3K 字符。overview.md 两处过期状态订正（Sprint 3 收口 tag m3、门3 仅剩试玩视频待补录）。阶段判定=Phase 5 制作循环中，下一步 Sprint 4（EPIC-4）冲刺计划草案待用户拍板。
- 2026-08-30 16:50（主理人）：A+B 执行完毕。团队 cordit-sprint4-gate：TASK-A（eng-m3demo）= M3 门3 沙箱实录成功 evidence/m3-gameplay.avi（17.53s@30fps，12.2MB）+ _m3_auto_demo/_m3_battle_host 临时场景 + tools/record_m3_gameplay.bat + 操作单 + GUT 187/187 零回归（m3-demo-gut-full.log）；重要勘误：沙箱非 headless console 启动有 GPU 可录，此前"无法渲染"仅对 --headless 成立。TASK-B（eng-s4plan）= production/sprints/sprint-4-draft.md（E4-S0~S7 共 19h，W1 基建/W2 遗迹/W3 闭环）。新发现疑似缺陷 D3：battle_command.gd _do_skill 用 targets_for() 遍历结算→单体技能命中全体敌人且忽略 target_slot（GUT 未覆盖多敌场打单体），已上报用户裁决是否立项。待用户拍板：门3 收口 commit（avi 需显式批准入库）、D3 立项、Sprint 4 五项拍板。
- 2026-08-30 17:35（主理人）：M3 门3 收口 commit 8944098（用户批准入库：avi 12.2MB + demo 临时场景 + bat + 操作单 + 双日志；11 files，+1265）。EPIC-3.md 门3 勾绿 = M3 五门全绿。EPIC-3 至此完全关闭。sprint-4-draft.md 留工作区未提交（待 Sprint 4 拍板后随开工 commit）。仍待用户拍板：D3 立项 + Sprint 4 五项。
- 2026-08-30 17:55（主理人）：TASK-D3 完成并验收。修复 battle_command.gd（+34/-2，_resolve_skill_targets 辅助：单体技能走 target_slot、群体不变、兜底=阵营数组序首个存活）；tests/gut/test_d3.gd 新增 13 例；全量 GUT 200/200 PASS（d3-gut-full.log）；M3 视频重录覆盖 m3-gameplay.avi（15.7MB，n240 帧可见甲虫①掉血/甲虫②满血=修复画面级证据）。建议 commit 划分两笔：①D3 修复 ②视频重录，待用户批准。sprint-4-draft.md 保持未跟踪勿误收。
- 2026-08-30 18:20（主理人）：用户拍板 Sprint 4 五项「全部按建议」（①19h 不变 ②不设回复点 ③tile-inspect 修路径保留 ④S5 硬编码先行 ⑤3 周节奏），sprint-4.md 转正生效（草案留档）。团队 cordit-studio-6693 就位。E4-S0 工具链修复完成并验收（程基岩，实际 14 分钟）：4 脚本改仓库相对路径（REPO_ROOT/RGBA_DIR 推算）+ README 缺陷表清零，五步链路 exit 0，gen_town 重生成产物 MD5 零漂移，verify_town 108/108 回归 PASS；证据 evidence/e4s0-toolchain-repair.md；未 commit。E4-S1 SaveManager 已派发（基线 200/200）。工作区遗留待批 commit：D3 修复一笔 + 视频重录一笔 + sprint-4 计划文档（随开工收口统一提交）。
- 2026-08-30 19:04（主理人）：E4-S1 SaveManager 完成并验收（程基岩，实际 12 分钟）：save_manager.gd 空壳→完整实现 ~210 行（save/load_save/has_save/last_loaded + _snapshot/_restore/_migrate/_atomic_write）；关键决策：map/position 调用方传参（A3 边界，GameData 零改动）、回灌走 last_loaded 供 E4-S7、原子写 tmp+rename 旧档零接触、version>当前拒绝读入、save_path 可覆写测试隔离。GUT 基线 200/200 → 收口 215/215（新增 15 例）。主理人独立复跑金标准通过（evidence/e4s1-gut-lead-verify.log，215/215 exit 0）。新踩坑：GUT 9.7 把测试期间引擎 ERROR 计为 Unexpected Errors，损坏 JSON 用例须 assert_engine_error 显式预期对冲。强杀实测=人工项留用户本机。程基岩会话随后因 429 限流中断（2026-08-31 16:02 重置），E4-S2 派发受阻；待用户定：等配额恢复派工 or 主理人直接执行。
- 2026-08-30 20:30（主理人）：用户拍板 B 线，主理人直接执行 E4-S2 道路地图并收口：新增 tools/gen_road.py + tools/verify_road.py（克隆 town 模板，65 项含 BFS 连通性/spawn 8 格安全两项新增）、scenes/maps/road.tscn（48×64 S 形盘山道+断桥虚空 ChasmBlocker+石像+双行林墙封堵带×5，TileSet 复用零新增）、scripts/maps/road_map.gd、tests/gut/test_e4s2.gd（6 例 PackedScene 级）。过程两轮 FAIL→FIX 有据：单行带被 BFS 抓出 62 tile 直穿捷径→双行化（220 tile=49s 纯走+交互≈60s 达标）；双行开口按格未按列封死南门→开口改列定义；GUT 全线装载失败根因=.godot/imported 缓存被清剩 1 文件→--import 重建（新踩坑：GUT 装载类用例报 ext_resource 错误先查导入缓存）。收口 221/221 exit 0；verify_road 65/65 + verify_town 108/108 回归。证据 evidence/e4s2-road-map.md + e4s2-gut-s2.log。真实走图体感留 M4 试玩。Sprint 4 进度 3/8（S0/S1/S2 ✅），下一步 S1 周线尾=派发或直执 S3 遗迹三层（前置：tileset 资产登记，程基岩配额明日 16:02 恢复）。

## E4-S3 前置：遗迹 tileset 选型（21:00 收口）
- 发现并修复 tile-inspect 解码管线缺陷：**未应用 tRNS**（temple PNG 调色板索引 3=全透明，透明区被 bKGD 填白）。新增 `tools/tile-inspect/decode_png.py`（PLTE+tRNS+ctype6，纯标准库），三张 .rgba 缓存重建（temple 透明占 64%，forest 0，town 104344/262144）。
- 目检结论：**f1/f2/f3 全用 classical_temple_tiles 单图集，无需 D1 Sewer/D2 Cave**（要素齐全+三层视觉统一）。D1/D2 触发条件=出现独立洞穴关卡设计，暂不登记。
- 选型表落档 `design/assets/temple-tileset-selection.md`：A 地面(27,22)+3 变体、B 砖墙(24,8 行+行9 垛带)、C 柱廊(39,44 3tile 竖排挂碰撞)、D 拱门(行10-13 构件组)、E 雕像(14,24 2×3 祭坛锚)、G 灰石 Boss 房(52,34+)；forest 仅草丛点缀(4,6)。
- 坐标口径：gen 脚本直接用 `atlas_coords = Vector2i(x, y)`；碰撞=B/C/D框/E/F/G 挂，A/D洞/H 不挂；透明贴片统一放装饰层。
- 教训：**用 .rgba 缓存做像素分析前必须确认 tRNS 已应用**，否则透明贴片全误判。

## Phase 0 诊断 + Sprint 4 前段收口 + E4-S3 开工（晚间 20:46-21:05+）
- 主理人阶段诊断：Phase 5 制作中段——Sprint 4 已开工，S0/S1/S2 完成（证据 e4s0~s2 齐全），GUT 基线 187→221。
- 意外发现：D3 真实缺陷（battle_command.gd _do_skill 用 targets_for 结算单体技能→命中全体、无视 target_slot 光标）已由录制线修复，test_d3.gd 专项 + 200/200 回归 + M3 视频重录（15.7MB/22s）。
- 用户拍板 A 路线：先 commit 再开工 S3。三笔落账：2242b99（M3 尾注 D3 修复+视频重录）→ db8a6be（Sprint 4 前段收口 S0~S2，38 文件）→ e39e644（chore 补录 30 个 .uid）。工作区清零。overview.md 已对齐当前位。
- E4-S3 已开工：TeamCreate cordit-sprint4 + spawn engineering-lead（程基岩），任务 #1，简报含全部冻结约束/环境口径/交付路径。红线：禁 commit。
- 流程坑：spawn 成员前必须先 TeamCreate，Agent 工具直接 team_name 会报 "No active team found"。

## E4-S3 收尾完成（晚间 22:04-22:20）
- 程基岩 53 分钟完成 E4-S3 施工（12 文件），主理人独立复验：test_e4s3 8/8、GUT 全量 229/229、verify_ruins 156/156 全 PASS。
- 验收发现三缺口：s3 回归日志坏（跑错 exe 路径）、证据档缺失、EPIC-4 验收框未勾。
- 打回后程基岩二次 429 中断（0s 失败）；用户拍板 A+B 双线：主理人代笔（沿 E4-S2 先例）+ 程基岩复核。
- 代笔结果：日志已用正确路径重跑覆写（229/229），evidence/e4s3-ruins-maps.md 证据档已写；EPIC-4.md 验收框实际已被程基岩在 429 前勾绿（含实际耗时 ≈1h 记录）——三缺口全消。
- 待办：B 线程基岩复核回传（或限流恢复确认）→ 请示用户落 S3 收口 commit。

## E4-S3 复核闭环（22:2x-22:3x）
- 程基岩限流恢复后复核回传：PASS 零修正（BFS 109/113/78、waypoints、229/229 全对）；工时窗口实测 56min（框架计时 53min），EPIC-4「实际 ≈1h」如实。
- 新坑入账：Python 工具在 PowerShell 控制台因 GBK 打不出 ✅ 抛 UnicodeEncodeError（断言跑完才崩，非产物问题）→ 统一 Git Bash 或 PYTHONIOENCODING=utf-8。
- S4 接手预判（程基岩）：低难度零欠账——waypoints 均为 visible_enemy 现行导出量；唯一正活=引入三态 state 枚举与迁移规则（现仅巡逻/驻守无显式枚举）。
- 证据档已补 §6 复核结论 + §7 环境备注与移交事项（原 §6 顺延改写）。
- 用户确认后落 S3 收口 commit `021ffb7`（14 文件，2698 insertions）。Sprint 4 进度：S0~S3 ✅（剩 S4 敌人三态 1.5h → S5 宝箱 2h → S6 传送 2.5h → S7 读档 1h ≈ 7h）。

## E4-S4 完成待收口（22:26-22:56+）
- 程基岩 30 分钟交付 E4-S4（1.5h 口径，-67%）：visible_enemy.gd 195→383 行，四态枚举 PATROL/GUARD/CHASE/RETURN，_set_state 唯一改态口；伪视野纯函数 _can_see（64px+±90° 点积）；放弃双阈值（96px 拉开 / 卡墙 1.5s——"目标向真实速度分量<25% 追速"修正 get_real_velocity 贴墙残速语义）；回位精确吸附。
- 主理人独立复验：GUT 全量 240/240 PASS（229 存量+11 新增零回归）；EPIC-4 两验收框已勾+耗时已记；证据档 e4s4-enemy-ai.md 七节完整。
- 实现亮点：f2 精英空 waypoints 装载即 GUARD，S3 场景零改动获得定守；E2-S2 14/14 旧用例零改动。给 S5/S6 影响面为零（无新存档字段）。
- 工作区待提交 7 文件（含 overview.md 已更新当前位）。E4-S4 收口待用户拍板。
- 用户确认后落 E4-S4 收口 commit `5cbc36b`（7 文件）。Sprint 4 进度：S0~S4 ✅（剩 S5 宝箱 2h → S6 传送 2.5h → S7 读档 1h = 5.5h）。

## 2026-08-31 凌晨续记（Sprint 4 推进）
- **E4-S5 收口**：commit `5c7ac2b`（44 文件 +2916）。27 点位全交互 + chests_opened 存档闭环；GUT 240→258。主理人独立复验 PASS（亲跑 exit 0）。
- **E4-S8 完成（待用户放行 commit）**：inventory 入存档 schema v1→2（+inventory 字段 + _migrate v<2 分支留空壳靠 SCHEMA 默认兜底）；JSON float→int 逐键转换防分型；ADR-3 正文同步十字段；既有测试 test_e4s1/test_e4s5 同步升级（非行为回归）。GUT 258→266（+8），主理人亲跑 266/266 exit 0 复验 PASS。
- Sprint 4 剩：S6 传送+自动存档 2.5h → S7 失败读档 1h，清完即达 M4 门。
- **E4-S8 收口**：commit `c1c4562`（8 文件 +1232）。inventory 持久化缺口关闭。
- **会话收尾归档（2026-08-31 01:55）**：MEMORY.md 进度节 + overview.md 当前位置/里程碑/风险表已刷新至"S0~S5+S8 ✅、剩 S6~S7 3.5h、基线 266/266"。下次会话直接续推 E4-S6（传送网络+进图自动存档 2.5h）→ S7（失败读档 1h）→ M4 收口仪式（用户本机试玩+视频#4+tag m4）。
