# 2026-08-31

## Git 远程同步与长效认证改造（凌晨会话）

- `.gitignore` 调整：`.workbuddy/` 整体排除 → 仅排除瞬时噪声（`godot_proc_check.txt`、`automations/`），`memory/` 三文件入库（commit `a41ff00`，用户终端推送成功）。
- GitHub 网络诊断：直连被重置；系统代理 `127.0.0.1:7892` 可用。已写 `git config --global http.https://github.com.proxy`（仅对 github.com 生效）。
- 长效方案落地（SSH 替代 HTTPS token）：
  - 生成 ed25519 密钥 `~/.ssh/id_ed25519`（无口令，注释 weixufeng@yozosoft.com-cordit）
  - `~/.ssh/config`：github.com → ssh.github.com:443 + ProxyCommand 走 7892（connect.exe 用 D:/software/Git 全路径）
  - 隧道已验证通（Permission denied (publickey) = 网络层 OK，待用户把公钥加到 GitHub 账号）
- 待办：用户在 GitHub 网页添加公钥后，把 origin 切到 `git@github.com:ysyonline/cordit.git`，再跑 `ssh -T git@github.com` 验证。明日新机器 clone 走 HTTPS 即可（远程已是最新）。

## SSH 切换完成（02:30）

- 用户已把公钥加到 GitHub 账号；`ssh -T` 返回 `Hi ysyonline!` 认证成功。
- origin 已切 `git@github.com:ysyonline/cordit.git`，push `a41ff00..0f1f0da` 成功——**SSH 推送在沙箱内也可用**（无需弹窗认证），后续会话可直接代推。
- 排障插曲：origin/main 引用一度"消失"，根因是诊断用的探针文件 `refs/heads/probeB`（非 SHA 内容）导致 fetch 中途失败回滚（"did not send all necessary objects"）；清理后 fetch 正常落盘。教训：往 .git/refs 里塞垃圾文件做实验会破坏 fetch，实验后必须立即清理。
- 最终核验三方一致（本地 main = origin/main = 远程真实 HEAD = `0f1f0da`）。

## origin/main 引用消失之谜——定性（02:45，修正早前误诊）

- 现象：会话内 `git fetch/push` 报成功，但 `.git/refs/remotes/origin/*` 随即消失（status 显示 `[gone]`），下一次 fetch 又报 "[new branch]"。曾误诊为后台 gc/maintenance 中断，关闭后依旧。
- **真根因**：WorkBuddy 内置 PortableGit（`~/.workbuddy/binaries/PortableGit/versions/1.2.0`）在 fetch/push 收尾时静默回滚 `refs/remotes/` 的写入（对象、refs/heads、reflog 均正常落盘，仅远程跟踪引用丢失）。
- **验证**：用户自装 Git（`D:\software\Git`，v2.41.0）fetch 后引用稳定落盘、status 干净。
- **影响面**：纯本地跟踪引用（装饰性），远程仓库与本地 main 均正确，push 实际成功。用户终端/VS Code 完全不受影响。
- **会话内对策**：需读 origin/main 时用 `/d/software/Git/cmd/git.exe fetch origin` 重建；或直接 `git ls-remote origin main` 查远程真实指针。仓库本地已设 `maintenance.auto=false`+`gc.auto=0`（无害，保留）。
- 踩坑：沙箱无法完成 GitHub OAuth 弹窗认证，push 认证必须用户终端执行；WorkBuddy 内置 Git Bash 无 connect.exe，须用 D:\software\Git 的。

## 新机环境核查（09:23，user3667 账户克隆到 D:\code\cordit）

- 用户从 GitHub 重新 clone 到本机（D:\code\cordit），工作树干净、与 origin/main 同步。
- **Godot 未安装**：全机（user3667 账户）搜不到任何 Godot exe；记忆里 `C:\Users\weixufeng\...` 的路径属旧账户，对本账户无效。开发第一硬前置 = 装 Godot 4.7.2（精确版本，GUT 9.7.1 兼容 4.7）。
- 本地 `origin` 实为 **HTTPS**（`https://github.com/ysyonline/cordit.git`），与凌晨日志里"切到 SSH"的 weixufeng 配置不一致——因为 SSH 密钥/`~/.ssh/config` 代理都活在旧账户家目录，本账户没有。
- **推送仍缺认证+网络**：本账户无 SSH 密钥，且 GitHub 直连被重置需走代理 `127.0.0.1:7892`（旧 ~/.gitconfig 的 proxy 也只活在旧账户）。拉取/阅读无需认证；推送需重做 SSH 密钥（加到 GitHub）或 HTTPS+PAT，并配代理。
- 其余已就位：GUT 9.7.1（addons/gut）、OGA 美术全在 assets/（58 个 .import）、Python 工具链（managed 3.13.14 + venv 含 numpy/opencv/pillow）、.gutconfig.json 在根。无 .godot 缓存，首次打开 Godot 会重建导入。

## Godot 4.7.2 安装完成（09:35）

- 从 GitHub release `4.7.2-stable` 下载 `Godot_v4.7.2-stable_win64.exe.zip`（82MB，断点续传补齐），解压到 `D:\software\Godot\`。
- 资产含两个 exe：**编辑器版** `Godot_v4.7.2-stable_win64.exe`（180MB）+ **控制台版** `Godot_v4.7.2-stable_win64_console.exe`（198KB）。项目旧文档 `tests/README.md` 里的 `Godot_console.exe` 命令指的就是控制台版，4.7.2 仍提供，未过时。
- 版本校验：`4.7.2.stable.official.ed1daf0bf` ✓。
- 首次 `--headless --import` 成功（46 步资产重导入，0 错误），生成 `.godot/global_script_class_cache.cfg`（GUT 全局类缓存）。
- **GUT 无头跑测验证：19 脚本 / 266 用例 / 266 通过 / 6674 断言，退出码 0，与工程基线 266/266 全绿一致**。开发环境已完全就绪。
- 结论：硬前置（Godot）已消除；剩 Git 推送认证 + 代理（本账户 user3667 无 SSH 密钥、无 github 代理）仍待用户配。zip 安装包可删（留作重装备份亦可）。

## Sprint 4 收尾开团（10:39，工作室模式 B 轻量双人）

- 阶段诊断：Phase 5 制作，距 M4 门 3.5h（S6 传送+自动存档 2.5h + S7 失败读档 1h）。
- 用户拍板方案 B：程基岩实现 + 主理人独立复验（不 spawn 严守真）。
- 团队 `cordit-sprint4-m4` 已建；任务卡 #1（E4-S6）→ #7（E4-S7）串行依赖 → #8（主理人复验）；程基岩已 spawn 领 #1。
- 主理人预调研已写进任务卡：12 处传送坐标（town 室内 4 + 跨图 8，门位出自 gen_road/gen_ruins）、各图 @export spawn、GDD §3.4 时序、SaveManager.last_loaded 接口。
- 三个风险已摆桌：R1 town 南门栅栏 (12,46)(13,46) 封路且 verify_town.py L125 断言锁死；R2 main_controller INITIAL_SCENE_PATH 还是 fixtures/map_a.tscn；R3 本机 Git 推送认证未配（不阻塞开发，M4 收口前拍板）。
- 裁定授权：同图室内传送是否自动存档由 S6 裁（建议跨图才存），理由记 evidence。

## 程基岩代理停摆 → 主理人接手，S6+S7 一次收口（12:50–15:00）

- **停摆诊断**：程基岩代理（另一会话）429 限流中断于基线测试后，S6 零代码产出。用户拍板「我在本会话接手」，按原任务卡实现。
- **S6 交付（全绿）**：teleport_catalog.gd（12 处正本）+ teleports.json（同构镜像）+ trigger_teleport.gd（薄壳）+ teleport_assembler.gd（装配器，town 旧 4 门退役）+ autosave_notifier.gd（§3.4 收口）+ save_icon.gd（图标）+ 五图接入 + test_e4s6.gd 19 条。
- **门控存档（关键裁决升级）**：初稿"无条件进图即存"在 R2 切初始场景后会**启动即覆盖玩家既有存档**；且 GDD §3.4 原文即"过传送点存，不进图即存"。改为 SaveManager.save_requested_pending 意图位：跨图传送受理/战后胜利置位 → map_ready 消费落盘；启动装载/同图室内不落盘。胜利即存兼兑现 §3.2 防复活，并修复"存档位=默认出生位而非回置位"坐标偏差。
- **数据修正**（弹回审计法 + 正本对表）：同图 4 处 target 半格→整数格（旧 @export 口径）；Inn_Exit/HouseA_Exit 触发区 tile 19→18/31→30（旧 tscn 像素反推）；f1/f2/f3 to_spawn y 3.5/2.5/2.5→3/2/2（verify_ruins 锚定 spawn_px）。修正后 12/12 零重合。
- **R1 落地**：gen_town.py 删南门栅栏 (12,46)(13,46) → 重跑生成 → verify_town L125 断言反转。verify_town 108/108。
- **R2 落地**：INITIAL_SCENE_PATH → town.tscn；波及适配 e1s4 wrapper（整层替换挂 map_a）、smk_e1s3 wrapper（Router 合法切回 map_a 恢复断言前提）、e1s6 冒烟（门节点名改 Evt_tp_*）。门控存档保证 SMK-12 零落盘口径不破。
- **E2-S2 门失效回归修复**：town 四门 mask=1 是玩家 layer=1 时代配置，E2-S2 改 layer=16 后静默失效；装配器统一 mask=16 修复，test_12 永久锁定。
- **S7 交付**：DEFEAT 真读档（load_save → last_loaded map/position → MAP_SCENE_PATHS 短名换路径 → 回置存档点+免疫；GameData 随 _restore 整体回滚）；读档失败兜底回暂存图+告警。test_e2s4 扩 2 条。
- **测试基线 266→286/286 全绿**（证据 evidence/e4s6-gut-full.log）；verify 三件套 108+156+65 全 PASS；证据档 evidence/e4s6-teleport-autosave.md。
- **待 M4 收口**：用户本机试玩 + 视频#4 + 冒烟 headless 复跑 + commit 拍板。未 commit。

## M4 收口完成（17:42，四项委托全绿）

- **四项委托全绿**（用户 502 恢复后「开始吧」启动）：①试玩代测（7 幕确定性演示 headless 全绿）②视频#4（m4-gameplay.avi 89.3MB/64s 真实引擎画面入库）③冒烟复跑④commit `07457c5` + tag `m4`；验收档 `evidence/m4-acceptance.md`（`c1d7b7e`）。
- **冒烟 2 FAIL 修复**：e1s5-A1 断言过时（旧四门→Evt_tp_* 薄壳，S6 R2 适配清单漏了 e1s5——升级 4 行断言期望值不变）；SMK-12 demo 存档槽残留（_m4_auto_demo.gd 收尾自清理+手工删残留）。复跑五通道全绿 EXIT=0。
- **tag 真相**：仓库 tag 列表为空——MEMORY 记载的 m1/m2/m3 历史会话只记未打。本次补打 m4 于 07457c5；m1/m2/m3 是否补打待用户拍板。
- **commit 规模**：63 files +3902/-179；.import 55 个纯行尾噪声沿 c1c4562 惯例不入库；19 个历史欠账 .uid 补齐；视频 89.3MB 直接入库（沿 m1/m2/m3 先例，仓库 size-pack 11→~100MiB，后续可评估 LFS）。
- **坑新增**：Git Bash 偶发 0xC0000005 崩溃（PortableGit add_item fatal）——命令重试即可，非环境损坏；e1s6 复跑入口是 headless_e1s6.tscn（wrapper 只有 .gd 无 .tscn，静态包装会被 Router 结构查找拒绝）。
- **遗留**：人眼手感抽查、m1/m2/m3 补标、tools/README.md 工具链登记、sfx E6 钩子。
