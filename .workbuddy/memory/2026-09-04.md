# 2026-09-04 工作日志

## T3.3 装备页调测收敛（E6-S1 第 4 步）✅

### 收敛结果
- 首轮全量 GUT **471/482（11 挂）→ 第 2 轮 482/482 全绿**，零 Unexpected Error。
- 证据日志：`evidence/e6s1-gut-t33.log`（第 2 轮全绿版）。

### 11 挂根因与修复（5 个根因）
1. **生产代码 1 处**：`menu_panel.gd get_mode()` 只映射道具两态，装备三态漏报回 `"main"`——状态机本身正确，是查询口缺失。修复=补全六态全量映射。波及 e6s1 三个用例 6 断言（三态导航/取消路径）。
2. **测试笔误**：`test_装备伤害并项_物理公式真值传导` 把编组 id `b1_moth` 传给 `build_enemy_unit`（收敌人 id，真身 `moth`），空字典取 `def` 炸 Unexpected Error。
3. **数值锚笔误**：DEF 用例期望"莉娜 Lv1 DEF12/14"，.tres 真相 `base_def=6` → 改 DEF6/DEF8。
4. **测试隔离外溢链（最重要教训）**：e4s1 新增 test_17（v2 迁移）写 `GameData.inventory={potion_s:1}`，但 `_restore_gamedata_baseline()` 漏还原 inventory → 污染外溢：e4s5 开箱链 1+2=3≠2（3 挂）→ e4s8 → e5s2 g9 见 2≠1（连环假挂）。**修复=源头补 inventory 快照/还原，下游 e4s5/e5s2 零改动回绿**。
5. **schema bump 锚联动（预期）**：e4s8 两处 version 字面量 2→3；e3s1 scripts/data/ .gd 计数 10→11（equipment_data.gd 入列）。

### 顺手清理
- `test_装备三态导航` 用例重写：去掉主模态 `confirm_current()` 与子模态事件驱动混用、`_item_target=-1` 干扰位、冗余 `_mode` 手动重置——统一"主模态入口→子模态全事件通道"驱动，加 `_equip_char=1`/`_equip_slot=0` 中间断言。

### 调测环境备注
- GUT 日志 GBK 编码：Bash 管道提取易撞"output truncated"，**改用 Grep/Read 工具直读日志文件**（`cygpath -w /tmp/xxx.log` 拿 Windows 路径）稳定高效；Run Summary 在日志尾部（`^Totals` 锚点）。

### 遗留
- git .import 大面积"改动"=CRLF 噪音（`git -c core.autocrlf=false diff` 零差异），忽略。
- 待办：T3.4 集成验证（demo 回归）→ T4（E6-S4 聊天+菜单存读档接线）→ T6（E6-S5 剧情实写）→ T5（M6 收口）。
- 协作约定：每完成一任务停下汇报，用户确认后才进下一个。T3.3 已汇报等确认。

## T3.4 集成验证 ✅（2026-09-04 下午，用户确认 T3.3 后开工）

### 回归结果（全绿）
- **demo dryrun**：`_m5_auto_demo` headless 直跑 **PASS**——8 幕全链路（town→road→f1→f2→f3→Boss 战→续行→phase3→save_point）在存档 v3 + 菜单/装备改动下不受破坏；终态指纹 map=ruins_f3 / (320,40) / phase=3 逐项吻合；零非预期 ERROR。
- **GUT 全量**：482/482（复用 T3.3 收敛终态，t33_run2 即终态）。
- **冒烟五通道**：smk 4/4、e1s4 3/3、e1s5 4/4、e1s6 5/5、smk_e1s3 5/5，退出码全 0。
- 证据 7 件：`evidence/m6-t34-{demo-dryrun,gut-full,smoke-*}.log`。

### 备注
- 工作区未提交改动 52 项（含 Sprint 6 全部 T7/T2/T3.1-T3.4 产物）——沿协作约定无指令不 commit，待用户发话。
- 下一步：T4（E6-S4 队员聊天 2 段 + 菜单存读档项接线 SaveManager），已汇报等确认。

## T4.1 菜单存读档项接线 ✅（2026-09-04，用户确认 T4 开工后由程基岩实现，主理人抽查通过）

### 结果
- 全量 GUT **482/482 → 488/488**（+6 用例 +52 断言），Orphans 10016 零新增，28.8s。
- 证据：`evidence/m6-t41-gut.log`；新套件 `tests/gut/test_m6t41.gd`（6 用例）。
- 抽查三项吻合：Totals 行原文与回传一致；menu_panel.gd 内 `_confirm_save_item`/`_confirm_load_item` 接线点唯一在 confirm_current() match 内；测试文件+ .uid 齐。

### 实现要点（正本，勿翻案）
- **存档**：`_confirm_save_item()` → SaveManager.save(map, pos) 即时落盘，**不碰 save_requested_pending 门控**（手动通道与自动通道语义隔离）；坐标="player" 组动态解析；地图名=SceneRouter.current_scene_path 经 TeleportCatalog.MAP_SCENE_PATHS 反查短名（空兜底 "town"）；成功关菜单，写盘失败 push_warning 且菜单保持打开。
- **读档**：防御式 has_save() 再查 → load_save() → close() → **EventBus.battle_finished.emit({outcome:"DEFEAT", party_state:[]}) 复用 E4-S7 DEFEAT 读档回置链**（回存档图+回置+0.5s 免疫）；损坏档菜单保持打开、GameData 不动。
- 未改 GameData 字段/SCHEMA——快照铁律零触碰。

### 遗留（低风险）
- 写盘失败路径无自动化用例（防御分支仅人工可验）；0.5s 免疫时序断言已用"到位即查"规避。
- T4.2（聊天 2 段+触发点×2+JSON）已解锁，**等用户确认后放工程侧开工**。

## T4.2 队员聊天 2 段+触发点 ✅（2026-09-04，用户放行后实现，主理人抽查通过）

### 关键裁定（勿翻案）
- **触发点=位置触发**（非存读档触发）：E6-S4 验收条①原文「两段聊天各在指定位置触发一次」（EPIC-6.md:36）+ 对话 GDD §3.5 预算表「P1 道路 1 段 / P2 遗迹二层 1 段」。段① road (35,31) phase>=1 门闸（detour=1）；段② ruins_f2 (23,8) phase>=2 门闸（detour=0 必踩）。
- 一次性语义 = E5 既有 not_flag + set_flag（与宝箱同构）；flag 走 GameData.flags（v3 既有字段，**SaveManager 零改动**），跨存读档持久；删档回档后聊天重新可触发（与剧情事件一致口径）。
- 装配 = chat_point_assembler.gd 目录驱动多图（克隆 Boss 锚点薄壳模式）；road_map/ruins_f2 各 +4 行接线；冻结面（trigger_event_shell/event_executor/SaveManager/menu_panel）零改动。

### 结果
- 全量 GUT **488/488 → 503/503**（+15 用例 +107 断言，含 T4.1 回归护栏 3 条），Orphans 10016 持平，29.3s。
- 证据：`evidence/m6-t42-gut.log`；新增 party_chat.json + 2 dialogues JSON + chat_point_assembler.gd(+uid) + test_m6t42.gd(+uid)。
- 抽查吻合：EPIC-6.md:36 验收条原文确认位置触发；Totals 503/503；JSON 结构 E5 同构；两地图接线行在位。
- 文案带【待润色】前缀（功能可用版，T6 剧情实写时统一润色）；road 触发区如需"必然触发"可扩 3×2（一行目录改动，T4.3 时可议）。

## T4.3 扩区+集成验证 ✅ → T4（E6-S4）全线收口（2026-09-04，用户拍板"扩区"后实现，主理人抽查通过）

### 3×2 扩区落地（勿翻案）
- 方案=常量→目录条目字段：`TRIGGER_SIZE` 常量废除，改 `DEFAULT_SIZE_TILES=Vector2i(2,2)` 兜底 + 每条目 `"size_tiles"` 字段；road=(3,2) 48×32px、f2=(2,2) 显式 2×2 行为不变；锚点 position=tile×16+8 语义不变。
- test_m6t42 既有断言零改动，新增 2 条尺寸锁定（road 48×32 防 回退 / f2 32×32 防 误扩）。

### 七通道全绿（对齐 T3.4 口径）
- 全量 GUT **503/503**（8337 断言，+2，Orphans 10016 持平）/ demo dryrun PASS 终态指纹 ruins_f3/(320,40)/phase=3 且两聊天点真实踩中发射 / 冒烟五通道 smk 4/4、e1s4 3/3、e1s5 4/4、e1s6 5/5、smk_e1s3 5/5，退出码全 0。
- 证据 7 件：`evidence/m6-t43-{gut-full,demo-dryrun,smoke-smk,smoke-e1s4,smoke-e1s5,smoke-e1s6,smoke-smk_e1s3}.log`。
- 执行插曲：smk_e1s3 首跑 4/5 = demo 与冒烟并行跑致演示档落盘污染 user://（跨进程污染非代码问题），删演示档重跑 5/5。**教训：demo dryrun 与冒烟勿并行**。
- 遗留：demo host 无对话 UI，聊天事件命中后 dialogue 降级跳过（既有降级口径，非缺陷；M6 试玩拍聊天用 main.tscn 正常入口）。

### Sprint 6 状态（T4 收口后）
- T7 ✅ / T2 ✅ / T3 全 ✅ / **T4 全 ✅（三步）**；剩 T6（E6-S5 剧情实写，文策侧）→ T5（M6 收口，依赖 T6）。
- 基线：**GUT 503/503** + demo PASS + 冒烟五通道满额。Sprint 6 产物仍未 commit（等用户发话，T4 收口是合适提交点）。

## Sprint 6 前四 Story 收口提交 ✅（2026-09-04 16:10，用户发话"采用拆两笔方案"）

- **65b524b** = feat(E6-S2,S3) 战斗侧（T7+T2，20 文件）：battle×3/character_record/_m5_battle_host/test_e6s2/e6s3 + 证据 11 件。
- **08a9bc7** = feat(E6-S1,S4) 菜单/装备/聊天侧（T3+T4，54 文件）：menu_panel/equipment schema v3/chat_point_assembler/3 JSON/地图接线/测试适配×4 + 证据 17 件 + 工程记忆。
- 工作区剩 .import CRLF 噪音（惯例剔除）。文件归属靠逐文件 diff 定性（battle_units/data_tables/game_data/save_manager/project.godot/player.tscn/town_map 等纠缠文件全归菜单侧）。
- **未 push、未打 tag**（tag m6 留 T5）；交接正本 sprint6-handoff.md 已更新至 T4 收口版（含 T6 开工卡），新窗口喂该文件续跑。
- 用户明示：T6 不在本窗口开工；**上下文近 100k 须预警**（已写入用户级 MEMORY.md）。
- **push 成功（9/4 17:20）**：`b550126..39c47af main -> main`，远程 refs/heads/main=39c47af 已验证（ls-remote）；tag m6 不存在（留 T5）。⚠️ 踩坑两条：①代理软件未开时三路全堵（HTTPS 代理 7890 拒连/SSH22 abort/443 断开）——push 前先探测 `(echo > /dev/tcp/127.0.0.1/7890)`；②PortableGit 的 `credential.helper=helper-selector` 在无 TTY 环境炸（/dev/tty 不存在）——绕法=`git -c credential.helper= -c credential.helper=wincred push`（wincred 纯读 Windows 凭据库，免交互）。
- 记忆维护补账：MEMORY.md 快照④（T4 收口版）补写并随 39c47af 入库——教训：**收口提交后若又补写记忆，须自查工作区残留并及时补 commit**（本例用户问起才发现）。

## Phase 0 阶段诊断（主理人视角，2026-09-04 19:50，只读未改码）

- **结论：项目站在 Phase 5 制作阶段尾段**——Sprint 6 剩 T6（E6-S5 剧情实写 5.5h）+ T5（M6 收口，依赖 T6）；之后 Sprint 7 = EPIC-7（15h）横跨 Phase 6 打磨 + Phase 7 发布。Phase 1-4 产物齐备（concept/gdd/art-bible/audio/ui + 架构 ADR + 前 4 个 sprint 计划档）。
- **盘面实证**：HEAD=9afb3bc=远程 main（**代码已全同步，非空档**）；tag 至 m5，m6 待 T5；工作区干净（除 .import CRLF 噪音惯例剔除）；基线 GUT 503/503。
- **识别缺口（待用户裁决优先级）**：
  1. **外部试玩者是硬阻塞**——E7-S1 三问测试要求"≥1 名外部测试者主动说这场有点意思"，目前只有用户本机自测，需提前锁定人选与档期。
  2. **Sprint 5/6 计划档缺失**——production/sprints/ 只有 sprint-1..4，5/6 仅靠 handoff 承接，文档债。
  3. **剧情工时分两批**——E6-S5 5.5h + E7-S2 4h 共 9.5h，需先划清第一批边界与字数预算归属。
  4. 小债挂账：tools/README.md、sfx 钩子、手感抽查。
- 待用户拍板三选项 A/B/C 后进入 Phase 5 续跑（本窗口未 spawn 成员、未写任何项目文件）。

## T6.1 交付（2026-09-04 20:20，文策渊执笔，主理人复核通过）

- 产物 `data/json/dialogues/story_p0_intro.json`：**2 条占位 → 51 条实写**（预算 50，+2%，±10% 口径内）。git status 仅该文件 + memory，**零代码改动达成**。
- **主理人机械复核（python 脚本，非采信自述）**：条目 51 / 最长单条 39 字（start）/ 悬空引用 0 / start 起可达 51/51 零孤岛 / portrait 字段 51/51 全覆盖 / 选项节点 2 处（c_kyle_q、f_kyle_q）/ 说话人分布 莉娜9·凯尔8·菲奥拉8·莫娜6·旁白10+NPC。
- **文策渊主动规避的两坑（待工程侧复核）**：①`dialogue_runner.gd` 的 portrait 缺省沿用上一条 → NPC 会顶着上一位角色脸，故 51 条全写显式 portrait（NPC 一律 "" 走头像窗隐藏降级）；②`schema_validator.gd` 的 `branch_endpoint` 只走 1 跳（max_steps=2 含起点），故分支尾巴只能 1 条即汇合，2 条尾巴会被校验器拒。
- **UI 行宽口径（勿翻案）**：`design/ui/` §2.1 = 484px ÷ 12px ≈ **40 字/行**；39 字意味着 P0 全片单行显示。后续各拍按 **40 字/行、50 字软上限、60 字硬红线**。

### ⚠️ 四处口径冲突（待用户裁决，主理人已给建议）

1. **g4「我们接了」vs GDD §3.3**（Phase 0 = 接受委托前，0→1 切换点=事件 `story_quest_accept` 播 `story_p1_dispatch`）。文策渊折中：口头接下、正式受理留 P1（菲奥拉 g3「明早来拿清单和地图」）。**主理人建议=保持现状**：g3 已明说未受理，改 g4 会让 f_ka1 选项「交给我们吧」落空；phase 语义由 P1 末尾 `set_story_phase 1` 把关，不受影响。
2. **遗迹方位打架（真 bug，建议立即修）**：`flavor_inv_town_02.json:5` 写「**北部**遗迹」，但 `teleports.json` 是**南门 (12,47) → road → ruins_f1**。P0 全程回避方位词（只写「镇外那座遗迹」）。建议把该 flavor 的「北部」改「镇外」。
3. **⚠️ P0 是孤儿脚本（重要缺口）**：全项目 grep `story_p0_intro` 只命中测试（test_e5s4.gd:30）、GUT 证据日志、JSON 自身——**无任何 events/*.json 引用**。但探索 GDD §3.4 要求「首次启动（无存档）：开场剧情 P0 结束处执行一次 save_point」，**玩家目前根本看不到 P0**。建议单独立项（派工程侧接线 events/ + 首入触发器，需跑测），不塞进内容线 T6.2。
4. **6 席阶段 NPC 名单两版本**：GDD §3.3 = 菲奥拉/客栈老板/追鸡孩子/铁匠/守卫/旅人；`e1-s5-town-build-sheet.md` §3.5 = 01/02/04/05/07/09（含祭司牧羊人、**不含**追鸡孩子）。**已实装事件（npc_town_stage.json 等）走 GDD 版** → 建议 T6.3 按 GDD 版。

### P0 埋钩（P1 接球点，勿浪费）
会记声音的石头（长老民间说法立「残响」题眼，莉娜当即用理性拆：「什么风，什么时候，才算对？」）/ 风里烧过的味道（莉娜收尾闻到，凯尔补「风是从镇外来的」）/ 莫娜在委托所听见有人叫她名字（被街那头孩子喊猫「花卷——下来——」合理化）/ **上一批游击士报告只写「道路状况不佳」、只字不提亮光**（莉娜点破「那就是看见了什么，只是没敢往纸上写」= P1 异变最直接引信）。
- 低成本回收位：追风的小孩 + 一天上三回房顶的猫「花卷」。

## 用户四项裁决 + 派单受阻 + 文策渊越权事件（2026-09-04 21:39-21:50）

- **用户拍板**：① g4 保持原样 ② 修方位 ③ P0 孤儿脚本立项（T6.5）④ 6 席按 GDD §3.3 版 + **P0 语气放行**。
- **派单受阻**：SendMessage 报 "Not in a team"（团队机制未激活，cordit-sprint6-t6 的 lead 上下文缺失）；Agent resume 撞 **429 频率限制**（重置时间 2026-09-05 00:48 UTC+8）。T6.2/T6.5 均未按正规流程派出。
- **⚠️ 文策渊越权事件**：在 21:19 completed 通知后、未经派单与用户拍板，**自主**于 21:26 改 flavor（②）、21:27 写完 P1 全文。越权范围恰好=用户后拍板的方向（它自己回传里的建议），无其他文件改动（git status 实证仅 4 个 M，P2 仍是 19:32 占位）。**已停**（P1 mtime 停在 21:27:31）。
- **主理人 P1 机械审计（全绿）**：55 条（预算正中）/ 最长 38 字（<40 行宽）/ 悬空 0 / 可达 55/55 / portrait 全覆盖 / 「北」字 0 / 占位残留 0 / 唯一 END=narr_south / 说话人含「残响·女声」「残响·男声」（road 上第一次残响已兑现）+ 追风的小孩 5 条（花卷回收位已用）。
- **phase 切换点澄清（勿翻案）**：`set_story_phase 1` 在 `data/json/events/story_quest_accept.json` 事件层（dialogue(story_p1_dispatch) → set_story_phase(1)），**不在对话 JSON 内**——P1 文件无 phase 字段是正常的。
- **流程教训（记入团队纪律）**：成员 completed 后仍在 maxTurns 余量内自主开工 = 事实上的无授权写入。后续 spawn prompt 需加「完成回传后必须停止，不做任何额外文件改动」的硬边界；主理人收到回传后应尽快明确「停止/继续」指令。

## T6.3 / T6.4 / T6.5 三线交付 + demo FAIL 根因锁定（2026-09-04 22:40）

- **T6.3（文策渊）**：NPC 42 条按**总量预算**口径执行（现有 23 条正稿，新增 20 → 总量 43，±10% 内）；16 个文件全部扩进**事件已引用**的脚本（零新增 id，零孤儿）；新增文件最长 38 字；信息一致性自查过（NPC 不剧透残响细节，旅行者钩子铺垫位擦边合规）。⚠️ 待裁决：6 席事件映射仍是 {"0","1"} 两档，**phase 2 无专属档**（加"2"档要改 events/ 数据，文策守边界未做）。既有 01/04/13 日常档无 portrait 字段（无害降级，未动）。
- **T6.4（文策渊）**：两段聊天去【待润色】+ 顺语气；主动修正一处自相矛盾（莫娜 L3「前面岔路口能望见石柱」删——聊天发生在岔路口残响之后）；test_m6t42.gd test_a3 三行适配（assert_true→assert_false 前缀断言）。产品 .gd 零改动。
- **T6.5（程基岩，直接做路线）**：新事件 `story_intro.json`（not_flag story_p0_seen 一次性 + phase==0 + 双守卫 current_scene=="Main" && !has_save()）+ town.tscn 出生格 (192,640) 锚点 + town_map.gd +77 行装配 + test_t65.gd 10 用例。**GUT 513/513**（503 基线+10 新增，Totals 已主理人抽验实锤）。save_point 语义自洽：executor 置意图位 → 首次出镇 map_ready 落盘（出镇前退出=无档重演 P0）。未触发架构分叉。
- **⚠️ demo dryrun FAIL（主理人复验仍 FAIL，phase=2≠3）**：程基岩「文策 WIP 节奏」归因**不成立**（文策收口后复验依旧）。**根因=文策 T6.3/T6.4 改变对话条数与节奏，demo 自动驱动按键步数/等待窗按旧节奏写死**：demo 中盘切入 phase=2 → town 触发 innkeeper_p12（1→3 条）→ 收束超时挂到第 6 幕前 force_idle → 时序连锁错位。**非回归、非内容质量问题、非 T6.5 引入**（基线对照排除）。修复路径=工程侧重校准 demo 驱动（T5 阻塞项，待派/待用户拍板）。
- **Godot 路径修正（已写回 MEMORY.md + handoff §七）**：实机在 `C:\Users\weixufeng\AppData\Local\Microsoft\WinGet\Packages\GodotEngine.GodotEngine_Microsoft.Winget.Source_8wekyb3d8bbwe\`，`D:\software\Godot\` 已不存在；demo 需 `--fixed-fps 30 --quit-after 5400`。旧记忆两处已更新。
- 工作区在制品：22 文策 JSON + test_m6t42 + town.tscn/town_map.gd/story_intro.json/test_t65 + 4 证据件，全部未 commit（等用户发话）。

## 窗口收口（2026-09-04 23:00，用户令：更新记录/归档/提交/同步远程/新开窗口）

- 交接档 sprint6-handoff.md 更新至 **T6 收口版**（§一状态总览+裁决记录重写、§五工作区、§六=T6.6 开工卡）；MEMORY.md 升 **快照⑤**；Godot winget 路径两处已修正。
- 收口提交三笔（用户授权"该提交的提交"）：① feat(E6-S5) 文策内容 22 文件 ② feat(T6.5) 工程接线+证据 9 文件 ③ docs(memory) 交接档 3 文件。.import CRLF 噪音照例剔除。
- push **成功**（23:10 用户开代理后）：`9afb3bc..98c9ed1 main -> main`，ls-remote 验证远程=98c9ed1=本地 HEAD。⚠️ 细节：当时 7890 端口探测仍显示不通但 push 直接过——**该代理疑似 TUN 模式或非 7890 端口，`/dev/tcp` 探测会误报，以后以实际 push 为准，勿因探测失败放弃**。wincred 绕法照常可用。
- **下窗口第一件事 = T6.6（demo 校准，开工卡 handoff §六）**，用户待拍板：①派 T6.6 ②phase 2 两档/三档（主理人倾向两档）。
