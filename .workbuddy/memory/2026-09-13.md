# 2026-09-13 工作日志

## B-01 引导链真人端到端验证 PASS（02:00 前后完成）

用户亲历 R-1 试玩者同款困惑（f3 石棺前绕圈不触发 Boss）→ 证实 phase=0 未接委托的引导缺位根因 → 指路后完整走链成功。

### 真人验证链（与 headless 复现逐环一致）
1. town 告示板按 Z 接委托 → phase 0→1，告示板「❗」消失，HUD 文案切「前往遗迹一层」
2. road 踩战斗点 → road_beetle_01 VICTORY
3. f1 入口自动触发 story_p2_ruin_enter → phase 1→2
4. f3 棺前按 Z → story_p3_boss_pre → b5_core Boss 战开打

### 战况与存档状态
- Boss 战结果：**DEFEAT**（凯尔阵亡致团灭）——数值预期内（Boss 倍率 2.5，kyle Lv1）
- 读档回滚：map=ruins_f3 | phase=2 | pos=(320,40) 南门，flags=[chat_road_01_seen, chat_f2_01_seen]，cleared=[road_beetle_01]，kyle Lv1 HP95/120，lina/mona 满血
- 事件簿记：BattleEventBridge DEFEAT 后簿记清空，Boss 事件可从头再触发
- DEFEAT 读档语义实战验证通过（回存档点+phase 保持）

### 重试建议（已告知用户）
- 可选：先打 f2 B4 石头守卫练级（2.5 倍率 HP 中等）/ 战斗中用防御与药
- 或直接重试 Boss（phase=2 状态未丢）

### 结论
- B-01 修复（告示板「❗Z」+ 常驻任务目标 HUD）在真人实机上全链路生效 → **R-1 根因修复实证闭环**
- R-1 复测判断依据变化：试玩者若按引导走，四环节均有显式指引；04 批次 zip 已含 B-01 修复，可发

## B-01 套件已 commit+push（02:47）

- **commit `1b5b925`**「fix(M7/B-01): 引导缺位修复——告示板「❗Z」提示+常驻任务目标HUD（GUT 551/551）」，22 文件 +4357/-63
- 含：B-01 三件套（hud/town_map/test）+ 冒烟件 + repro 三件 + evidence 六件（含 r1 答卷、_m7-verify-push.txt）+ 回收档 §0.3 + 记忆×3
- **m7-gameplay.avi（124MB 作废）未入库**，留磁盘 untracked
- push：代理 7892 已关（errno=10061）→ 改 `core.sshCommand=ssh -o ConnectTimeout=8` **直连 GitHub 成功**，remote main = `1b5b925`，工作区仅剩 avi untracked
- **04 批次 zip 已由用户发放试玩者**（02:44 拍板）
- 用户拍板：Boss 不打了（DEFEAT 一次即止）；**新开窗口续跑**

## 续跑入口（新窗口从这里接）

- **HEAD = remote main = `1b5b925`**，tag 仍止于 m7（dcee4d6）
- 等待中：试玩者复测回收（04 批次）→ R-3 重录（m7-gameplay.avi 须在含 B-01 的新包上录）→ M7 门最终判定
- Boss 战败读档存档在用户机器上：f3(320,40) phase=2，可重试（用户已表态不打）

## 游戏进程收尾（02:51 通知）

- 实机验证用的游戏进程（后台托住）已由用户正常退出，**rc=0 干净收场**，无悬挂任务
- 全量日志终版复核：DEFEAT 读档后用户其实又按了一次 Z **二次进入 Boss 战**（BattleEventBridge 二次转发 b5_core 成功）——「事件簿记清空后可重触发」不仅过了单测，真人实机也复现成功；用户在二番战开场即退出游戏
- 结论不变：B-01 链路 + DEFEAT 语义 + 事件重触发，三项真人级验证全部闭环

## R-3 重录前置核查与执行卡下发（02:52 新窗口）

- 状态核实：HEAD=79cd84d（=1b5b925+两笔 docs 收尾，remote main 同步）；磁盘 avi（130MB，09-12 16:30）=作废旧件，untracked，**bat 启动时会自动删除**，无需手动处理。
- 源码 HEAD 含 B-01（1b5b925 为祖先），与 04 批次 pck 内容一致 → **record_m7_gameplay.bat 直跑源码即满足「含 B-01 的新包」口径**（M5 同款 Movie Maker 直录模式）。
- 执行卡（方案 A'）：prep 不录（fresh 开局→告示板接委托→B1-B3 清完→f2 入口存档）→ bat 正式录（读档→B4 防御→B5 弱点/切目标→结尾钩子，全程 HUD 可见证 B-01 在场）。
- ⚠️ 已告知：现存 f3 phase=2 存档（Boss 重试态）将被 prep 存档覆盖；用户此前已表态 Boss 不再打。
- 待用户拍板：①按执行卡开录 ②确认覆盖存档 ③是否派严守真落 shotlist B-01 补遗。

## 试玩者复测回收（03:01 用户口头转述）

- **04 批次试玩者已玩完，反馈「没有大问题了」**——R-1 引导缺位在 B-01 修复后实证消除，复测通过（口头结论，无正式问卷）。
- 等待链①解除 → 仅剩 ②R-3 重录（执行卡已下发，用户可随时双击 bat）→ ③M7 门最终判定。
- 落档策略：复测口头结论暂记日志；R-3 交付后派严守真做 M7 门评审时一并正式落档（复测通过 + 视频证据作为门判输入）。

## R-3 录制执行完毕（03:05-03:20，主理人口径「录完了，不要检查了」）

- **prep 段**（03:03-03:11，普通模式）：fresh 开局→接委托（0→1 含凯尔选项）→road 甲虫 VICTORY→f1 自动 P2（1→2）→f2 入口手动存档 `ruins_f2 @ (384.0, 164.8)`，零 ERROR；旧 f3 存档被覆盖（既定口径）。
- **实录段**（03:14-03:18，Movie Maker 640×360@30，源码 HEAD=79cd84d 含 B-01）：读档→f2→f3→开宝箱（potion_l）→**03:18 菜单退出**。产物 `evidence/m7-gameplay.avi` 11415 帧=**6 分 20 秒**，`evidence/m7-recording.log` 已由我方落盘（跳过零 ERROR 核查=用户拍板）。
- **⚠️ 内容事实（按控制台事件日志，非内容检查）**：录制段**无战斗事件、无结局事件**——B4/B5/结尾钩子三镜头均未入片；片长 6:20 超 shotlist 2-3 分钟目标。R-3 现状=**部分覆盖**（新包+B-01 HUD 在片✓；shotlist ③④⑤ 未取证）。
- 主理人两轮表态：先问「B4B5 能否不打」，被告知 B5+结尾不可免后仍提前收工；「不要检查了」。
- **待办**：①avi+log LFS 入库与 push（**无指令不 commit**，等用户拍板）②M7 门判定时 R-3 部分覆盖须摆上台面（补录 B5+结尾约 2 分钟 vs 用户豁免）③M8 前恢复 design/。

## R-3 转向 AI 代录（03:22 用户授权「你自己来录，把流程都走一遍」）

- 用户拒绝 A/B 选项，直接授权 AI 代录全流程（B4/B5/结尾）——**合法授权链完整**。
- 派单：engineering-lead（程基岩）spawn，Task M7-R3-AUTO。方案=M6 auto-demo 范式（真 main.tscn 入口+确定性禁随机+存档红线）+ M5 B5 胜利剧本移植 + O-7 UI 信号通道驱动（避免直驱 submit_command 掩盖接线）。
- 交付路径锁定：tools/dev/_m7_r3_autodrive.{gd,tscn} + evidence/_m7_r3_dryrun.log + evidence/m7-gameplay.avi（覆盖手录版=预期）+ m7-recording.log。存档方案：APPDATA 沙盒复制真实档起手（比 M6 备份协议更干净），真实 save.json 零风险。
- 汇报纪律：回传后我核对交付物与 dryrun 断言，再向用户汇报；不 commit 等拍板。

## R-3 AI 代录完成（06:33-08:4x，Task M7-R3-AUTO 关单）

- **交付六件全核验**：①evidence/m7-gameplay.avi 重录版（122MB / **3004 帧=1:40.13** @30fps，08:33）②m7-recording.log（**29 断言全 PASS 零 FAIL**，唯一 WARNING=B5 击破凭据空占位属 dev 预期）③_m7_r3_dryrun.log ④preplant_run.log ⑤production/qa/m7-r3-recording-findings.md（三生产缺口带行号证据）⑥production/sprints/story-battle-item-persistence.md（Story 立项未实现）。
- **链路实证**：B4 R4 VICTORY（群愈/毒击/毒 tick/防御/群击全在场）→ B5 R7 VICTORY（蓄力预告/防御应对减伤/「弱点！」弹字/charge_release）→ finale 末条 52 字停留 2.4s → phase=3 落地+存档兑现（f3 (320,40)）。确定性跨进程稳定（干跑与录制逐字节一致）。
- **preplant 口径（M7 门评审须知）**：录制起点=沙盒预植档（非用户真实档）：Lv3 改 Lv4 满配、f2 (384,40) phase=2、ether_s×2（保底 B5 R7 必要补给，偏离既有 ×1 已留档）；用户真实 save.json 全程零接触。
- **时长裁定**：1:40 接受不重录（AI 信号通道结构性快，三镜头硬要求全覆盖）；作为口径偏差备注由严守真在 M7 门评审签字豁免。
- **三生产缺口（findings 正本，下 Sprint 候选）**：a) charge 事件无生产 UI 反应（battle_ui 只渲染 damage/weakness；battle_command 头注与事实不符=_emit_all 空桩）；b) 战前对话 story_p3_boss_front 被转场强制收束玩家实况不可见（executor 同步流，需产品侧确认叙事预期）；c) 战斗内道具消耗不持久化（M6 修复②同族，已立 Story）。
- 驱动器三件套（tools/dev/_m7_r3_autodrive/_m7_r3_battle_director/.tscn/.uid）暂留，处置随 tool-disposition 惯例（录后可删）。
- **R-3 状态：达成（AI 代录口径）**。待办链：用户拍板 → LFS 入库+push → 派严守真 M7 门终审（R-3 全证据+复测口头结论+时长豁免）→ tag m7 收口 → M8 前恢复 design/。

