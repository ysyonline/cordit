# 2026-09-12 · Phase 0 阶段诊断（第二次同问复核）

## 背景
用户再问"现在该走哪个阶段"。查实：**2026-09-08 21:53 已做过一次同题诊断**（见日志 2026-09-08.md 末节），
结论 = Phase 6 打磨尾 → Phase 7 发布门，release-checklist NO-GO（9 阻塞）。

## 本次实查（非采信 9/8 记录）
- HEAD = `c05d164`（9/8 21:50），**9/8 21:50 之后 4 天零提交**；工作区净（仅 .workbuddy/artifacts 不入库）。
- tag 仍止于 `m6` —— **m7 未打**。
- QA 门判定文件 `production/qa/m7-final-qa-gate.md` = 🟡 CONCERNS（有条件放行）。
  放行三条件 R-1 外部试玩者反馈 / R-2 非开发机实测 / R-3 视频 #7 归档 —— **三项全未回收**。
- 已解项（9/8 提交链）：A1 工作区变更已 8 批入库、A3 GUT 530/530（`a1e3582`）、D1 bart 城堡件已移除、
  design/ 52 件已裁定 B 入库（`4f3b717`）。
- 未解项：A2 导出包未在提交后重导（`export/win/` 时间戳仍为 Sep 5 19:56 < 9/8 提交）；
  tag m7 未打；A4 exe 版本号空（`export_presets.cfg` product_version=""）、A5 图标仍占位（icon=""）、
  D2 随包许可/署名未验证。

## 判定
**阶段未变** —— 仍在 Phase 7 · 发布门（M7 关闸前最后一关）。门只卡在"外部的人 + 另一台机器"，
代码侧零失败零回归，无需改代码。

## ⚠️ 本次新增记录（9/8 日志未写明的风险）
- **design/ 正本只在 git 历史里**：GDD v1.1（battle-system-gdd.md）、art-bible、temple 选型表等 52 件
  经 `4f3b717` 确认删除入库，工作区已无。取回方式 `git show 4f3b717^:design/<path>`。
  → 若 M7 后走新周期（M8/正式版），**数值与系统正本必须先恢复**，否则下一周期无参照。
- 9/8 提到的"立体感三路线（甲/乙/丙）"未落任何文档（全库 grep 零命中），属口头方案，需用户重述或补档。

## 05:23 用户拍板 A → 建队派单
- **TeamCreate `m7-closeout`**（主理人亲自建，未委派）。
- 并行 spawn 两名成员（文件不重叠、可独立推进）：
  - `release-ops-lead` · **M7-CLOSE-01 包侧收尾** → 重导包+冒烟、版本号回填（0.1.0）、图标方案（不执行替换）、
    随包许可 D2 验证、打 zip `轨迹残响-v0.1.0-slice-win64.zip`；产出 `production/release/m7-package-closeout.md` + 更新 release-checklist。
  - `quality-lead` · **M7-CLOSE-02 试玩包定稿** → 修订 playtest-kit 过期节、发放前 checklist、
    新建 `production/playtests/m7-external-playtest-01.md` 回收模板、B1/B2 时长处置预案。

## ⚠️ 本轮派单前的关键发现（本次诊断真正的增量价值）
- **方向键键位已在代码里补齐**（`project.godot`：move_left/right/up/down 各绑 4194319–4194322，
  interact 绑 Space(32)+Enter(4194309)，cancel 绑 Esc）—— 9/8 提交 `7f8627a` 所为。
- **但 `export/win/` 包仍是 9/5 19:56 旧包 → 现包仍是 WASD-only**。
  ⇒ **若直接拿现包发试玩者，恰好踩中 playtest-kit §7.3/§7.4 警告的那个坑**（普通玩家第一反应按方向键 → 误判"游戏坏了"）。
  ⇒ A2「提交后重导」由"可延后"**升级为发放前的硬前置**；已写入两份 spawn prompt 与 kit 修订要求。
- playtest-kit §7.3/§7.4 因上述修复而**过期**，须由 quality-lead 修订（已派）。

## 用户侧待办（关键路径，主理人无权代做）
1. 锁定 ≥1 名外部试玩者（唯一真外部依赖）；2. 本机录试玩视频 #7 → `evidence/m7-gameplay.avi`；
3. 游戏内目检（头像/告示板接线/键位提示/R2 形象分配）。

## 05:40 两成员交付 → 汇编落 `production/release/m7-gate-assembly.md`
### ✅ M7-CLOSE-01（release-ops-lead）包侧收尾完成
- 重导：exe 109,132,800 B / pck 3,403,476 B（+198.3 KiB），导出 EXIT=0，冒烟 **EXIT=0 ×2**（含干净 APPDATA 首启 → P0 锚点 → story_p0_intro 51 条目 → save_point 请求）；默认 APPDATA 那次 `has_save=true` 顺带佐证打包环境读档生效。
- 版本号 `0.1.0` 已实测进 exe 属性（OS VersionInfo + PE VS_VERSIONINFO 双证）；company_name/copyright 留占位待用户给定。
- 许可**验到**：pck 内 `assets/CREDITS.md` + 12 条 `assets/licenses/**`，反向排除 0 命中。
- zip `export/轨迹残响-v0.1.0-slice-win64.zip`（40.05 MiB，CRC OK）；旧包备份 `export/_prev-20260905-win/`（禁发放）。
- 证据 ×4：`evidence/m7-closeout-{export,smoke}.log`、`m7-closeout-exe-version.txt`、`m7-closeout-pck-verify.log`。

### ✅ M7-CLOSE-02（quality-lead）试玩包定稿
- §7.3/§7.4 过期节改写；§7.1 扩为发放前 checklist；新增 §7.6 分发物红线、§7.7 时长预案；回收模板 `production/playtests/m7-external-playtest-01.md`。§6 阈值零改动（diff -U0 核验）。

### ⚠️⚠️ 三条事实更正 + 主理人失误教训（**重要，勿再犯**）
1. **旧包不是 WASD-only**（推翻我 05:23 的诊断）。实读证据：`project.godot` 工作区 mtime = **9/5 19:52:47**，旧包产出 **19:56:58** → 方向键已被打进旧包；`7f8627a`(9/8) 只是把 9/5 已生效的工作区改动**提交入库**。
   **根因教训：判断"包里有什么"必须看导出时刻的*工作区状态*（文件 mtime + 二进制实证），不能看 commit 时间——提交时间 ≠ 内容时间。** 下次核包一律走 mtime + 解包 keycode/资源命中，禁止用 commit 日期推断。
2. 重导仍必要，但理由是**美术落后一档**（9/5 20:00 后 11 个文件：R7 换装 9 件 / A1 光照 / A2 装饰 / CREDITS.md），非功能修复。已在旧包内：B4B5 数值、头像修复+71 对话 JSON、E7-S2 剧情、键位、中文字体。
3. 旧清单两处结论过期已更正：pck 内**有** 12 条 licenses（非"无"）；字体已配置（"豆腐块"项目侧根因已消除）。

### ⚠️ 本轮新暴露高风险：D1 仅"部分解除"
- `ad2efa2` 只把 castle 件从 **HEAD 树**移出；**git 历史 blob 仍可达**（`git log --all -- '*castle*'` 可列，8af601f 加入 / ad2efa2 删除）。
- ⇒ 历史未清 + 公开 clone = 仍构成 GPL/CC-BY-SA 再分发 → **公开仓库路径仍 NO-GO，不要对外说 D1 已解决**。

### 待用户拍板：U-1..U-7（详见 closeout §8）+ B1/B2 时长口径 + D1 仓库路径
- U-1 提交 export_presets.cfg 改动（否则 clone 重导丢版本号/署名）｜U-2/U-3 署名真值｜U-4/U-5 CREDITS 松动件｜U-6 图标（建议门内保持现状）｜U-7 tag m7。
- 无 `CREDITS.txt` 文件，实际落点 `assets/CREDITS.md`；许可只在 pck 内（分发件旁无可读文本）→ D2 拆 a(已验到)/b(未达成)。
- 门仍未关：R-1/R-2/R-3 全未回收，唯一依赖仍是"人 + 第二台机器"。

## 06:00 用户四条裁定 + 包侧闭环（HEAD 前进到 539ba19）
### 用户裁定（2026-09-12）
- **署名主体 = `轨迹残响工作室`** → `company_name` / `copyright` 回填真值。
- **U-1 = 现在补提交** → 已执行 **commit `539ba19`**（仅 `export_presets.cfg`，1 file / 4+ 4−，无 CRLF 噪音，未用 `git add -A`）。**未 push、未 tag**。
- **B1/B2 时长 = A 判为可接受**（教学场低于 2 分钟下限属设计意图；预置裁定，实测待问 7 回收）。
- **D1 = 仓库保持私有，只发 zip**（不执行历史清理）；口径纪律：**不得对外声称 D1 已技术性解决**（历史 blob 客观可达）。
- **追加包级证据 = 要加**（用户原话"要加"）→ 已派 quality-lead：干净 APPDATA 全量 GUT（期望 530/530）+ 门控补 G-0 节。
- **U-5 = 按 credits-audit §6 建议文本修订** `assets/CREDITS.md`（B 区 2 条悬空登记移入新增 E 区"规划中（未产出）"）。
- **U-6 = 门内保持现状**（图标不改；换正式图标排为 **M7 后 polish 首项**）。

### 包侧闭环结果（M7-CLOSE-01B）
- exe `01F7DCF0…`（109,132,800 B，**体积一字节未变**→署名只进属性）；pck `0B206AE1…`（**两轮一致 → 资源零改动**）；zip `C1C2187B…`（42,019,046 B / 40.07 MiB）。
- exe 属性双证一致：FileVersion/ProductVersion `0.1.0`、CompanyName `轨迹残响工作室`、LegalCopyright `Copyright (c) 2026 轨迹残响工作室`，旧占位 **0 命中**。
- zip **14 条目**：exe + pck（根目录同名，满足 embed_pck=false 配对）+ `CREDITS.txt` + `licenses/` 12 条（CC0-1.0/CC-BY-3.0/CC-BY-4.0/OFL-1.1 + notices×7）→ **U-4 松动件已就位**。
- 冒烟 EXIT=0 ×2，两轮均含 `行走图 M1` 与 `[MapLighting]` 行 → R7/A1 运行期生效。
- D1 裁定已记录；changelog 时长口径已改（未写成"已达成"）。
- ⚠️ **环境异常备忘**：本会话 PowerShell/Bash 的 **stdout 捕获曾中途失效（返回空）** → release-ops 改用"命令写文件 + Read 读回"取证，结论可靠；中途一次 RUN1 报 EXIT=1 无日志，单跑不可复现，判为工具伪影。

### 主理人裁定与授权（编排层）
- **kit §6.4 与 A 裁定冲突** → 裁定 **(a) 追加补注**（不改原判据文字，仅把 ❌ 适用面收窄到 B3+ 正常战；B1/B2 按 A 可接受）。
- **授权 release-ops 补正 changelog 两处同类过期表述**：`:105` 操作行（补方向键/空格/回车——"试玩者不知方向键"的另一半根因）、`:94`（中文显示→已内置像素字体）。
- **⚠️ CREDITS 链式影响（关键提醒）**：`assets/CREDITS.md` **同时在 pck 内与 zip 松动件内** ⇒ U-5 修订后必须
  **改 CREDITS.md → 重导包（pck 会变）→ 冒烟 → 重渲染 CREDITS.txt → 重打 zip → 更新全部指纹**。
  只重打 zip 会造出"pck 内旧致谢 + 松动件新致谢"的**新不一致**。
- 文档批次（`production/**`、`evidence/**`、`assets/CREDITS.md`）**统一等主理人发话再入库**。

### 全局状态
- **门仍是 🟡 CONCERNS**；包与工具已全部就绪，**唯一剩余依赖 = 外部的人 + 第二台机器**。
- 试玩邀请话术已备好（基于 kit §1–§5 提炼，可直接转发）；问卷须等玩到结尾再发。

## 08:50 "继续"续跑收口（HEAD → `4b9d49d`）
- **C3 CREDITS 三方一致性复核 PASS**（汇编 §七 未做项闭合）：源件指纹 `760E0665…` 与 verify.log [9] 一致；zip 松动件 CREDITS.txt 实读含修订后 E 区（"规划中"×3）；zip 内 pck 与 export/win pck 逐字节一致（`BAA778C5…`）。三方同为修订后文本。
- 追加包级 GUT 确认：干净 APPDATA 全量 **530/530**（m7-closeout-gut-clean.log）。
- **文档批次 16 件入库 `4b9d49d`**（汇编正本/closeout/kit/checklist/changelog/playtests 模板/CREDITS.md/证据×6/记忆×2）；精确 add，工作区仅剩 `.workbuddy/artifacts/`（不入库）。未 push、未 tag。
- **阶段结论不变**：Phase 7 · 发布门 🟡，唯一阻塞 R-1 外部试玩者 / R-2 非开发机 / R-3 视频 #7——全在用户侧（人+机器+录屏）。AI 侧可做事项已全部清零。

## 09:01 用户裁定"1执行" → 发放动作闭环（HEAD → `ae419ff`）
- **发放前硬检查 §7.1-A PASS**：zip/exe/pck 三件 SHA256 实测与 closeout §2 最终值逐字节一致（`25BF07E9…` / `01F7DCF0…` / `BAA778C5…`）；导出时间 06:05:30 双件成对。
- **回收档 §0 元信息已回填**（kit 版本/导出时间/三件指纹/体积/发放形式=zip/硬检查相符勾选）；试玩者标识、发放/回收日期留待用户填。
- **新建邀请话术提炼件 `production/playtests/m7-playtest-invite.md`**（§1-§5 提炼为一页转发文本：文件说明+SmartScreen 放行+键位表+两点请求+问卷后置提醒）。
- commit `ae419ff`（2 files）。**实际"发"给试玩者 = 用户手工动作**（AI 无渠道），物料已全就绪：zip + 话术 + 问卷（kit §5，玩到结尾再发）。
- ⚠️ 提醒过用户：只发话术+zip+问卷，**kit §6/§7 绝不可发给试玩者**。

## 09:05 用户确认"已经给了" → R-1/R-2 在途
- **发放日期 2026-09-12 已回填回收档 §0**（试玩者化名待回收时补）。
- 关闸状态更新：R-1 ⏳ 在途（等试玩反馈）/ R-2 ⏳ 在途（问 12/13 非开发机项顺带覆盖）/ R-3 ❌ 仍未录（本机视频 #7，与试玩并行，不等对方）。
- 等待期 AI 侧可做：polish 预研（图标/立体感三路线需用户重述）、design/ 正本恢复预演——均已告知，等用户点单。

## 09:08 用户裁定"执行R3" → 录制执行件就绪（HEAD → `39ec692`）
- **前置检查全绿**：Godot console exe 在（winget 路径）；GameDVR=1；OBS 未装（走 Movie Maker，无需录屏软件）；`scenes/main.tscn` 与 project.godot main_scene 一致；无 movie_writer 配置=默认 AVI JPEG writer（m3/m4/m5 同款）。
- **⚠️ M6 口径发现**：M6 视频实际也是 auto-demo（m6-closeout R6 行自认"非真人手动"）——**M7 是第一次真要求手动游玩取证**，shotlist 口径"不走 auto-demo"必须遵守，不能沿用 m5 的 bat 直接套。
- **用户存档现状**：map=ruins_f1 / story_phase=0 / Lv1（9/8 旧档，未到 B4）→ 方案 A 赶路段需从 f1 真打上去（B1-B3 别全逃），到 B4 前手动存档再开录。
- **交付件**：`tools/record_m7_gameplay.bat`（main.tscn 入口 + `--write-movie --fixed-fps 30`，**无 --quit-after**，玩家正常退出收盘；GODOT_EXE 指 winget console 版）+ `evidence/_r3-user-steps.txt`（三阶段操作卡）。
- 分工：玩+退出=用户；启动器/日志核验/验收勾绿=AI。用户回"录好了"后按 shotlist §三 ③④⑤ 验收 + 日志零 ERROR 检查。

## 09:50 R-3 乙方案落地：预置存档写入 + headless 验档 PASS（AI 执行）
- **原档定性修正**：备份 `_r3-save-backup-20260912-0933-save.json` 实为**全新档**（phase=0/Lv1/flags 空集全空/装备未穿）——"忠实玩家自然进度"前提不成立，预置档按"理想自然进度"口径构造。
- **`tools/dev/_r3_preplant_save.py` 已执行**：写入真实存档槽 `…app_userdata\轨迹残响\save.json`。落盘口径：map=ruins_f2 pos=(384,40)、phase=2、flags=["story_p0_seen"]（**全主线唯一 flag**——P1 `story_quest_accept`/P2 `story_ruin_enter` 均靠 `set_story_phase` 门闸推进，不写旗标；曾误写 story_p1/p2_seen 已纠正）、凯尔 Lv3 200/16 代穿 iron_sword+leather_armor、莉娜 146/46、莫娜 159/38、cleared 5 敌、开 6 箱、背包 potion_s×3/potion_m×2/ether_s×1/antidote×1、equipment 池空。
- **headless 验档 PASS**（`tools/dev/_r3_smoke_verify.gd` → `evidence/_m7-r3-smoke-verify.log`，EXIT=0）：真实链路 `load_save()` → `battle_finished(DEFEAT)` → BattleResultHandler 回图链，读档落位 f2 南门 (384,40)、phase=2、flags 正确、凯尔 Lv3/200/装备齐、B4 守卫 `ruins_f2_guardian` 在场、**预置档未被覆盖**（AutosaveNotifier 门控正常跳过写盘）。
- **新坑（-s SceneTree 脚本）**：Autoload 标识符（SaveManager/EventBus/GameData）在编译期不可见，须 `root.get_node("AutoloadName")` 运行期取；`-s` 模式无 Main/World 常驻根，`SceneRouter.change_scene` 拒绝切换 → 场景验证改"直接 instantiate f2 场景"替代（读档回灌逻辑不受影响）。
- **⚠️ O-5 缺陷已登记**（m7-final-qa-gate.md G-8 表）：`story_ruin_enter` 生产侧零触发端（scripts/scenes/gen_ruins.py 全库无装配）→ 自然游玩 phase 无法 1→2，**B5 链在自然流程下会断**；Major，M8 前必修。预置档直接写 phase=2 恰好绕开，不阻塞录制。
- **用户侧操作（已交底）**：双击 `tools\record_m7_gameplay.bat` → 主菜单选继续 → 从 f2 南门开打 B4 → f3 棺前按交互键（Z/E）开 B5 → 结尾钩子「第一个，还有五个。」停 2 秒 → 菜单退出。回滚命令在 `evidence/_r3-restore-command.txt`。

## 10:00+ 玩家反馈两条 + O-6 Blocker 发现（碰怪无战斗，🔴 本日最大事件）
- **反馈 #1**（无坐标显示/没看到遗迹守卫/无明确提示）→ 分诊：守卫在出生点下方 22 格（约 3.5 屏），暗红占位块辨识度低；坐标无 HUD 属设计现状。与 O-6 相关（守卫本该被 CHASE 追击暴露）。
- **反馈 #2 = 「碰怪无战斗」→ 根因坐实为 O-6 Blocker，证据链四条**：
  1. `git log --follow scenes/battle/battle.tscn` **仅 1 条提交 `588c176`**（E2-S3 占位交付，第 2 周）——E3 真战斗（第 3-4 周）上线后**从未回改此场景**；
  2. battle_scene.gd 占位实现（彩色方块 3~9 个 + 胜利/失败按钮），头注释自述"战斗是假的，数据流必须是真的"；
  3. **真实战斗（BattleCommand+BattleUI）全库装配点只有 GUT 测试与 `evidence/_m3/_m4/_m5_battle_host`（录屏宿主）**；M6 视频"战斗能打"靠 demo 内 BattleDirector 叠加层——全都不是生产链路；
  4. 玩家试玩实证：遇敌链路其实**触发了**（visible_enemy 发射链 / scene_router 路由链 / 物理层 player layer16 ↔ TouchArea mask16 三方代码层全通畅），进的是占位方块画面，玩家不认为这是战斗。
- **为什么 523/523 全绿没拦住**：GUT 直驱 BattleCommand（不进场景树），生产路由 `SceneRouter.BATTLE_SCENE_PATH → battle.tscn` 与被测对象**不是同一条链**——验证盲区。demo dryrun 同理（占位场景也走 Router，走通≠真战斗）。
- **连带失效**：A8 行 3「遇敌能打」生产包不成立；E7-S1 三问（防御/换目标/有意思的一场）自然流程下不可能达成；R-3 视频若用现包录，战斗段全是占位画面。
- **QA 门已降级 🔴 FAIL**（原 🟡 CONCERNS，2026-09-05 判定作废）：G-8 表新增 O-6（Blocker），O-4/O-5 保留；放行条件**新增 R-0**（接线修复 → GUT 不回退 → **重新导出** → 新包指纹更新）——**顺序红线：R-0 → 重导出 → R-1/R-2/R-3**；**已发放的 zip（SHA256 25BF07E9…）作废停发**，若试玩者还没开玩应追发新包。
- **修复样板已定位**：`evidence/_m5_battle_host.gd` L80-95 四行装配（BattleCommand.new → setup(payload.group_id, BattleUnits.build_party(Lv), build_encounter) → start() → BattleUI.new+bind(bc)），生产化时改从 GameData.party 现值构造（build_party_unit 逐个+覆写 hp/mp，build_party 是满血口径勿直接用）+ 消费 get_staged_payload。
- **待用户拍板**：① 是否立即派单修 O-6（建议：是，M7 门已 FAIL 无可争议）；② 试玩者是否需追发新包；③ O-4（方向键入包已修但那是 9/5 包的事，此包已含）——已在包内无需重复修。
- **文档侧**：O-6/O-4/O-5 已登记 m7-final-qa-gate.md；两条玩家反馈待试玩者正式答卷后并入 m7-external-playtest-01.md 缺陷表（当前先以 QA 门 O-6 为正本）。

## 晚间续：O-6/O-5 修复闭环 + 重导出补录（R-0 销项）

- **O-6 修复落地 ✅**：battle_scene.gd 重写接入 E3 真实战斗（生产路由 battle.tscn 场景内装配真实 BattleCommand+BattleUI）；test_e2s3.gd 重写为真实绑定。新用例当场揪出 battle_command.setup() 生产缺陷——无类型 Array.duplicate() 直接赋 Array[Dictionary] 成员报类型错，改逐元素 append 重建（与 set_inventory 同款防御）。
- **O-5 修复落地 ✅**：ruins_f1_map.gd 装配入口剧情锚 Evt_Ruin_Enter（双守卫：current_scene==Main + 事件表在册；(448,56)/mask=16；new_event_id→story_ruin_enter）。生产语境冒烟 8/8 PASS（_o5-smoke-verify3.log），真实触发链发射日志在案。
- **回归**：GUT 530/530 ×2（_o6-gut-clean2.log / _o5-gut-clean.log，APPDATA 重定向干净环境）。
- **重导出**：exe 109,132,800 B（与 01D 逐字节一致——改动面仅脚本/测试归 pck）/ pck 3,405,028 B D6AC9B5E…；导出包 headless 冒烟 EXIT=0。正本=closeout §13。
- **文档**：closeout §13 补录（指纹/证据/延续待办）；QA gate R-0 销项 + O-5/O-6 修复标注 + FAIL 判定下追记（门仍 FAIL，仅剩 R-1/R-2/R-3 外部回收，翻转待主理人）。
- **坑（本轮实证）**：①WorkBuddy Bash shim 坏（dirname/cd 报错），全程走 python -c / 脚本通道；②Read 工具显示层对中文/标识符有漂移，关键文件（指纹、哈希、代码）一律 python repr()/unicode_escape 字节级回读；③python 正则提取文档表格时注意反引号包裹（上轮三连 MATCH=False 根因）；④GDScript 无类型 Array.duplicate() 会退化类型，赋 Array[Dictionary] 成员必炸——凡 duplicate 进类型化成员必须逐元素 append。
- **下一步**：zip 重打包 + §7/试玩档 §0/R-0 指纹回写 + commit/tag —— 等主理人发话。

## 14:35 Phase 0 复诊 + R-3 时序取证（视频 #7 判无效）

### 阶段判定（第二次复诊，实查非采信）
- HEAD `4821c42`（09-12 09:11）；tag 仍止于 m6；工作区 50 处变更（O-5/O-6 修复套件+文档+记忆，等 commit）。
- QA 门正本 `production/qa/m7-final-qa-gate.md` = **🔴 FAIL 维持**（2026-09-12 降级判定未翻转）；
  R-0 已销项（追记在案），放行剩 **R-1 外部试玩 / R-2 非开发机 / R-3 视频#7** 三项外部回收。
- **阶段结论：Phase 7 · 发布门不变**，代码侧零失败（GUT 530/530），门只卡外部回收。

### 🔴 R-3 时序取证（本次诊断核心增量）
- `evidence/m7-gameplay.avi` 存在：2.9 GiB（3,077,686,518 B），mtime **10:47:01**，SHA256 `2251DEDAA610C85D…`。
- 时间线对撞：视频 10:47 < battle_scene.gd 12:20 < battle_command.gd 13:40 < ruins_f1_map.gd 14:01 < 导出包 14:08。
- **判定：视频录制时刻，O-6 战斗修复尚未发生 → 视频内战斗段为占位方块画面 → R-3 无效，须在新包（14:08 版）上重录**。
- 与 QA 门 R-3 行口径完全一致："须在 R-0 修复后的包上录，否则视频里战斗段是占位画面"。

### 14:40 用户拍板 A → 02 批次重打包闭环（AI 执行完毕）
- **zip 重打**：`export/轨迹残响-v0.1.0-slice-win64.zip` = **02 批次**，SHA256 `B8221BCB…2FFD6FAB`（42,014,458 B，14 条目，CRC PASS）。pck 用 14:08 修复版（`D6AC9B5E…`），exe 不变（`01F7DCF0…`）；zip 内 exe/pck 与 export/win 逐字节一致（zipfile 读回比对）。旧 zip 退役至 `export/_RETIRED-06-06-old-package.zip`（未删除）。
- **pck 抽验**：`battle_scene` ×5、`story_ruin_enter` ×1 字符串命中 → 02 包内含真战斗+剧情锚。
- **指纹回写三处**：closeout §7（追加 02 批次行）+ §13.2（作废警示改"已重打包"）；playtest 档 **§0.1 追加更正节**（历史记录保留不改，硬检查标注须按 02 重做）；kit 新增 **§7.4.2 批次口径更新**（01D/02 对照表 + 实机判别特征：真战斗 UI vs 彩色方块）。全部经**字节级回读核对**（Read 显示层漂移教训执行）。
- **🔴→✅ 预置存档被覆盖隐患已修复**：核查发现 save.json 已被 10:47 游玩写回 f1/Lv1/phase0（mtime 11:08）——若不处理，重录将从错误起点开始。已重跑 `_r3_preplant_save.py`（mtime 14:54）恢复 ruins_f2/(384,40)/phase=2/flags=["story_p0_seen"]/三人 Lv3。headless 验档 **PASS**（`evidence/_r3-reverify-1440.log`，EXIT=0）：读档落位 f2 南门 / DEFEAT 回图链通 / B4 守卫在场 / **档未覆盖**（AutosaveNotifier 正确跳过）。
- **⚠️ 待用户**：①**追发 02 zip 给试玩者**（红线，01D 作废）；②本机重录视频（bat → 主菜单"继续" → B4 → f3 交互键 → 钩子停 2s → 退出）；③录完叫 AI 验收（shotlist §三 ③④⑤ + 日志零 ERROR + 时长三问）。


## 16:35 O-7 战斗卡死 Blocker 立案 → 修复 → GUT 535/535 全绿 → 冒烟 PASS

### 立案（用户重录 R-3 时实锤）
- 症状：碰怪能进真战斗界面（O-6 修好路由），但点指令无响应，停在"战斗初始化"。
- 根因：O-6 只装配漏驱动——battle_scene.gd 零处消费 ui.command_selected、零处调 submit_command/enemy_action。
  玩家链（UI 信号→状态机）从未被测：M5 demo _autoplay 与 GUT _drive_battle 都直调 submit_command 绕过 UI。
- 立案号 **O-7**（QA G-8 序列），battle_scene.gd 头注释为正本。

### 修复（用户拍板 A 全套；全在 battle_scene.gd）
- ①输入桥 ui.command_selected→_on_command_selected→submit_command（variance_from_roll 生产浮动 0.9~1.1）；
- ②敌方自动驱动 _drive_until_party_turn（0.9s 节拍，ENEMY_ACT_BEAT）；提交后/回到我方时 refresh_command_menu；
- ③结算驻留：battle_over 后等揭示弹完（interact 跳过，15s 护栏）→play_outro 黑屏 0.5s→才转发 battle_finished
  （修复"胜利瞬间秒回图、E6-S2 结算画面生产链不可见"的连带缺陷）；_finalizing 守门防重。

### 测试
- O-6 两处转发断言改轮询等待 _wait_battle_finished（转发延迟 1.6s+ 属预期）。
- 新增 tests/gut/test_o7_ui_bridge.gd 5 用例（接线/消费/防越权/UI驱动至胜利驻留转发/逃跑转发）——封死"玩家链"盲区。
- **⚠️ 新坑入册：GUT 不隔离用户存档**——真实 save.json（R-3 预置 f2/Lv3 档）经 user:// 泄入测试造成 12 条假红
  （期望 Lv1 实得 Lv3 等）。修法：跑测命令加 APPDATA=D:/code/cordit/.godot_user_tmp/gut-sandbox 重定向。
  **跑测正本命令自此带上 APPDATA 重定向**（与 e7s3 导出管线同手法）。
- 结果：**GUT 535/535 全绿**（evidence/o7-gut-file2.log，61s）；headless 冒烟 PASS
  （evidence/o7-smoke.log：UI信号→三人攻击→VICTORY→掉落入包→转发，全链通）。

### 待办（等用户发话）
- ①重导出 exe+pck → ②重打 zip（**03 批次**，02 批次 B8221BCB… 作废）→ ③指纹回写三文档 → ④工作区变更 commit+tag。
- 试玩者发包必须等 03；R-3 重录也须在 03 上做。

## 17:25 用户问「我怎么验证」→ 发现并修复 O-8（目标选择键盘断点）

- **O-8 立案**：核查战斗 UI 输入闭环时发现 move_cursor/confirm_target/cancel_targeting 生产零调用——
  O-7 修完后点「攻击」能进目标选择态，但无键可确认（只有防御/逃跑直达信号）。GUT 直调函数再次掩盖断点。
- **修复**：battle_ui.gd _unhandled_input 补目标选择态键盘驱动（←→↑↓移光标 / interact 确认 / cancel 取消），
  揭示态 interact 跳过行为保留。
- **测试**：test_o7_ui_bridge 新增 2 用例（键盘确认含 target_slot / 键盘取消回菜单）；GUT **537/537 全绿**
  （evidence/o8-gut-full.log）；冒烟复跑 PASS（evidence/o8-smoke.log）。
- **验证引导**：游戏已从源码拉起（PID 10480），预置档 f2 mtime 14:54 完好；用户走 读取→B4→碰守卫→Z确认攻击。
- **批号影响**：03 批次 zip 现在必须同时含 O-7+O-8 两笔修复。

## 17:45 首次实测反馈 → 抓到并修复 O-9（转场层全屏吞鼠标点击）

- **用户实测**：进战斗后指令仍无响应（日志零「玩家指令」行），用户关窗退出。
- **O-9 根因**：BattleTransition / BattleHitFeedback 是全屏 Control 容器，自身未设
  mouse_filter=IGNORE（只设了子节点）——Godot Control 默认 STOP，置顶转场层
  吞掉整屏鼠标点击，指令菜单按钮永远收不到 pressed。GUT 用信号注入/直调函数，
  不走鼠标命中检测，第 4 次掩盖输入层断点。
- **修复**：transition / hit_feedback 容器自身 + PredBar/StatusBar/TargetCursor/
  DmgLabel/ResultPanel/背景层统一补 IGNORE；CmdMenu/SubMenu 保留 STOP（按钮宿主）。
- **测试**：test_o7_ui_bridge 新增 O-9 用例（7 个节点 mouse_filter 断言）；GUT **538/538 全绿**
  （evidence/o9-gut-full.log）。
- **验证**：已重新拉起游戏（PID 3264）。鼠标点击与键盘（←→↑↓/Z/X）双通道均应可用。
- **教训入册**：输入层 bug 链 O-7(信号无人消费)→O-8(目标确认无按键)→O-9(转场层吞鼠标)
  ——三层断点全是「GUT 直调/信号注入测不到」的盲区；**UI 交互必须配 mouse_filter/命中
  类断言 + 真人实测**，纯逻辑单测对输入层零覆盖。

## 19:08 用户拍板四问处置（①B ②复验 ③A ④A）→ O-10 批次实施完成

- **四问定性**（取证见会话）：① b5_core 不在地图=设计（story_boss_pre 事件经 f3 棺前锚点 (19-20,35) 交互键召唤）——引导缺口；② f3→f2 返程数据链完整（tp_f3_to_f2 @ tile(19,0) 南门薄条 16px），待用户贴最顶门洞复验；③④ 新需求。
- **①B Boss 交互提示**（ruins_f3_map.gd +_attach_interact_hint）：棺前锚点上方浮动「❗Z」金色 12px 标签，0.6s 呼吸脉冲；挂场景根直铺（y-sort 会把 YSorted 内锚点的子 Label 压到棺画下——Control 不参与 y-sort，根直子节点渲染序恒最上）。
- **③A 地图名 HUD**（新 scripts/ui/map_name_hud.gd + .uid；teleport_catalog.gd +MAP_DISPLAY_NAMES 五图中文显示名+display_name()）：UILayer 常驻（town_map.gd _assemble_map_name_hud，与 menu_panel 同构守卫：按脚本判重防重复 connect）；map_ready 驱动淡入0.3+驻留2.4+淡出0.6；左上角羊皮纸底墨字 12px；【装配顺序坑】必须挂 town _ready 内 announce_ready（即 map_ready 广播）之前。
- **④A 存档反馈升级**（save_icon.gd 重写：12×12 方块 0.5s → 「已存档 · <图名>」文字条 1.6s；flash(p_ok) 兼容签名保留 + 新 flash_map(p_ok,map_name)；autosave_notifier._spawn_save_icon 加 map_name 参数透传；menu_panel.gd _confirm_save_item 成功/失败均弹反馈条，复用 save_icon 挂 ui_host）。
- **两坑两教训**：
  1. map_name_hud 初版漏 preload TeleportCatalog → 解析错误在 -s 模式被 debugger 断点轰炸 50 万次（日志 3.2 亿字符）——headless 跑测日志须先查体积再 grep；
  2. save_icon._bg 用 PRESET_FULL_RECT+赋 size → 引擎「非等对锚改 size」告警，GUT 判 Unexpected Errors 8 条假红（m6t41/m6t42）→ 改默认等值锚点+直接设 position/size。
- **验证**：GUT **538/538 全绿**（.godot_user_tmp/gut_run_o10.log；修复前 530/538→修复后全绿）；headless 冒烟 tools/dev/_o10_smoke_verify.gd **4/4 PASS**（display_name 映射 / InteractHint 在位文案❗Z / HUD 收 map_ready town→f3 映射链 / flash_map 文案「已存档 · 遗迹第三层」）。
- **冒烟语义澄清**：f3 地图不自装 HUD 是架构正解（town 首装+UILayer 常驻跨图复用，同 menu_panel）；冒烟直接驱动 map_ready 广播链验证。
- **改动文件清单**：teleport_catalog.gd / map_name_hud.gd(新)+uid / save_icon.gd(重写) / autosave_notifier.gd / menu_panel.gd / town_map.gd / ruins_f3_map.gd / tools/dev/_o10_smoke_verify.gd(新)。
- **② 待用户复验**：f3 读档后向北贴最顶端门洞（tile(19-20,0)），触发区 16px 薄条；若仍不切图回传 godot.log 时间点。
- **下一步**：03 批次 zip 须含 O-7/O-8/O-9/O-10 四笔；R-1/R-2/R-3 在 03 批次上回收。


## 20:15 归档收尾（用户开新窗口续跑）

- 用户指令「归档。我要开新窗口来继续」——本会话终了。
- 记忆落盘已完成：MEMORY.md 当前状态节改写为**归档版快照**（含六笔修复清单/GUT 538/538+冒烟 4/4/唯一未决=f3→f2 用户复验/待办六步序/git status 20:15 实盘未提交清单）。新窗口按 MEMORY.md「待办」节续跑即可，无需回读本日志。
- git status 实盘：M 18 文件 + ?? 新增约 50 件（清单见 MEMORY.md 归档节末行）。**未 commit**（协作铁律：无用户指令不 commit）。

## 21:1x O-13 顶部触发区不对称修复（用户拍板 A：4 条全修）

- **用户新规（本窗口起）**：每开始一个任务前必须确认，同意才执行。已用 AskUserQuestion 逐项拍板：①先冒烟定位（A）→②修全部 4 条顶部触发区（A）。
- **根因实证（非猜测）**：玩家碰撞盒 = `Collision` RectangleShape2D **size(12,6) offset(0,-3)**（挂脚底原点**上方**，极小）。顶部触发区（tile y=0，size 2×1 → 像素 y∈[0,16]）实测要求角色中心走到 **y≤16** 才触发（`body_entered`），站在门洞第 2 格中心 y=24 毫无反应；底部触发区（tile y=max）方向相反、碰撞盒先接触，天然两格宽容度 → **不对称**。这就是"贴门洞走却不切图"的真因（不是路不通，门洞 (19,0)(20,0)+(19,1)(20,1) 已挖空）。
- **修复**：4 条顶部触发区 size (2,1)→**(2,2)**（覆盖 tile y=0~1，阈值放宽到中心 y≤32）：tp_road_to_town / tp_f1_to_road / tp_f2_to_f1 / tp_f3_to_f2。改 `scripts/events/teleport_catalog.gd`（含头注新增「O-13 顶部触发区不对称」段）+ `data/json/events/teleports.json` 镜像两处同步。
- **余量核算**：落位距触发下沿 f1/road 24px、f2/f3 8px，均不弹回；北向即出口方向，8px 不构成误触。
- **排除方案**：C2（整块下移到 tile y=1）探针实测贴墙位 x=296,y=40 误触发 → 已排除并写入头注警告。
- **验证**：`tools/dev/_f3f2_smoke_verify.gd` 修复前阈值 y=16 → 修复后 **y=32**（SMOKE PASS）；`tools/dev/_f3f2_probe2.gd` 候选配置探针（**脚本内改 shape 模拟，不落盘**）；**GUT 538/538 全绿，8504 asserts，test_e4s6 19/19**（镜像锁死未破）。证据：`evidence/_f3f2-smoke.log`、`_f3f2-probe2.log`、`_f3f2-smoke-after.log`、`_o13-gut-full.log`。
- **⚠️ 新坑（探针方法论）**：①脚本内改 `shape.size` 后物理服务器有 **2~3 个位点延迟**才生效，候选几何的前几个位点读到的是旧几何 → 探针前 2~3 行数据不可信，须丢弃；②`body_entered` 是**进入边沿**信号，瞬移到区内后不"移出"再进则后续位点恒 false（曾误判 y=12/8/4/0 不触发），判定前必须先把玩家挪到远处等几帧重置。
- **下一步(待用户确认)**:重导出 exe+pck → 重打 zip **03 批次**(须含 O-5~O-13)→ 指纹回写三文档 → commit + tag m7 → R-1/R-2/R-3 回收。

## 21:20 实机启动（源码直跑，非 exe）

- 用户要"启动游戏看效果"→ 跑**当前源码**(`Godot_v4.7.2-stable_win64.exe --path D:\code\cordit`，不加 --editor 即直跑游戏)，非 export/win 旧 exe（14:08，不含 O-13）。
- **⚠️ 新坑：用 `DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP` 启动 Godot GUI 会"刚启动就关"**（进程 6s 内消失，但日志显示 town 已装载成功、无崩溃堆栈）。改回普通 `subprocess.Popen`（保留管道/不 detach）后 30s 稳定存活。**以后启动 GUI 游戏一律用普通 Popen。**
- **日志辨识**：`godot.log` = 本次会话；`godot<时间戳>.log` = 上次会话轮转件。GUT 的 `-s` 运行会产生 **315MB** 警告轰炸日志（旧坑复现），别误判为本次崩溃——本次 godot.log 仅 1.3KB 且 OpenGL/常驻根/town 装载全部正常。
- 启动实测串：`OpenGL 3.3.0 Compatibility (Intel UHD)` → `[MainController] 常驻根就绪` → `[SceneRouter] 装载完成 -> town.tscn`（含 `[TownMap] 地图名 HUD 装配完成` = O-10 生效）。

## O-13 实机验证闭环（21:45）
- 用户实机走完全部返程：tp_f3_to_f2 ×2、tp_f2_to_f1、tp_f1_to_road、tp_road_to_town 全部 受理=true；手感确认「好走」；全程零 ERROR/WARNING。冒烟+实机双确认，O-13 锁死。
- ⚠️ 零风险记录一条：tp_f2_to_f1 出现一次 受理=false（SceneRouter 忙时拒绝，玩家折返重踩后成功）。若试玩者反馈「门没反应」，候选修法=触发器请求被拒后短暂重试或排队；M8 前不主动改。
- 启动方式坑补全：Bash 工具内 Popen 启动 GUI 游戏，命令结束即被沙盒收掉进程树（前两轮「闪退」真因，非游戏崩溃）；正解=run_in_background 挂住 p.wait() 托住进程树，游戏退出 rc=0 正常收尾。
- 下一步：03 批次导出（exe+pck+zip，须含 O-5~O-13）等用户发话。

## 03 批次导出完成（21:50）
- 导出坑再证：headless 导出必须带 APPDATA=D:/code/cordit/.godot_user_tmp（模板装在 .godot_user_tmp/Godot/export_templates/4.7.2.stable/），否则 rc=1 找不到模板；导出日志零 ERROR/WARNING（evidence/_o13-batch03-export.log）。
- 组装：02 旧 zip 归档为 _RETIRED-14-51-batch02.zip；CREDITS.txt 直接从 02 zip 抽取（玩家版成品 12596 字节）；licenses/ 从 assets/licenses 复制。
- 03 批次指纹（SHA256 前 16 位）：zip=D94C410E1C17B12A（39903.8 KB，21:50）· exe=01F7DCF08A63F2B1 · pck=B73F705DACA746BD（3332.4 KB）。exe 尺寸与 02 相同属正常（引擎模板二进制不变，差异在 pck）。
- 待办续接：④指纹回写三文档（closeout §7 / playtest §0.1 / kit §7.4.2）→ ⑤commit+tag m7 → ⑥R-1/R-2/R-3。

## 03 批次验证 + 指纹回写完成（21:5x）
- 导出包启动验证：03 exe 启动至清溪镇（主理人目检画面正常）；tasklist 的 Not Responding 为游戏窗口误报；release 版日志缓冲至退出才落盘（godot.log 0 字节属正常）。日志轮转：21.53.34.log 实为源码版末段轨迹（含 tp_f2_to_f1 受理=false 实锤=「上一轮切换淡入淡出尚未完成，拒绝重入」，SceneRouter 忙时拒绝，非 bug）。
- 指纹回写三处全落：closeout §7 加 03 行 + 新增 §13.5（03 批次补录全表）；playtest-01 加 §0.2；kit 加 §7.4.3。zip 校验实测：testzip CRC OK、14 条目、UTF-8 0x800 全置位。
- 03 批次完整指纹：zip=D94C410E1C17B12A…CEA26（40,861,491 B）；pck=B73F705DACA746BD…07570（3,412,416 B）；exe=01F7DCF0…不变。
- 下一步：⑤工作区 commit（O-5~O-13 套件+4 份 production 文档+记忆）+ tag m7 → ⑥R-1/R-2/R-3 回收。

## ⑤ commit + tag m7 完成（22:0x）
- 提交 dcee4d6「fix(M7): O-5~O-13 全套修复 + 03 批次重导出与指纹回写（GUT 538/538）」，112 文件（amend 后），+27k 行。轻量 tag m7 = dcee4d6，风格对齐 m1~m6。
- 提交内拦截两件：m7-gameplay.avi（124MB 作废件不入库，留磁盘，R-3 重录后随新证据入库）+ .workbuddy/artifacts/m7-commit-plan.md（误入后 amend 清出，.gitignore 已补 .workbuddy/artifacts/ 规则）。
- 工作区残余：仅 ?? evidence/m7-gameplay.avi（有意不入库）。
- 待办：push（等用户发话，远端 git@github.com:ysyonline/cordit.git 走 127.0.0.1:7892 代理）→ ⑥ R-1/R-2/R-3。

## push 完成 · M7 门控收口（22:14）
- push origin main m7：c05d164..dcee4d6 main -> main + [new tag] m7 -> m7，rc=0（走 7892 代理；stderr 的 crashpad 报错为 Git for Windows 噪音，不影响）。
- 三方一致实证（evidence/_m7-verify-push.txt）：local HEAD = local tag m7 = remote main = remote tag m7 = dcee4d6f454bf4c9b8917788476906ed9380b9f6。与 M6 收口口径相同。
- 注意：该 push 命令因代理环境变量需要触发沙箱豁免（用户批准后执行）。
- M7 待办全部清零；剩余 ⑥ R-1（试玩者追发 03 zip）/ R-2（非开发机）/ R-3（重录 m7-gameplay.avi，现有 avi 作废留磁盘）。

## R-1 发放前置完成（22:1x）
- 发放包指纹复核：D94C410E1C17B12A…CEA26 逐字节吻合，mtime 21:50 未被触碰。
- 组装 export/R1-发放包-20260912/ 三件：①轨迹残响 zip（发放件）②发给试玩者-问卷.txt（kit §1-§5 正文+头注，UTF-8 BOM）③R1发放备忘-自用.txt（指纹/红线/口信/回收路径）。
- 实际发送动作待用户执行（微信/网盘等渠道由用户决定）；发完后回收档 §0.2 硬检查栏按备忘指纹勾选即可。

## R-1 已发出（22:37）
- 用户确认 03 批次三件套已发试玩者。回收档 §0.2 已勾：硬检查相符 + 追发状态=已发出（2026-09-12）。
- R-1 状态转「在途，等答卷」；回收时问卷原话归档 m7-external-playtest-01.md §1-§5 对应栏。
- 剩余：R-2 非开发机（可由该试玩者覆盖，等问 12 答案）+ R-3 重录 m7-gameplay.avi。

## R-1 答卷回收归档完成（22:5x）
- 试玩者答卷已逐题原话归档至 m7-external-playtest-01.md §1（含错别字照录）；§5 证据清单勾选答卷+指纹两项。判定区（§2/§4）留给流程，未代填。
- ⚠️ 高信号四条（初读，未经正式判定）：①问7 未打到 Boss——「看见感叹号触发了对话完，就不知道怎么继续触发boss战了」→ f3 Boss 锚交互链疑似断点（故事 phase 链或提示不足，story_phase=0 佐证）②问1+问7 看不见守卫图像（战斗敌人可视性）③问6 不知道怎么切换敌人（O-8 目标选择键盘的可见性/教学缺口）④问13 story_phase=0 → 剧情推进链在真机上一环未触发，与①互证。
- 正面：非开发机启动零异常（问12）、存档读写有 save.json（问13）、无卡死无崩溃。R-2 非开发机启动项大概率可判达成。
- 待办：主理人走 §2 判定 + §4 结论；Boss 触发断点需诊断（story_ruin_enter → f3 boss_pre 链路）；答卷原文件建议移出发放目录防覆盖。

## 归档收尾（23:0x，新会话续跑入口）
- 答卷原件已移入 evidence/r1-playtest-01-answered-20260912.txt（sha256 前16=35FA47BCA8345693，10659 B），发放目录已清出，回收档 §5 路径已更新。
- 本日会话内已完成：O-13 修复+实机验证 → 03 批次导出+验证 → 指纹回写 → commit dcee4d6 + tag m7 + push 三方一致 → R-1 发出 → 答卷归档。
- ⚠️ 未提交（下会话开工第一件事征询用户）：m7-external-playtest-01.md（§0.2 勾选+§1 归档+§5 更新）+ evidence/r1-playtest-01-answered-20260912.txt + 今日两份记忆文件。
- ⚠️ 下会话最高优先：诊断剧情链真机断点（story_phase=0 + 试玩者未触发 f1 入口锚/boss 战，两条互证）——先 headless 复现 f1 入口锚→phase 1→2→f3 boss_pre 全链路，定位代码断点 vs 引导缺位；再谈 §2/§4 正式判定与 M7 关账。
- 三条体验缺口待排期：战斗敌人可视化（看不见守卫图像）、目标切换可发现性（O-8 无 UI 提示）、防御指令零使用。

## R-1 剧情链断点诊断（「继续」续跑 · headless 实证）

- **目标**：R-1 story_phase=0 + 试玩者「看见感叹号触发对话完,就不知道怎么继续触发boss战」→ 定位是代码断点还是引导缺位。
- **复现脚本**：`tools/dev/_story_chain_repro.gd`（`extends Node` + 配套 `.tscn`，对齐 `evidence/_o5_smoke_main.gd` 范式，绕开 WorkBuddy Bash shim 坏 + `-s SceneTree` Autoload 未挂载两坑）+ Python 子进程驱动 Godot console exe。
- **关键修正**：① STEP1 前 `town.dialogue_runner.force_idle()` 关 P0 自动开场对话（否则 is_idle 门闸拦告示板交互）；② STEP3 改用 `exec.pending_battle_group` 断言绕过 SceneRouter 桥接"本端跳过"吞掉的 enemy_touched 监听。
- **结论（实证）**：`evidence/_story_chain_repro.log` 结尾 `REPRO_RESULT: ALL PASS` —— town 告示板→f1 入口锚→f3 棺前锚 三段链 + 门闸全部正确。**代码链零断点**。R-1 story_phase=0 属**引导缺位**：玩家从未对 town 告示板按 Z 接取任务，且无任务目标/路点 HUD，普通玩家走出小镇后不知所措。
- **根因证据链**：`npc.gd:45`「"!"气泡属后续 Story」(NPC/告示板「!」指引未实现) + 全项目无 objective/waypoint 系统 + O-12「❗Z」提示已就位但玩家到不了 f3。与问9「指引玩家的太少,线索太少」互证。
- **落档**：m7-external-playtest-01.md ① §2 判定：三问(B-1)全未过 / B-2「可完整玩到结尾」未过(硬阻塞) / 结尾钩子未达成 / Boss 时长未实测；② §3 缺陷清单 B-01 Blocker(引导缺位,阻断 M7 关门)·B-02 Major(敌不可见)·B-03 Major(切目标无提示)·B-04 Minor(防御零用)·B-05 Major(音频/演出缺失)·B-06 Minor(开机动画缺失)；③ §4 结论区草稿 🟡 CONCERNS（待用户拍板）；④ §5 追加复现实证日志。
- **待用户拍板**：① M7 门最终判定（🟢/🟡/🔴）；② B-01 修复是否开工（告示板「!」+ 常驻任务目标 HUD + 入图路径提示，纯 UI 层无数值/战斗重构）；③ 是否关 M7 门（发布签字须人工审批）；④ R-3 重录 m7-gameplay.avi（现有 avi 为 O-6 前录制已作废）。
- **协作纪律**：本会话落档属 production 文档交付一部分已执行；修复 B-01 代码须等用户明确指令（无许可不 Write/Edit 生产代码/不 commit）。

## B-01 引导缺位修复落地（23:5x~00:5x，用户拍板「按建议执行」）

- **改动清单（全部在工作区未提交）**：
  1. `scripts/ui/quest_objective_hud.gd`（新，+ .uid）——常驻任务目标 HUD：UILayer 常驻（town 首装跨图复用），`story_phase_changed` 驱动 + `_ready` 直读 GameData 初始同步（带档启动不重放信号）；文案表 OBJECTIVES：0=「◆ 查看告示板，接取委托」/1=「◆ 前往遗迹一层，调查异常亮光」（与 story_p1_dispatch 委托原文对齐）/2=「◆ 深入遗迹第三层，调查石棺」/≥3 隐藏。左上 (8,32)（地图名 HUD 之下）；PRESET_FULL_RECT 坑规避（等值锚点+直设 size）。
  2. `scripts/maps/town_map.gd` 三处增量——`_assemble_quest_objective_hud()`（菜单/HUD 同款守卫：按脚本判重防重复装配）挂 `_assemble_map_name_hud` 之后；`_attach_billboard_hint(inv)`（`_assemble_content_points` 内告示板映射分支调用）——告示板上方「❗Z」金色脉冲标签（O-12 同款手法），场景根直铺防 y-sort 遮盖，**phase>=1 门控隐藏**（装配时 GameData 现状判定 + story_phase_changed 回调）；`billboard_hint`/`quest_objective_hud` 实例变量供测试对表。
  3. `tests/gut/test_b01_guide.gd`（新，+ .uid）13 用例：A 组 HUD 纯逻辑 5（phase 0/1/2/3 初始同步+映射表口径）/ B 组信号驱动 4（0→1/1→2/→3 隐藏/幂等）/ C 组装配级 4（真 town.tscn：HUD 在位+随 phase 落显、告示板「!」在位+门控隐藏、带档进镇即隐藏、二次装配防双实例）。
  4. `tools/dev/_b01_smoke_verify.gd` + `.tscn`（新）——生产装配冒烟（`extends Node`+`.tscn` 直启范式）：真 town 装配 4 项断言全 PASS（`evidence/_b01-smoke.log`）。
- **测试实证**：GUT **551/551 全绿**（36 脚本 8535 asserts，`evidence/_b01-gut-full.log`；新增 13 全过+存量 538 零回归）。
- **⚠️ 新坑入册（GUT 存档隔离坑变体）**：首跑 551 中 1 假红（test_e5s5 test_d1 DEFEAT 用例 phase 被回滚到 0）——根因**沙盒 APPDATA 自身残留旧 save.json**（此前会话 m6t41 fake-Main 链写盘遗留）：DEFEAT emit 被真实 BattleResultHandler 桥消费 → load_save() 读到残留档 → GameData 回滚。**跑测前必须先清沙盒存档**（或跑测脚本加清理步骤）；「APPDATA 重定向」只隔离真实用户档，不隔离沙盒自身历史。
- **⛔ 本批不含**：告示板提示的美术图标帧（现用文字「❗Z」，打磨期换）；commit/重导出/03 批次重打（等用户发话——修复须随重导出进包才能复测）。
- **下一步（待用户）**：① 实机目检（源码直跑看 HUD+「!」效果）；② 拍板是否重导出+重打 04 批次 zip（B-01 修复须进包）；③ 复测后 M7 门判定；④ commit 时点。

## 04 批次重导出闭环（2026-09-13 02:1x，用户拍板「②不要等①」）

- **实机目检已拉起**：源码直跑 GUI 进程稳定（pid 5164；DETACHED_PROCESS 被沙盒拦——run_in_background 普通 Popen+p.wait() 托住是唯一可行路径，第三次实证）。等用户目检画面。
- **导出**：`--export-release` EXIT=0 零 WARNING（`evidence/_b01-batch04-export.log`）；exe 109,132,800 B `01F7DCF0…`（逐字节不变，引擎模板恒定）；**pck 3,416,012 B `BFDD4722F46D162F…`**（+3.6KB=B-01 套件）。
- **pck 抽验坑（新）**：字节扫中文串（查看告示板/清溪镇/已存档 等 03 已实证在包内的串）**全部未命中**——pck 内 GDScript 编译成 .gdc token 流，中文字符串常量非明文 UTF-8，字节扫中文是**假阴性**；ASCII 路径串（quest_objective_hud 命中×4）在资源引用表明文。**pck 内容判定的可靠手段=运行级冒烟，不是字节扫**。
- **导出包冒烟 PASS**（`evidence/_b01-batch04-smoke.log`）：常驻根就绪 + `[TownMap] 任务目标 HUD 装配完成（QuestObjectiveHud 常驻）`（B-01 在导出包内生效的运行级实证）+ P0 锚点 + 零 SCRIPT ERROR。尾部 2 条 Physics2D ERROR=CTRL_BREAK 强杀清理噪音，非脚本错。
- **04 zip**：`F584BB5128730A79…69B19BA4`（42,029,052 B，40.08 MiB，**14 条目**=exe+pck+CREDITS+licenses/12，CRC OK，zip 内 exe/pck 与 win/ 逐字节一致）；03 旧 zip 退役 `export/_RETIRED-21-50-batch03.zip`。坑：03 zip 带**顶层目录** `轨迹残响-v0.1.0-slice-win64/`，重打必须对齐（首版 2 条目扁平结构已作废重打）。
- **指纹回写**：回收档 §0.3 已落（04 批次三件指纹+验证记录+「R-1 复测须重新发放」标注）。
- **导出产物落名坑**：`export_path="export/win/{preset_name}.exe"` 模板占位符会生成 `{preset_name}.exe/.pck` 新文件（不覆盖同名正式件）——导出后须手动归位覆盖正式名。
- **待用户**：①实机目检结论（游戏已拉起在桌面）；②04 批次发试玩者复测（AI 无渠道，用户手工发）；③复测回收后 M7 门判定；④commit（B-01 套件+04 证据+回收档更新，等发话）。
