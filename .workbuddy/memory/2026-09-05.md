
## T5（M6 收口）①②——测试核验 + A8 行6 勾绿（00:16-00:30）
- T5-1 ✅ GUT 全量 513/513 PASS（8370 asserts，51.4s，EXIT=0），证据 `evidence/t5-m6-gut-run.log`；尾部 RID 噪音为已知引擎退出噪音。
- T5-2 ✅ A8 行6 勾绿：EPIC-6 五 Story 14 框全翻 [x]；战斗 GDD §8"三种结局流程完整"打勾；**一致性回填** EPIC-4（10 框）/EPIC-5（14 框）补勾（当时走 m4/m5-acceptance.md 未回翻框）；EPIC-6 M6 收口段补状态注记。
- 新建 `evidence/m6-closeout.md`（镜像 m3-closeout 结构：门1/2/4 PASS，门3 视频/门5 tag 待用户）。
- 视频 #6 口径确认（handoff:110）：main.tscn 正常入口手动录屏拍 M6 内容，非 auto-demo 链路；必录分镜五项已写入 closeout。
- 未 commit（无指令不提交）；等用户录 `evidence/m6-gameplay.avi` 回传后走 tag m6 + push。

## T5（M6 收口）③——视频 #6 录制完成（00:30-01:30）
- 派程基岩建 `evidence/_m6_auto_demo` 三件套（克隆 M5 驱动器改造）：**main.tscn 正常入口**（实例化真 main.tscn 接管 current_scene，P0 双守卫走生产路径）、五镜头全拍无裁剪（P0 开场/菜单三页/胜利结算/逃跑/两段聊天）、存档备份-移除-恢复三段协议（本次运行前无用户档，demo 档清除恢复 SMK-12 纯净）、两轮 dryrun 逐字节一致、GUT 513/513 零回归。
- **录制 PASS**：`evidence/m6-gameplay.avi` 4743 帧 @30fps=2'38"，336MB，录制日志零 ERROR，终态五镜头全 PASS（凯尔装备/背包 potion_s=2/chat_f2_01_seen/指纹 ruins_f2 (384,40) phase=2）。
- **⚠️ 程基岩发现两处真实产品缺陷（demo 已补线，M7 修复）**：① road.tscn 敌人 return_map="road" 短名战后回图被拒；② road/f2 聊天点全局 executor 缺 runner 注入，正常入口聊天不开演（T4 验收走 demo 注入路径未覆盖正常入口）。已记 m6-closeout R4/R5。
- m6-closeout 门3 已翻 PASS；门5 tag m6+push 待用户验收视频后发话。

## 进度快照更新（05:15，用户指令）
- MEMORY.md 进度节与 sprint6-handoff.md §一/任务表同步至 **T5 收口版**：T5-1/2/3 ✅，T5-4（tag m6+push）⏳ 等用户验收视频；新增 R4/R5 缺陷记录（M7 修复项）。

## M7 推进（10:00-11:30）——B1-B5 调校 + 剧情统稿 + 导出包
- **R4/R5 修复已提交**（`b827a63`，先行会话完成）。
- **E7-S1 B1-B3 调校已提交**（`e3612e2`）：B3 火蜥/冰晶数值上调 + 飞蛾 2→3；GDD §7 同步；GUT 514/514。
- **E7-S1 B4/B5 调校完成（未 commit）**：遗像守卫 HP 240→360、遗迹核心 HP 480→600（仅 HP）；模拟器 `_m7_balance_sim.gd` 双路径验证——B4 NORMAL 5 回合（sweep/poison 逼治疗+净化）、B5 6 回合（charge×2）；承伤落 §3.6 分档；GUT 514/514。证据 `evidence/m7-balance-sim-b4b5-v5.log`。
- **E7-S2 剧情统稿完成（未 commit）**：p2_ruin_enter / p3_boss_front / p3_finale 三文件 6 处【占位】润色移除；旁白括号+portrait 补齐；口吻对齐 P0/P1；GUT 514/514（`evidence/m7-e7s2-gut.log`）。
- **E7-S3 导出包完成**：模板 1.28GB 下载（两次中断后 curl 断点续传成功）→ 解压 35 文件至 `.godot_user_tmp/Godot/export_templates/4.7.2.stable/` → 新建 `export_presets.cfg`（Windows Desktop x64，all_resources + 递归 `**` 排除）→ 导出 `export/win/轨迹残响.exe`(109MB)+pck(3.1MB) → headless 启动 EXIT=0（常驻根就绪+切 town）。
- **导出预设入库**：`9cc0de5` commit + push（.gitignore 移除 export_presets.cfg 排除行 + 新增 `.godot_user_tmp/` 排除）。
- **新坑记录**：tpz 剥前缀/模板装重定向 APPDATA/exclude 递归 `**`/导出前先建目录/中文 .ps1 须 UTF-8 BOM/.tres Dictionary 键字母序——详见 MEMORY.md 环境节。
- **剩余**：非开发机实测导出包（用户另一台机器）；外部试玩者锁定（三问测试硬阻塞）；B4/B5 tres + 剧情 + 测试 + 证据未 commit（等用户指令）。

## 工作室模式阶段诊断（11:32，游承峰 Phase 0）
- **判定**：Phase 1-5 ✅ 全完成（门 M1-M6 已 tag）→ **Phase 6 打磨收尾中（95%）→ 立即接 Phase 7 发布**。项目不重跑早期阶段。
- **门控四态**：E7-S1 数值调校 CONCERNS（内部 B1-B5 全绿，卡"≥1 名外部试玩者"三问第 3 条）／E7-S2 剧情 ✅ 实质完成未入库／E7-S3 导出 CONCERNS（产物已出，非开发机未实测）／E7-S4 收官清单 ❌ 未开工。
- **缺口**：①外部试玩者（硬阻塞·需用户）②非开发机实测（硬阻塞·需用户）③E7-S4 三 GDD done 勾选 + CREDITS 终审 + 视频 #7（可做）④18 项变更入库 + tag m7（可做，无指令不提交）。
- 引擎/平台已定（Godot 4.7.2 / Windows x64），已向用户问评审强度（full/lean/solo）与优先级，待拍板后派单。

## M7-T1/T2 派单与 429 事件（11:48-12:07）
- 用户拍板：lean 阵容（quality-lead + release-ops-lead）+ 先清内部可做项。建团队 cordit-m7-closeout，spawn 严守真（yan-soujin）/ 路远行（lu-yuanxing）并行。
- **两成员 12:00 前后撞 429 频率限额（19:48 UTC+8 重置）——但 7 份交付物已全部落盘**（qa×4 + release×3，11:53-12:00 写入），只是 SendMessage 回传失败。产物有效，未重跑。
- 主理人直接读成品汇编（未编造成员结论）。关键判定：
  - **严守真 QA 门控：🟡 CONCERNS**（本轮实跑 GUT 514/514 + dryrun PASS + 导出 EXIT=0，pck 抽检 8/8、含 M7 终稿钩子句）。阻塞仅 2 条外部验收（试玩者+非开发机，一次试玩同收）。⚠️ **O-4：方向键/空格/回车全无效**（全仓只绑 WASD/Z/E/C/X，ui_* 零引用）——普通试玩者会以为游戏坏了，强烈建议试玩前修（D-7：project.godot input 段 + 重导出，~15 分钟）。O-1 首启不落盘建议不修。
  - **路远行：版本号建议 v0.1.0-slice**；⚠️ **P0：bart 城堡件 2 文件在 git 仓库内**（design/art-bible/mockup/_src/castle_tiles.png + castle_tiles2.png，GPL/CC-BY-SA 多许可未声明选用档）——**不阻塞导出包分发**（pck 实测 0 命中），阻塞"仓库公开发布"路径。P1：随包 CREDITS.txt+licenses 缺失。Fusion Pixel 字体未被引用（决策点）。
- 新证据：evidence/m7-qa-gate-{gut-full,dryrun,export-launch}.log。跑测新坑：MSYS2 下 `-s` 参数须正斜杠（反斜杠被吞）。
- 试玩回收负反馈预案：冰晶弱点火→雷（改 1 个 .tres 字段），不动公式/倍率。

## 拍板执行与汇编收尾（12:07-14:15）
- **用户三项拍板**：①O-4 今晚限额重置后派单修 ②bart 轻处置 ③试玩包修完再发。
- **bart 轻处置已执行**：`ad2efa2` 移除 castle_tiles.png/.import ×2（暂存只含这 4 个删除，未污染 18 项工作区变更）。_src 目录工作区本已整体清理，git 索引残留一并清掉。
- **O-4 挂定时单**：automation `automation-1788583905795`，19:50 触发（429 19:48 重置），任务=input 映射修复+GUT 514/514 复核+重导出+headless EXIT=0 验证。
- **汇编联动点（已并入 automation）**：playtest-kit §3 操作表按"方向键没反应"现状撰写（§1-§5 可直接转发、语言平实、SmartScreen 过关指引齐全）——O-4 修好后须同步改为"方向键/空格/回车可用"，否则误导试玩者。
- changelog 头部核验通过（面向玩家口吻，一句话版本定位准确）。

## 第二轮拍板（14:15）与批次任务定稿
- **新三项拍板**：①CREDITS 今晚一起清 + 随包署名落地（P1）②Fusion Pixel 字体接上用 ③O-1 不修（维持严守真建议）。
- **automation 已扩充为 M7 收官批次作业**（`automation-1788583905795`，19:50 触发）：O-4 input 修复 + 字体接入（gui/theme/custom_font → fusion-pixel，注意关抗锯齿）+ CREDITS 按 audit §6 修订 + 随包 CREDITS/licenses 进 pck（include_filter 或导出后复制二选一）+ GUT 514/514 + 重导出 + pck 抽检 + playtest-kit §3 操作表同步。红线：不 commit/tag/push，完成后列变更清单等发话。
- O-1 处置：不修，已记录在 m7-final-qa-gate.md（触发窗口窄，损失约 1.5 分钟，建议不修）。
- **M7 剩余待办全景**：今晚批次作业（自动）→ 用户录视频 #7 → 用户发试玩包收三组数据 → 负反馈则冰晶弱点火→雷 → 拍板 commit + tag m7 + push → 🟢 切片完成。

## 试玩者锁定（14:35）
- 用户确认找到试玩者，B-1"人选"变量解除。发包时点仍按拍板：今晚批次作业跑完后发（最终操作体验 + 试玩包 §3 已同步更新）。
- 已提醒用户：人选须未接触过项目开发过程/未听聊过细节，保证三问反馈是主动自发的。

## 用户试玩导出包反馈处置（15:08-16:00，三连反馈）
1. **"第一句对话按什么都没反应"** → 诊断：对话推进只认 interact=Z/E（GDD 规格），无任何键位提示；非死机（日志 story_p0_intro 开演后无异常）。已告知用户按 Z/E，键位提示 UX 修复未拍板。
2. **"对话完成后定在画面不知道干嘛"** → 诊断：P0 后自由探索属设计，但 **story_quest_accept（接委托→phase 1）事件全项目零触发器接线**（仅 JSON+测试引用），P0 后剧情链断裂：告示板只播风味文本、进遗迹 P2 剧情因 phase=0 被静默跳过。修复方案已呈现（挂告示板交互），**用户尚未拍板**。
3. **"说话加点头像，太空了"（用户拍板 B 方案，已落地未 commit）**：
   - **根因①（重大）**：faces 集散图（288×400）顶部 144px 是标题带，**人脸行实际 y=144/192/288/336**；旧 portrait_catalog 按 y=row×48 取图 → 游戏里"头像"裁到的是标题条碎片，等于没头像。像素统计+逐格目检修正（`evidence/_faces_grid_check.png`）。
   - **根因②**：48×48 头像窗太小视觉上"没有"。用户拍板 2× 整数放大 96×96（Nearest 无糊化，推翻"48 原生零缩放"旧冻结）。
   - **B 方案内容**：portrait_catalog 重建 24 格（三主角 normal/smile/angry 各 3 格 + 14 NPC 各 1 格 + 1 备用；残响·男声/女声按设计无脸降级）；dialogue_box.tscn/.gd 头像窗 96×96（STRETCH_SCALE + PORTRAIT_DISPLAY_SIZE=96）；71 处对话 JSON 回填 portrait（空串占位也补，非空不动）。
   - **验证**：GUT 514/514 PASS（`evidence/_m7-portrait-gut.log`；test_H3 的 48×48 断言锚的是 AtlasTexture 区域尺寸，显示层放大不冲突）。
4. **待办沉淀**：① 用户游戏内目检头像效果；② 接委托接线修复待拍板；③ Z/E 键位提示待拍板；④ 导出包需重出（本次改动未进 pck）。

## M7-R6 修复批次完成（16:00-17:40，派单程基岩，主理人核查通过）
- **接委托接线 ✅**：story_quest_accept 加 `story_phase==0` 条件守卫（story_p0_intro 同款口径）；investigate_point 启用 get_event_id() 预留协议（setup_events 注入 + quest_event_id 覆盖属性解决 inv_town_02→story_quest_accept 键名不一致）；on_interact 改"事件路径优先"，条件不满足/未注入一律回落 flavor（E4-S5 行为零变化）；map_events.assemble 加可选三参、town_map 透传接线。phase>=1 后告示板回落风味文本。
- **Z/E 键位提示 ✅**：ContinueHint 16px→56px 宽、文本 "Z/E ▼"，visible 逻辑零改动。
- **GUT 523/523 全绿**（514+9 新增，test_m7r6.gd 锁 A/B/C 三层行为），证据 `evidence/_m7-r6-gut.log`。
- **导出包重出 ✅**：17:40 覆盖 `export/win/轨迹残响.exe`+pck（含头像修复+本批改动，pck 抽检含 story_quest_accept 与 "Z/E" 文本），headless 冒烟 EXIT=0（`evidence/_m7-r6-export*.log`）。
- **未 commit**；遗留：非开发机试玩回收、test_e5s4 注释措辞过时（断言仍真）、playtest-kit §3 告示板描述待同步。
- ⚠️ 仓库 HEAD 有非本会话 commit `ad2efa2`（移除 bart 城堡件，许可合规清理）——来自用户/另一会话，勿混淆。

## M7 收官批次（19:50 自动化派单，O-4/字体/CREDITS/导出）
- O-4 修复：project.godot [input] 四向补方向键、interact 补 Space/Enter（原 WASD/Z/E 保留）。
- 字体：gui/theme/custom_font 接 fusion-pixel ttf；.ttf.import antialiasing 1→0（超红线白名单，已向用户标注）；--import 重导入 + headless 启动无报错。
- CREDITS.md：A 区补文件名列、A7 改"已接入"、B 区删 2 悬空移 E 区、C 区补注、新增 F 区（bart 件记 ad2efa2 移除）。
- export_presets.cfg：include_filter=assets/CREDITS.md,assets/licenses/**（随包署名走 preset 机制）。
- GUT 523/523（M7-R6 后基线）；导出 EXIT=0（APPDATA 须重定向 .godot_user_tmp——复踩确认）；pck 3.20MB 含 CREDITS/licenses/fusion-pixel；pck 中 castle_tiles 命中=CREDITS F 区声明文本，非素材，虚惊。
- 导出包 headless 启动 EXIT=0，P0 正常开演。
- playtest-kit §3 操作表已更新（方向键/空格回车），§7.3/7.4 现已过时未动（任务限定只改 §3）。
- 未 commit，等用户发话。

## M7-R6b 收尾杂项完成（20:10-20:2x，程基岩，主理人核查通过）
- test_e5s4 test_c1 注释+断言消息措辞修正（断言本体零改动）；playtest-kit §3 复核**无告示板描述命中**（记忆条目过时，待办撤销），未动文档。
- **GUT 干净环境 523/523**（`evidence/_m7-r6b-gut-clean2.log`）。插曲：首轮 522/523——根因是 `user://` 出现**用户真实存档**（20:01 写入，用户开发机试玩中），e5s5 d1 的 DEFEAT 读档链路真调 load_save() 回滚 phase → 断言失败。**非本次改动引起**。
- **新坑记录（重要）**：开发机上有真实存档后，全量 GUT 会因 e5s5 d1（隐含假设 user:// 无档）污染失败；临时规避 = 重定向 APPDATA 跑测。
- **待拍板**：e5s5 d1 测试隔离缺陷——建议加"存档备份-移除-恢复"三段协议或重定向 SaveManager 路径（demo 三件套有现成做法）。
