# 2026-09-01 工作日志

## Sprint 5（EPIC-5 剧情串起来）启动 · 全权委托制
- 用户拍板：沿 M4 先例，全权委托制推进 Sprint 5（选项 A）。
- 主理人完成阶段诊断：M1-M4 已收口（tag m1-m4 已推远程，m4=07457c5），当前进入 Phase 5 制作阶段，下一站 M5。
- TeamCreate 建 `sprint5-epic5` 团队；任务链 7 条已建并锁串行依赖：
  - #1 E5-S1 DialogueRunner 状态机 + 对话框完整版（8h，P0）
  - #2 E5-S2 事件 JSON 加载器 + schema 校验器（4h，P0，依赖 #1）
  - #3 E5-S3 story_phase 机制（2h，P0，依赖 #2）
  - #4 E5-S4 剧情四拍 JSON 占位 + 12 NPC 事件（1.5h，依赖 #3）
  - #5 E5-S5 Boss 事件锚点（1h，依赖 #4）
  - #6 E5-QA 回归 + 冒烟 + M5 门槛核验（依赖 #5）
  - #7 M5 收口：视频 #5 + 验收档 + commit + tag m5（依赖 #6；commit/tag 前向用户确认）
- engineering-lead（程基岩）已 spawn 开工 E5-S1，任务 #1 置 in_progress。
- **E5-S1 ✅（12:05 前后收口）**：程基岩回传，主理人抽查证据核验通过——GUT 313/313 全绿（286→313，新增 26 条），冒烟五通道全 0，零失败用例；交付 dialogue_runner.gd（497 行四态机）/dialogue_box.gd/portrait_catalog.gd + dialogue_box.tscn + test_e5s1.gd（26 条）+.uid 齐全。
- 裁决①：GDD §3.1 带 choices 条目可省略 next（按示例口径，不改 GDD 正本；运行时缺省兜底 "END" 防病态数据）。
- 裁决②：E5-S2 采纳程基岩建议——先抽独立 schema 校验器（events+dialogues 两路复用）再落加载器/trigger 薄壳。
- 任务 #2（E5-S2）已派程基岩开工，基线 313/313 不可回退。
- **E5-S2 ✅（13:30 前后收口）**：首轮误放 scripts/data/ 被 e3s1 架构哨兵拦截；首轮 367/344（test_e5s2 编译失败级联）三轮迭代收绿。终态 367/367 全绿（+53 条，sanity 10），Orphans 10016 持平零新增泄漏。交付：schema_validator.gd（scripts/dialogue/，两路复用）、event_loader.gd + event_executor.gd + trigger_event_shell.gd（scripts/events/）、test_e5s2.gd（53 条 A-I 九组）。主理人核验：日志实测 367/367（UTF-16 编码需解码）、check_flag 全仓 0 命中、文件+uid 齐全。
- S2 四项裁决：①"9 种动作"按全量 10 type 落地（GDD 表格 wait/play_sfx 并行计数所致）；②trigger 统一化以脚本兑现，不建空壳 .tscn；③Godot JSON 恒产 float——整数字段"float 收/整值验/int 落"三步处理；④E4 镜像 JSON 无 events 键返回 skipped:true，与事件体系并存不互扰。
- 顺带修复：runner 首条目无 portrait 时头像残留真洞；dlg_story_quest_accept.json 规范化重写（原内嵌 set_flag 违反 §3.1）。
- 任务 #3（E5-S3 story_phase）已派程基岩开工，基线 367/367 不可回退。
- **14:06 第二次断网**：程基岩 S3 干了 29 分钟被断（502 同款）。网络恢复后核验：test_e5s3.gd + e5s3-gut-s3.log 已落盘，状态 384/378/6 挂（半成品）。主理人代读日志（iconv UTF-16 解码）归类 6 条失败：核心模式="phase=0 选了 p12 档"（疑 pick_phase_id 或 story_phase 初值/类型问题）+ inv_town_01.json 数据缺失级联。已发失败清单+线索给程基岩收尾。
- 网络当日已两断，若再断建议用户查路由器/网线稳定性。
- **14:26 第三次断网**：S3 修复轮未完成（日志仍 384/378/6 挂 @14:01）。网络恢复后已重发收尾指令（修全绿 + 五通道冒烟 + 根因回传）。当日网络稳定性差，断网三连已显著拖慢 Sprint 5 节奏（S3 一项三次中断）。
- **14:45 aborted 后重拉成员**：程基岩第三轮 18 分钟 aborted（非断网，网络本身 HTTP 200 通），零文件落盘。判断连断后上下文不稳，spawn 干净的 engineering-lead-2 接手 S3 收尾（prompt 含：6 挂清单+线索+落盘成果快照+环境坑全量）。教训：成员连续异常中止 2 次以上，与其反复唤醒不如重拉干净会话（落盘成果不受影响，以文件为准交接）。
- **15:10 新程基岩诊断回传（质量高）**：修正主理人两处日志侧误读（a4 实为 a3 级联误标、b2 断言顺序非根因），锁定 4 独立根因：A=interaction_controller.gd:93 事件 id 拼接 bug（四段 id 截成两段）；B=pick_phase_id 无错但运行时 story_phase 已是 1（疑跨套件 autoload 污染，实证中）；C=损坏档 JSON 引擎 ERROR 需照 E4-S1 test_08 先例声明预期错误；D=inv_town_01.json 缺失补数据。主理人护栏：B 修复方案先回传裁决禁顺手改全局；A 只修拼接点禁整文件回滚。
- **15:28 第四次断网**：A/C/D 修复已落盘（interaction_controller.gd/test_e5s3.gd/inv_town_01.json 三文件有改动），B 实证未做。已发续跑指令。当日断网四连，网络稳定性已成分瓶颈。
- **16:02 第五次中断**（HTTPS 证书验证失败，代理链路问题）。15:28 后 interaction_controller.gd + test_e5s3.gd 再有改动（A/C/D 验证轮推进中），但全量复跑未跑成（日志仍 14:01 版 384/378/6）。已要求程基岩先报停点再续跑，并授权"交接信息优先于修完"。
- **16:2x 停点快报 + B 根因锁定（关键成果）**：A/C/D 单文件验证 17/17 全绿 EXIT=0。**B 根因=test_e2s4.gd 跨套件泄漏**：test_DEFEAT读档成功 设 story_phase=7 → load_save 回滚到 7 → 该文件 before_all/after_all 快照（E2 时代写的）不含 story_phase 字段 → phase=7 经 e4s1/e4s5/e4s8 的 before_all 接力保存污染全链 → e5s3 在 phase=7 下映射 {0:p0,1:p12} 取键 1 开 p12。单文件干净/全量必挂已复现对照。修法（已批准）：e2s4 快照补 story_phase 备份/还原（仅该文件内，非全局）。
- **16:40 E5-S3 ✅ 收口**：主理人核验（日志实测 384/384 全绿、Orphans 10016 零新增、[Failed]=0、冒烟五件套 16:40 齐、e2s4 堵口 7 处引用在案）。6 挂=4 独立根因+2 冒烟演化适配：①e2s4 跨套件泄漏（3 行快照堵口）②controller 事件 id 拼接（剥前缀+序号段回拼）③a3 引擎错误按 E4-S1 先例声明 ④inv_town_01.json 补数据+d2 循环驱动修正；冒烟侧 e1s5 A4 绕行（NPC 碰撞盒挡路，神父24,9/长者12,24/主妇16,35）+ e1s6 dialogue_finished 契约对齐（npc_id→映射脚本 id，兼容语义 GUT d3 锁底）。基线抬至 384/384。
- **⚠️ 工程纪律（新增，长期有效）**：GameData 加新字段时必须 grep tests/gut 里含 before_all 快照的文件，把新字段补进备份/还原对（e2s4 式跨套件泄漏坑）；GameStateSnapshot helper 单测基类列入技术债候选。
- 任务 #4（E5-S4 剧情四拍 JSON 占位 + 12 NPC 事件）已派程基岩-2 开工，基线 384/384 不可回退。
- **16:53 第六次断网**：S4 中断，但断网前已落盘 story 四拍对话 JSON（p0_intro/p1_dispatch/p2_ruin_enter/p3_finale）+ story_anchor.json + story_quest_accept.json。已发续跑指令（补 NPC 事件 JSON + 三切换点 + done 标准验证 + 全量/冒烟）。
- **17:1x E5-S4 ✅ 收口**：主理人核验（393/393 全绿、Orphans 持平、四新 JSON 合法、set_story_phase 全数据面=3 处接线确认）。零产品代码改动（硬标准达成）。裁决三项：①平铺+story_ 前缀承载 GDD §3.5 子目录语义（schema 加载口子目录支持 defer 后续 sprint）；②菲奥拉实体锚点顺延 npc_13（5 实体+事件表兑现 6 席配额，不动 town.tscn）；③冒烟日志实为 UTF-8（前次 UTF-16/iconv 结论系 Read 工具显示误报，作废）。新增纪律：JSON 文本占位禁 ASCII 引号（『』替代）。probe*.txt 13 个中转文件待清。
- 任务 #5（E5-S5 Boss 事件锚点，冲刺最后一个工程 Story）已派程基岩-2 开工，基线 393/393。S5 核心=battle 动作中断恢复（S2 预留 pending_battle_group 兑现）+ 探索边缘 3（失败读档后事件可从头再触发）。
- **13:33 网络中断事故**：程基岩 S3 开工即 502（getaddrinfo ENOTFOUND open.bigmodel.cn）。定性：本机→路由器物理链路断（网关 ping 100% 丢），非代理问题；S3 无半成品落盘，现场干净。13:35 用户修复，baidu/bigmodel 双通确认后重发 S3 任务书续跑。教训：成员报 502 先 ping 网关分段定位，curl 注意环境代理变量干扰（`--noproxy "*"` 才是真实直连结果）。
- 风险登记：仓库体积 ~100MiB（M4 视频 89.3MB 入库后），M5 视频 #5 需评估 Git LFS；tools/README.md 未登记工具链（遗留小债）。
- **9/2 晨交接**：应用户要求产出会话交接正本 `.workbuddy/memory/sprint5-handoff.md`（状态总览/S5 任务书/全部裁决/工程纪律/环境坑/风险/团队状态），新开会话直接喂该文件即可无损续跑。
