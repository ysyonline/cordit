# 2026-09-01

## 推送受阻——环境配置蒸发事件（08:54）

- **现象**：M4 收口后推送失败。origin 从 SSH（git@github.com）变回 HTTPS；`~/.gitconfig` 昨夜配置的 `http.https://github.com.proxy=7892` 消失；`~/.ssh/config` 与昨夜生成的 ed25519 密钥**整体消失**（`.ssh/` 只剩 2023 年旧 id_rsa）。`.gitconfig` mtime=08-31 08:48（昨夜凌晨会话的配置没落盘到这台用户，或被某进程回滚）。
- **网络诊断**：直连 GitHub TCP 通但 TLS 握手被重置（20.205.243.166:443，典型墙）；SSH 22 与 ssh.github.com:443 同样被重置；本机代理端口 7890/7891/7892 + 全部 25 个监听端口逐一测试均非可用代理——**代理软件当前未运行**。
- **已恢复**：`git config --global http.https://github.com.proxy http://127.0.0.1:7892`（代理一开即生效）。
- **待用户**：①打开代理软件（昨天用的是 7892 端口那个）②确认是否换过账户/重置过环境（昨夜配置在 weixufeng 记载、user3667 实测消失，两账户家目录均无 SSH config）③代理开了说一声，我立即重推（4 commits + tag m4 待推，tag 需 `git push origin m4`）。
- **教训**：SSH 密钥与 .gitconfig 属易失环境，推送前先 `git remote -v` + `git config --get http.https://github.com.proxy` 双查，不要信记忆。

## 推送成功（10:05，代理端口 7890）

- 用户开代理后实测：端口为 **7890**（非昨夜记忆的 7892——代理软件可能换过或改过配置）。7890 CONNECT 隧道全链路通（github.com / api.github.com 均 HTTP 200）。
- 已把 `http.https://github.com.proxy` 更新为 `http://127.0.0.1:7890`。
- 推送成功：main `0c2af4d..f5a2e5f`（4 commits）+ tag `m4`（→ 07457c5）。ls-remote 三方核验一致：HEAD = refs/heads/main = f5a2e5f，refs/tags/m4 = 07457c5。
- GitHub 对 m4-gameplay.avi 89.32MB 给了 >50MB 警告（仍收下，上限 100MB）——**后续里程碑视频建议 Git LFS 或压帧率/时长**，再大就推不上了。
- 端口漂移教训：代理配置不要写死记忆端口，推送前先探测（7890/7891/7892/7897 一串试）+ curl 实测 CONNECT 隧道。

## m1/m2/m3 补打 tag 并推送（10:08，用户拍板）

- 按 commit message 定位历史 hash：m1=f9840ed（M1 收口 SMK 12/12）、m2=1b61358（M2 收口 GUT 62/62）、m3=72e1354（M3 收口 EPIC-3 GUT 187/187）。`git tag mN <hash>` 补打，`git tag -l` 核验 m1~m4 齐。
- 推送：`git push origin m1 m2 m3` + main（含 MEMORY 快照勘误）。MEMORY「tag 真相」节同步更新为已补齐。
- 至此四个里程碑 tag 全部在远程，历史 milestone 可追溯。
