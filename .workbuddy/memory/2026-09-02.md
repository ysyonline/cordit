# 2026-09-02 工作日志

## Sprint 5 · E5-S5 修复收口（主理人代笔，用户拍板选项 A）
- 成员通道当日三连挂（killed / aborted / aborted，最后一个 6 分钟零落盘），用户拍板"主理人亲自修"。
- 全量测试从 402/410（8 失败）修到 **410/410 全绿**（EXIT=0），冒烟五通道全 EXIT=0：
  - e1s4 3/3、e1s5 4/4（wrapper 入口）、e1s6 5/5、smk 4/4、smk-e1s3 5/5。
- 证据：`evidence/e5s5-gut-s5.log` + `evidence/e5s5-smoke-{e1s4,e1s5,e1s6,smk,smk-e1s3}.log`。

## 修复清单（7 处，3 组根因）
1. **场景树报错**：`ruins_f3_map.gd` `get_tree()!=null` 守卫改 `is_inside_tree()`（Godot 4 未入树调 get_tree() 即报引擎错）。
2. **双锚确定性装配**：同位双锚改"后续锚 continue 跳过"，弃 queue_free（帧末生效同帧计数不可靠）。
3. **桥接簿记死锁（生产级）**：`battle_event_bridge.gd` `_on_enemy_touched` 首行 `if _pending.is_empty(): return` 堵死登记路径（簿记只在此函数置位）——真实游戏 Boss 胜利后剧情永续行不了。删守卫，判别职责全交 `_from_event_battle` 哨兵。
4. **桥清理口**：新增 `clear_pending()`（与 executor.clear_battle_pause 对称）；test_e2 结尾对称清理，修掉泄漏到 e3 的簿记。
5. **测试时序**：e5s5 c2 / e5s4 c1 c2 在 emit VICTORY 前显式 `_runner.force_idle()`（battle 动作的 force_idle 是帧末，真实游戏战斗历时数秒无此问题，headless 同帧须补）。
6. **e5s4 契约适配**：c1/c2 补"挂起→模拟胜利→resolve_victory"驱动段，适配 S5 的 I5 全序列 battle 挂起语义（非回归，是数据契约演进）。
7. **整数除法告警**：`battle_scene.gd:132` `i / GRID_COLS` 改 `int(i / float(GRID_COLS))`（intdiv() 在此环境报 Parser Error，勿用）。

## 环境新坑（重要）
- **GDScript 无全局 intdiv()**（至少 4.7.2 的 --script 路径报 `Function "intdiv()" not found`）——整除要么 `int(a / float(b))` 要么 `a / b` + `@warning_ignore("integer_division")`。
- 冒烟入口形态不一：e1s4/e1s6/smk/smk-e1s3 是 `.tscn` 场景入口，**e1s5 是 wrapper tscn**（headless_e1s5.tscn 不存在）；直接 `--script` 跑 .gd 会报 "doesn't inherit from SceneTree or MainLoop"。
- Godot headless 全量跑测日志 ~33MB（含中文乱码段属正常），统计行在文件尾部，用 `tail -c` + grep 提取。

## 下一步（排队）
- E5-QA：派 quality-lead 独立复核（当前为 S5 收口前主理人代笔修复，按纪律需独立 QA 门）。
- M5 收口：清理 probe*.txt（13 个）、视频 #5（先评估 Git LFS）、验收档 evidence/m5-acceptance.md。commit/tag m5 前须用户确认。
