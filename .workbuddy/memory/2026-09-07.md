# 2026-09-07 · M7-A1 town 光照层（2.5D 升级 A 路线）

## 决策
- 用户反馈"地图简陋，想 2.5D"。诊断：架构已是 5 层 TileMapLayer + Y-sort（2.5D 纵深已具备），简陋根因 = 零光照 / 建筑无体积 / 装饰密度低 + 占位件。
- 两路线：A 俯视光照增强（推荐，改动小）vs B 斜 45° 等距重构（素材全灭 + 传送正本重锚，爆炸性波及）。**用户拍板：先做第一张图（town）的 A 路线**。
- A 路线拆三档：A1 光照层（本轮）/ A2 装饰密度+占位替换 / A3 建筑体积。A2/A3 待目检 A1 效果后再拍板。

## 交付（程基岩施工，主理人核查通过）
- **新增** `scripts/maps/map_lighting.gd`：MapLighting 静态装配器（无 class_name，项目纪律），CanvasModulate + PointLight2D + GradientTexture2D 程序化光斑（零新图片资产）+ Tween 呼吸光 + 玩家随身光。
- **修改** `scripts/maps/town_map.gd`：+103/-1（那 1 行删除是 M7-R6 批次既有改动 map_events 传参，非本轮），LIGHTING_CONFIG 常量正本 + `_assemble_lighting(player)` 增量装配。
- **新增** `tests/gut/test_m7a1.gd`：7 用例 84 断言（结构组 + 配置表逐光源对表 + 坐标口径哨兵 + 幂等）。
- **证据**：m7a1-gut-full.log（**530/530** = 523 基线 + 7 新增）、m7a1-demo-dryrun.log（SMOKE_EXIT=0，MapLighting 装配日志）、m7a1-town-lighting-square.png / -inn.png（MovieWriter PNG 序列取帧，headless get_image() 为 dummy 落黑图）。
- 光源 7 处：喷泉(448,448)水光、壁炉(1304,200)炉火呼吸、存档点(1432,200)、烛光(1368,440)呼吸、封印门(504,120)幽紫、南门路灯(208,744)、客栈门灯(472,296)。

## 关键裁定（程基岩裁量，主理人认可）
- range_tiles 读作**半径**（非直径），texture_scale=range/8，防光斑糊图。
- 呼吸光 = 壁炉+喷泉+烛光 3 处，config flicker.enabled 一键关。
- 光色具体化：烛光 (1.0,0.78,0.45)、存档点 (0.85,0.95,1.0)、路灯/门灯 (1.0,0.92,0.78)。
- GUT 9.7.1 的 `-gtest` 实际不过滤，单文件跑用 `-gselect=`。
- headless 截图 get_image() 是 dummy 渲染器落黑图 → MovieWriter PNG 序列（--write-movie --fixed-fps 30）为可行采集路径（m3/m4/m5 同款）。

## 待办 / 下一步
- **待用户游戏内目检 A1 效果**（亮度/光色是否满意，CanvasModulate 0.94,0.90,0.82 可调）。
- 目检满意后拍板：A2（装饰密度 + 火炉/存档占位件替换 + 室内遮罩收口）/ A3（建筑体积）。
- A2 时收口：TeleportCatalog 室内条目评估 + 室内遮罩层（防跑图边缘露房间外草地对角）。
- **未 commit**（用户未发话）。工作区累积：R6/R7/头像修复/A1 多批次。

---

# 2026-09-07/08 续 · M7-A2 town 装饰密度 + 占位件替换（用户目检 A1 后拍板续做）

## 交付（程基岩施工，主理人核查通过）
- **占位件替换**：壁炉 (81,12) = temple (2,0,0) 暖红玛瑙纹理柱（town/forest 全图集无独立火焰件，裁量报备）；存档点 (89,12) = 16oga (6,4) 祭坛蓝晶石。均挂满格碰撞。
- **装饰 60 格**：灌木×3/草簇/红花/黄花/石堆，全部 16oga 透明底件叠 GroundDeco（z=-9），不挂碰撞；16oga 以 source_id=3 接入 town tileset（CC-BY 已在 CREDITS A 区，零登记变更）。确定性 random.Random(0x4D374132)，重跑逐字节一致（主理人复跑自证 ✅）。
- **③ 室内遮罩 = 维持现状**：几何推演 limits_inn = 视口尺寸 → 相机静止，视口与主图 tile 区无交集，黑幕已盖满限区框；A1 备案"露草"系传送瞬态（reset_smoothing 已缓解）。零动刀，证据 = inn 截图近黑像素 88%。
- **⚠️ 计划外修复（生成器漂移欠账）**：T6.5 提交 1e3c984 把 P0_Anchor 手改进 town.tscn 而 gen_town.py 未收编——A2 首次重生成触雷暴露（t65 D2/D4 2 FAIL）。已按"生成器是正本"纪律收编进 gen_town.py，未手改 tscn。
- **验证链**：verify_town **128/128**（107+21 净增，含 22 处"装饰不压点位"哨兵）；GUT **530/530**（零新增用例，零旧断言破坏）；SMOKE_EXIT=0；截图 ×2。

## 坑位新增
- gen_town.py y_sort_origin 计数断言：多 tile 共用同一 atlas 键时 tres 只写一次（计 1），拆到独立键后计数 +N——替换占位件会牵动此类"算术连带断言"。
- tile-inspect scan_candidates.py 新增（色系统计打分定位候选 tile），可复用于后续图集选型。

## 待办（更新）
- **待用户目检 A2 截图**（广场 448,520 / 室内 1304,280 机位）→ 拍板 A3（建筑体积感）是否续做。
- **未 commit**：工作区累积 R6/R7/头像/A1/A2 五批次，等用户发话。
