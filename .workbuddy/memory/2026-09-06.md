# 2026-09-06 工作日志（cordit · M7-R7）

## R2-CHARSPRITE：角色行走图裁切+换装接线 ✅（未 commit，待用户目检）
- **触发**：外部测试玩家反馈"大地图 NPC 与主角是色块"。诊断=既定占位方案（E1 的 16×18 占位矩形），R2 裁切件自 M3 顺延从未执行。
- **拍板**：①执行 R2（方案 A）；②跟随队列暂缓。
- **执行**：程基岩（eng-lead），两程完成（首程 100 回合截断，续单收口）。
- **关键实测**：330×400 不能整除 16×18——男带 y=180 / 女带 y=306，块 x=16+48c，每块 48×72（正本 `assets/characters/charset_frames.md`）。
- **素材**：R4 透明转写件（R1 提亮件是 GIF 扁平化无 alpha，背景色 251,220,126 → alpha0）+ R5 调色板变体 ×4（v1_elder/v2_porter/v3_smith/v4_shepherd；男 9 女 4 超出原生男像 6 个），先登记后复制合规。
- **架构**：`scripts/core/char_anim.gd` 单例 SpriteFrames 工厂（8 动画，16 形象按 region 切换，零纹理复制）；player=AnimatedSprite2D 四向 idle/walk 8FPS；npc=Sprite2D 静态帧 + `charset_id`/`facing` export；`town_map.gd NPC_CHARSET` 分配数据正本。
- **分配初排**：`production/npc-sprite-assignment.md`（凯尔=m1；12 NPC 按气质映射；菲奥拉无锚点未分配）。目检辅助图 `tools/dev/_r2_npc_down_sheet.png`。
- **质量门过程**：首轮回传被我拦截——char_anim.gd 四处 Python 元组赋值（GDScript 无此语法）→ preload 链断 → 39 用例连坐。修复后 GUT **523/523 全绿**（21:46 日志，零 SCRIPT ERROR）+ 冒烟 EXIT=0（"行走图 M1" 生效）。
- **新坑记录**：①GDScript 不支持 `a, b = x, y` 元组赋值（--check-only 可拦）；②Git Bash 传 APPDATA 反斜杠双层转义成 `C://…//` 双斜杠 → FileAccess 写档失败连坐存档用例（490/523 假红），**APPDATA 须用正斜杠**。
- **遗留**：Orphans ~10000 既有水位未处理；道路/遗迹敌人暗红占位块不在本单（D3 敌人包未取用）。
- **待用户**：游戏内目检形象分配 → 拍板定稿；commit 未发话不执行。
