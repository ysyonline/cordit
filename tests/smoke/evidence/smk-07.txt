SMK-07 静态检查证据 · GameData 无 IO、无信号（A3 边界）
执行日期：2026-08-29　执行人：游承峰（team-lead，代核）
被测文件：autoload/game_data.gd（对应提交 092e8a7）

检查方式：正则搜索 `FileAccess|user://|signal|emit`（源码含注释全量匹配）

命中结果：零命中。
（E1-S2 提交说明「连注释都不留关键词，保 SMK-07 静态搜索零命中」与实测一致。）

行为核对：
- GameData 不声明任何 signal，不调用任何 emit —— 跨系统通知一律走 EventBus，边界干净。
- user:// 目录动态核对（运行前后文件清单不变）归并到 E1-S3 后全量重跑时与 SMK-12 一并复核；
  本条静态部分已可判定。

结论：静态搜索零命中，无 IO、无信号。
PASS（静态部分；user:// 动态复核挂 E1-S3 全量）
